---
phase: 03-core-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/render/Style.kt
  - src/main/kotlin/geo/render/Shape.kt
  - src/main/kotlin/geo/render/StyleDefaults.kt
  - src/main/kotlin/geo/render/PointRenderer.kt
  - src/main/kotlin/geo/render/render.kt
autonomous: true
must_haves:
  truths:
    - User can create Style objects with DSL syntax (invoke() operator)
    - User can render Point geometries with configurable color, size, and shape
    - User can choose from Circle, Square, Triangle shapes for points
    - User can reuse Style objects by mutating properties (zero-allocation)
  artifacts:
    - path: src/main/kotlin/geo/render/Style.kt
      provides: Style class with mutable properties and invoke() operator
      contains: "data class Style", "var fill", "var stroke", "var size", "var shape"
      exports: ["Style", "Shape"]
    - path: src/main/kotlin/geo/render/StyleDefaults.kt
      provides: Default styles per geometry type
      contains: "object StyleDefaults", "defaultPointStyle", "defaultLineStyle", "defaultPolygonStyle"
    - path: src/main/kotlin/geo/render/PointRenderer.kt
      provides: Point drawing logic
      contains: "fun drawPoint", "Shape.Circle", "Shape.Square", "Shape.Triangle"
      exports: ["drawPoint"]
  key_links:
    - from: src/main/kotlin/geo/render/Style.kt
      to: src/main/kotlin/geo/render/PointRenderer.kt
      via: Style parameter in drawPoint function
      pattern: "fun drawPoint.*Style"
    - from: src/main/kotlin/geo/render/Shape.kt
      to: src/main/kotlin/geo/render/PointRenderer.kt
      via: Shape enum used in when expression
      pattern: "when.*shape.*Circle|Square|Triangle"
    - from: src/main/kotlin/geo/render/StyleDefaults.kt
      to: src/main/kotlin/geo/render/render.kt
      via: Public API functions that merge styles
      pattern: "mergeStyles.*defaultPointStyle"
---

<objective>
Create Style class with DSL syntax and Shape enum, with default point styles and drawPoint function

Purpose: Establish the foundation for geometry rendering with a flexible styling API that supports zero-allocation performance for real-time animation
Output: Style class, Shape enum, StyleDefaults object, and drawPoint function
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-core-rendering/03-CONTEXT.md
@.planning/phases/03-core-rendering/03-RESEARCH.md
@src/main/kotlin/geo/projection/ProjectionMercator.kt

# Prior phase patterns
@.planning/phases/02-coordinate-systems/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Style class with invoke() operator and Shape enum</name>
  <files>src/main/kotlin/geo/render/Style.kt</files>
  <action>
    Create Style class in src/main/kotlin/geo/render/Style.kt:

    1. Style class as data class with mutable var properties:
       - var fill: ColorRGBa? = null
       - var stroke: ColorRGBa? = ColorRGBa.WHITE
       - var strokeWeight: Double = 1.0
       - var size: Double = 5.0
       - var shape: Shape = Shape.Circle
       - var lineCap: LineCap = LineCap.BUTT
       - var lineJoin: LineJoin = LineJoin.MITER
       - var miterLimit: Double = 4.0

    2. companion object with invoke() operator for DSL syntax:
       ```kotlin
       operator fun invoke(block: Style.() -> Unit): Style {
           val style = Style()
           style.block()
           return style
       }
       ```

    3. Shape enum (create in src/main/kotlin/geo/render/Shape.kt):
       - Circle, Square, Triangle

    Reference RESEARCH.md Pattern 1 and Code Examples sections.
    Use var properties for zero-allocation (no val copy() patterns).
  </action>
  <verify>
    1. grep -q "data class Style" src/main/kotlin/geo/render/Style.kt
    2. grep -q "var fill" src/main/kotlin/geo/render/Style.kt
    3. grep -q "enum class Shape" src/main/kotlin/geo/render/Shape.kt
    4. grep -q "Circle, Square, Triangle" src/main/kotlin/geo/render/Shape.kt
  </verify>
  <done>
    Style class exists with mutable properties, Shape enum exists, invoke() operator enables DSL syntax
  </done>
</task>

<task type="auto">
  <name>Task 2: Create StyleDefaults object with default point/line/polygon styles</name>
  <files>src/main/kotlin/geo/render/StyleDefaults.kt</files>
  <action>
    Create StyleDefaults object in src/main/kotlin/geo/render/StyleDefaults.kt:

    1. defaultPointStyle:
       - fill = ColorRGBa.WHITE
       - stroke = ColorRGBa.WHITE
       - strokeWeight = 1.0
       - size = 5.0
       - shape = Shape.Circle

    2. defaultLineStyle:
       - fill = null
       - stroke = ColorRGBa.WHITE
       - strokeWeight = 1.0
       - lineCap = LineCap.BUTT
       - lineJoin = LineJoin.MITER

    3. defaultPolygonStyle:
       - fill = ColorRGBa.WHITE.withAlpha(0.0) // transparent
       - stroke = ColorRGBa.WHITE
       - strokeWeight = 1.0
       - lineCap = LineCap.BUTT
       - lineJoin = LineJoin.MITER

    Reference RESEARCH.md "Style Defaults per Geometry Type" section.
    Follow CONTEXT.md "Sensible defaults for geometry types" decision.
  </action>
  <verify>
    1. grep -q "object StyleDefaults" src/main/kotlin/geo/render/StyleDefaults.kt
    2. grep -q "defaultPointStyle" src/main/kotlin/geo/render/StyleDefaults.kt
    3. grep -q "ColorRGBa.WHITE.withAlpha(0.0)" src/main/kotlin/geo/render/StyleDefaults.kt
  </verify>
  <done>
    StyleDefaults object provides sensible defaults for point, line, and polygon styles
  </done>
</task>

<task type="auto">
  <name>Task 3: Create drawPoint function with shape switching (Circle, Square, Triangle)</name>
  <files>src/main/kotlin/geo/render/PointRenderer.kt</files>
  <action>
    Create writePoint function in src/main/kotlin/geo/render/PointRenderer.kt:

    Signature: fun writePoint(drawer: Drawer, screenX: Double, screenY: Double, style: Style)

    Implementation:
    1. Apply style to drawer (fill, stroke, strokeWeight)

    2. When expression on style.shape:
       - Circle: drawer.circle(screenX, screenY, style.size / 2)
       - Square: drawer.rectangle(centered at screenX, screenY with size x size)
         - Calculate: x = screenX - size/2, y = screenY - size/2
       - Triangle: Create equilateral triangle using ShapeContour.fromPoints():
         - Height = size * sqrt(3) / 2
         - Top vertex: (screenX, screenY - height/2)
         - Bottom left: (screenX - size/2, screenY + height/2)
         - Bottom right: (screenX + size/2, screenY + height/2)
         - Call drawer.contour(triangle) with ShapeContour

    Reference RESEARCH.md "Point Rendering with ShapeSwitching" and "Code Examples" sections.
    Use ShapeContour.fromPoints() with closed = true for triangle.
  </action>
  <verify>
    1. grep -q "fun writePoint" src/main/kotlin/geo/render/PointRenderer.kt
    2. grep -q "Shape.Circle.*drawer.circle" src/main/kotlin/geo/render/PointRenderer.kt
    3. grep -q "Shape.Square.*drawer.rectangle" src/main/kotlin/geo/render/PointRenderer.kt
    4. grep -q "Shape.Triangle.*ShapeContour" src/main/kotlin/geo/render/PointRenderer.kt
  </verify>
  <done>
    writePoint function renders Point geometries with Circle/Square/Triangle shapes using provided style
  </done>
</task>

</tasks>

<verification>
1. Build project: ./gradlew build
2. Verify Style class can be created with DSL: check compile success
3. Verify Shape enum has Circle, Square, Triangle values
4. Verify StyleDefaults provides three default styles
5. Verify writePoint function compiles with Drawer parameter
</verification>

<success_criteria>
Users can create Style objects with DSL syntax, render Point geometries with configurable color/size/shape (Circle/Square/Triangle), and style objects can be mutated for zero-allocation performance
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-rendering/03-01-SUMMARY.md`
</output>