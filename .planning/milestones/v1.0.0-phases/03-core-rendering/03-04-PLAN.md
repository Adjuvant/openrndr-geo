---
phase: 03-core-rendering
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/kotlin/geo/render/StyleTest.kt
  - src/test/kotlin/geo/render/PointRendererTest.kt
  - src/test/kotlin/geo/render/LineRendererTest.kt
  - src/test/kotlin/geo/render/PolygonRendererTest.kt
  - src/test/kotlin/geo/render/MultiRendererTest.kt
  - src/test/kotlin/geo/render/GeometryProjectionTest.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "JUnit tests exist for all rendering features"
    - "Tests cover Style class creation and DSL syntax"
    - "Tests cover drawPoint with different shapes"
    - "Tests cover drawLineString styling"
    - "Tests cover drawPolygon with fill and opacity"
    - "Tests cover Multi* geometry rendering"
    - "Tests cover Point.toScreen() projection helper"
  artifacts:
    - path: "src/test/kotlin/geo/render/StyleTest.kt"
      provides: "Style class testing"
    - path: "src/test/kotlin/geo/render/PointRendererTest.kt"
      provides: "Point rendering testing"
    - path: "src/test/kotlin/geo/render/LineRendererTest.kt"
      provides: "Line string rendering testing"
    - path: "src/test/kotlin/geo/render/PolygonRendererTest.kt"
      provides: "Polygon rendering testing"
    - path: "src/test/kotlin/geo/render/MultiRendererTest.kt"
      provides: "Multi-geometry rendering testing"
    - path: "src/test/kotlin/geo/render/GeometryProjectionTest.kt"
      provides: "Projection helper testing"
  key_links:
    - from: "src/test/kotlin/geo/render/StyleTest.kt"
      to: "src/main/kotlin/geo/render/Style.kt"
      via: "direct function calls"
    - from: "src/test/kotlin/geo/render/PointRendererTest.kt"
      to: "src/main/kotlin/geo/render/PointRenderer.kt"
      via: "calls to drawPoint()"
    - from: "src/test/kotlin/geo/render/GeometryProjectionTest.kt"
      to: "src/main/kotlin/geo/Geometry.kt"
      via: "Point.toScreen() method"
---

<objective>
Create comprehensive JUnit tests for all rendering features to ensure correctness and prevent regressions.

Purpose: Unit tests provide verification that rendering component behavior is correct and changes don't break existing functionality.
Output: Six test files covering Style, PointRenderers, LineRenderer, PolygonRenderer, MultiRenderer, and GeometryProjection.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-rendering/03-UAT.md
@src/test/kotlin/geo/GeometryTest.kt
@src/main/kotlin/geo/render/Style.kt
@src/main/kotlin/geo/render/PointRenderer.kt
@src/main/kotlin/geo/render/LineRenderer.kt
@src/main/kotlin/geo/render/PolygonRenderer.kt
@src/main/kotlin/geo/render/MultiRenderer.kt
@src/main/kotlin/geo/Geometry.kt
</context>

<tasks>

<task type="auto">
  <name>Create StyleTest.kt for Style class and DSL syntax</name>
  <files>src/test/kotlin/geo/render/StyleTest.kt</files>
  <action>
Create src/test/kotlin/geo/render/StyleTest.kt with tests:

1. testDefaultValues: Verify Style() creates default values (fill=null, stroke=WHITE, strokeWeight=1.0, size=5.0, shape=Circle)
2. testDslSyntax: Create Style using DSL invoke() operator, verify properties are set
3. testAllProperties: Set all Style properties and verify they're stored correctly
4. testTransparentStyle: Create transparent style using Style.transparent(0.5), verify alpha is 0.5
5. testDefaultTransparent: Create Style.transparent() with defaults, verify fill and stroke are fully transparent

Use assertEquals with 0.0001 tolerance for Double comparisons. Follow pattern from @src/test/kotlin/geo/GeometryTest.kt.
  </action>
  <verify>./gradlew test --tests geo.render.StyleTest</verify>
  <done>StyleTest.kt compiles and all 5 tests pass</done>
</task>

<task type="auto">
  <name>Create PointRendererTest.kt for drawPoint function</name>
  <files>src/test/kotlin/geo/render/PointRendererTest.kt</files>
  <action>
Create src/test/kotlin/geo/render/PointRendererTest.kt with tests:

1. testDrawPointCircle: Call drawPoint() with Circle shape, verify no exceptions (rendering visual tests not possible in unit tests)
2. testDrawPointSquare: Call drawPoint() with Square shape, verify no exceptions
3. testDrawPointTriangle: Call drawPoint() with Triangle shape, verify no exceptions
4. testDrawPointWithCustomStyle: Create custom Style with size=10, fill=RED, call drawPoint(), verify no exceptions
5. testDrawPointWithNullStyle: Call drawPoint() with style=null, verify default style is used (no exceptions)
6. testDrawPointVector2Overload: Call drawPoint(drawer, Vector2(...)), verify it works via delegate to main function

Note: Rendering tests verify function calls succeed without visual assertions (requires test framework with drawer mocking).
  </action>
  <verify>./gradlew test --tests geo.render.PointRendererTest</verify>
  <done>PointRendererTest.kt compiles and all 6 tests pass</done>
</task>

<task type="auto">
  <name>Create LineRendererTest.kt for writeLineString function</name>
  <files>src/test/kotlin/geo/render/LineRendererTest.kt</files>
  <action>
Create src/test/kotlin/geo/render/LineRendererTest.kt with tests:

1. testWriteLineStringWithValidPoints: Create LineString with 3 points, call writeLineString(), verify no exceptions
2. testWriteLineStringWithTwoPoints: Create LineString with 2 points (minimum valid), verify no exceptions
3. testWriteLineStringWithSinglePoint: Try to call writeLineString() with 1 point, verify it returns early (guard clause)
4. testWriteLineStringWithEmptyList: Try to call writeLineString() with empty list, verify it returns early
5. testWriteLineStringWithStrokeStyling: Create Style with stroke=BLUE, strokeWeight=2.0, lineCap=ROUND, verify apply to drawer
6. testWriteLineStringWithNullStroke: Set style.stroke=null, verify drawer.stroke defaults to WHITE

Reference @src/main/kotlin/geo/render/LineRenderer.kt for behavior and @src/test/kotlin/geo/GeometryTest.kt for testing patterns.
  </action>
  <verify>./gradlew test --tests geo.render.LineRendererTest</verify>
  <done>LineRendererTest.kt compiles and all 6 tests pass</done>
</task>

<task type="auto">
  <name>Create PolygonRendererTest.kt for writePolygon function</name>
  <files>src/test/kotlin/geo/render/PolygonRendererTest.kt</files>
  <action>
Create src/test/kotlin/geo/render/PolygonRendererTest.kt with tests:

1. testWritePolygonWithValidPoints: Create Polygon with 3+ points, call writePolygon(), verify no exceptions
2. testWritePolygonWithThreePoints: Create Polygon with exactly 3 points (minimum valid), verify no exceptions
3. testWritePolygonWithTwoPoints: Try to call writePolygon() with 2 points, verify it returns early (guard clause)
4. testWritePolygonWithFillColor: Create Style with fill=RED.withAlpha(0.5), verify opacity is preserved
5. testWritePolygonWithStrokeStyling: Create Style with stroke=BLACK, strokeWeight=2.0, verify styled correctly

Note: Test fill opacity using style.fill?.a or ColorRGBa withAlpha verification.
  </action>
  <verify>./gradlew test --tests geo.render.PolygonRendererTest</verify>
  <done>PolygonRendererTest.kt compiles and all 5 tests pass</done>
</task>

<task type="auto">
  <name>Create MultiRendererTest.kt for Multi* geometry rendering</name>
  <files>src/test/kotlin/geo/render/MultiRendererTest.kt</files>
  <action>
Create src/test/kotlin/geo/render/MultiRendererTest.kt with tests:

1. testDrawMultiPoint: Create MultiPoint with 3 points, call drawMultiPoint(), verify no exceptions
2. testDrawMultiLineString: Create MultiLineString with 2 line strings, call drawMultiLineString(), verify no exceptions
3. testDrawMultiPolygon: Create MultiPolygon with 2 polygons, call drawMultiPolygon(), verify no exceptions
4. testDrawMultiPointWithCustomStyle: Apply custom Style to drawMultiPoint(), verify it gets passed to drawPoint for each point
5. testDrawMultiLineStringWithStyle: Apply custom Style to drawMultiLineString(), verify consistent styling across line strings
6. testDrawMultiPolygonWithStyle: Apply custom Style with fill opacity to drawMultiPolygon(), verify consistent styling

Reference @src/main/kotlin/geo/render/MultiRenderer.kt for behavior - Multi* functions delegate to base functions.
  </action>
  <verify>./gradlew test --tests geo.render.MultiRendererTest</verify>
  <done>MultiRendererTest.kt compiles and all 6 tests pass</done>
</task>

<task type="auto">
  <name>Create GeometryProjectionTest.kt for Point.toScreen()</name>
  <files>src/test/kotlin/geo/render/GeometryProjectionTest.kt</files>
  <action>
Create src/test/kotlin/geo/render/GeometryProjectionTest.kt with tests:

1. testPointToScreenMercator: Create Point(lat/lng), create ProjectionMercator, call point.toScreen(projection), verify Vector2 result
2. testPointToScreenEquirectangular: Create Point(lat/lng), create ProjectionEquirectangular, call point.toScreen(projection), verify result
3. testPointToScreenReturnsVector2: Verify toScreen() returns OpenRNDR Vector2 type
4. testPointToScreenWithBNG: Create UK coordinate Point, create ProjectionBNG, call point.toScreen(projection), verify valid screen coordinates

Use @src/main/kotlin/geo/Geometry.kt Point.toScreen() extension method and @src/test/kotlin/geo/projection/ProjectionTest.kt patterns.
  </action>
  <verify>./gradlew test --tests geo.render.GeometryProjectionTest</verify>
  <done>GeometryProjectionTest.kt compiles and all 4 tests pass</done>
</task>

</tasks>

<verification>
1. Run ./gradlew test --tests geo.render.* to execute all rendering tests
2. Verify all 27 tests pass (5 + 6 + 6 + 5 + 6 + 4)
3. Run ./gradlew build to verify full project still compiles
</verification>

<success_criteria>
- src/test/kotlin/geo/render/ directory exists with 6 test files
- ./gradlew test --tests geo.render.* passes all 27 tests
- ./gradlew build completes successfully
- Test coverage includes Style, Point, Line, Polygon, Multi*, and Projection helpers
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-rendering/03-04-SUMMARY.md`
</output>