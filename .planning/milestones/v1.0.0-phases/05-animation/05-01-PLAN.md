---
phase: 05-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/animation/GeoAnimator.kt
  - src/main/kotlin/geo/animation/EasingExtensions.kt
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can create a GeoAnimator and integrate it with extend() loop"
    - "User can use OpenRNDR's built-in Easing enum for animation"
    - "User can call updateAnimation() each frame to progress animations"
  artifacts:
    - path: "src/main/kotlin/geo/animation/GeoAnimator.kt"
      provides: "Global animation controller with Animatable lifecycle"
      contains: "class GeoAnimator : Animatable", "updateAnimation", "extensions"
    - path: "src/main/kotlin/geo/animation/EasingExtensions.kt"
      provides: "Convenience easing functions for animations"
      exports: ["easeInOut", "easeOut", "easeIn", "linear"]
  key_links:
    - from: "GeoAnimator.kt"
      to: "org.openrndr.animatable.Animatable"
      via: "inheritance"
      pattern: ": Animatable\\(\\)"
    - from: "GeoAnimator.kt"
      to: "extend { }"
      via: "extension function pattern"
      pattern: "fun.*extend.*GeoAnimator"
    - from: "GeoAnimator.kt"
      to: "updateAnimation()"
      via: "per-frame call"
      pattern: "updateAnimation\\(\\)"
---

<objective>
Create GeoAnimator infrastructure with OpenRNDR Animatable pattern and easing functions.

Purpose: Establish the foundation for animation by integrating with OpenRNDR's built-in Animatable API, providing global animation lifecycle management and easy access to easing functions.

Output: GeoAnimator class for animation lifecycle, EasingExtensions for easing convenience, and extend() integration for OpenRNDR.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-animation/05-CONTEXT.md
@.planning/phases/05-animation/05-RESEARCH.md
@.planning/ROADMAP.md
@src/main/kotlin/geo/render/Style.kt
@src/main/kotlin/geo/layer/GeoLayer.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GeoAnimator with Animatable lifecycle</name>
  <files>src/main/kotlin/geo/animation/GeoAnimator.kt</files>
  <action>
Create package `geo.animation` and implement GeoAnimator class:

1. Create GeoAnimator as an object (singleton) extending `org.openrndr.animatable.Animatable`
2. Override `updateAnimation()` method (inherited from Animatable - per-frame update call)
3. Private internal state: `animations = mutableListOf<Animation>()` for tracking active animations
4. Public API for creating animations (empty placeholder - populated when tweening primitives added in 05-02)
5. Create extension function `fun Program.animator(): GeoAnimator` that returns the singleton instance
6. Document lifecycle pattern: "Call GeoAnimator.updateAnimation() at start of extend() block before reading animated properties"

Use existing patterns from Style.kt (companion object with invoke()) and GeoLayer.kt (DSL builder) for consistency.

Do NOT:
- Implement manual time tracking (use Animatable's built-in system)
- Create custom easing functions (use OpenRNDR's Easing enum)
- Implement custom animation loop (use updateAnimation())
  </action>
  <verify>
File compiles successfully with gradle build.
Package structure created: src/main/kotlin/geo/animation/
  </verify>
  <done>
GeoAnimator class extends Animatable with updateAnimation() implementation
Extension function Program.animator() returns singleton instance
  </done>
</task>

<task type="auto">
  <name>Task 2: Create EasingExtensions for convenience easing access</name>
  <files>src/main/kotlin/geo/animation/EasingExtensions.kt</files>
  <action>
Create EasingExtensions.kt in geo.animation package that provides convenient access to OpenRNDR's built-in Easing enum:

1. Import `org.openrndr.math.easing.Easing` (OpenRNDR's built-in easing)
2. Create extension functions for common easing patterns:
   - `fun Easing.Companion.linear() = Linear`
   - `fun Easing.Companion.easeInOut() = CubicInOut`
   - `fun Easing.Companion.easeOut() = CubicOut`
   - `fun Easing.Companion.easeIn() = CubicIn`
3. Document that these map to OpenRNDR's 13 easing families (Linear, Quad, Cubic, Quart, Quint, Sine, Expo, Circ, Elastic, Back, Bounce)
4. Add KDoc showing usage: `GeoAnimator { easing = easeInOut() }`

Do NOT:
- Import or use orx-easing library (commented out in build.gradle.kts line 24)
- Implement custom easing math (use Easing enum)
- Create external dependency for easing functionality
  </action>
  <verify>
File compiles successfully with gradle build.
Extension functions accessible after import.
  </verify>
  <done>
EasingExtensions provides convenience functions: linear(), easeInOut(), easeOut(), easeIn()
KDoc documents these are wrappers around OpenRNDR's Easing enum
  </done>
</task>

</tasks>

<verification>
- Tests verify GeoAnimator.updateAnimation() can be called (synthetic test creating dummy animation)
- Extension function Program.animator() returns singleton without errors
- Easing extension functions return valid Easing enum values
- Package structure: geo.animation/GeoAnimator.kt and EasingExtensions.kt created
</verification>

<success_criteria>
GeoAnimator infrastructure with Animatable lifecycle management
EasingExtensions providing convenient access to OpenRNDR easing functions
Extension function for integrating GeoAnimator with OpenRNDR extend() blocks
All code compiles without external dependencies beyond openrndr-animatable
</success_criteria>

<output>
After completion, create `.planning/phases/05-animation/05-01-SUMMARY.md` with:
- Tech-stack.added: ["org.openrndr:openrndr-animatable (used, not added)", "EasingExtensions.kt"]
- Key files created: ["GeoAnimator.kt", "EasingExtensions.kt"]
- Patterns established: ["Animatable lifecycle", "Global singleton animator", "Extension function pattern"]
</output>