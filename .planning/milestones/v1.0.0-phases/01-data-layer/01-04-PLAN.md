---
phase: 01-data-layer
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/GeoJSON.kt
  - src/main/kotlin/geo/GeoPackage.kt
  - src/test/kotlin/geo/GeoJSONTest.kt
  - src/test/kotlin/geo/GeoPackageTest.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Users can call .features() on GeoJSON/GeoPackage objects to get Sequence<Feature> directly"
    - "Convenience functions delegate to Source-based API internally"
    - "UAT test 1 passes: GeoJSON.load() returns Sequence<Feature> for easy access"
  artifacts:
    - path: "src/main/kotlin/geo/GeoJSON.kt"
      provides: "features() and featuresString() convenience functions"
      exports: ["features(path)", "featuresString(content)"]
    - path: "src/main/kotlin/geo/GeoPackage.kt"
      provides: "features() convenience function"
      exports: ["features(path, maxFeatures)"]
    - path: "src/test/kotlin/geo/GeoJSONTest.kt"
      provides: "Test coverage for convenience functions"
    - path: "src/test/kotlin/geo/GeoPackageTest.kt"
      provides: "Test coverage for convenience functions"
  key_links:
    - from: "GeoJSON.features(path)"
      to: "GeoJSONSource.features"
      via: "delegation: wrapper function that returns source.features"
      pattern: "GeoJSON\\.load\\(path\\)\\.features"
    - from: "GeoPackage.features(path)"
      to: "GeoPackageSource.features"
      via: "delegation: wrapper function that returns source.features"
      pattern: "GeoPackage\\.load\\(path\\)\\.features"
---

<objective>
Add convenience functions to GeoJSON and GeoPackage for direct Sequence<Feature> access, improving API usability while preserving the Source-based factory pattern for advanced use cases.

Purpose: UAT found that users expect simple direct access to features via `.features()` instead of understanding the Source object pattern. Adding convenience functions makes the common case easy while keeping the advanced pattern available.

Output: Working convenience functions on GeoJSON and GeoPackage objects that return Sequence<Feature> directly, with test coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-data-layer/01-UAT.md
@.planning/phases/01-data-layer/01-01-SUMMARY.md
@.planning/phases/01-data-layer/01-02-SUMMARY.md
@.planning/phases/01-data-layer/01-03-SUMMARY.md
src/main/kotlin/geo/GeoJSON.kt
src/main/kotlin/geo/GeoPackage.kt
src/main/kotlin/geo/GeoSource.kt
</context>

<tasks>

<task type="auto">
  <name>Add convenience functions to GeoJSON object</name>
  <files>src/main/kotlin/geo/GeoJSON.kt</files>
  <action>
Add two convenience functions to the GeoJSON object:
1. `fun features(path: String): Sequence<Feature>` - Loads file and returns features directly
2. `fun featuresString(content: String): Sequence<Feature>` - Loads string and returns features directly

Implementation pattern (delegation to existing API):
```kotlin
fun features(path: String): Sequence<Feature> = load(path).features
fun featuresString(content: String): Sequence<Feature> = loadString(content).features
```

Add KDoc documentation explaining that these are convenience functions for the common case of just getting features directly without needing the Source object's additional functionality (CRS tracking, spatial queries).

Location: Add after the loadString() function, before parseFeature().
</action>
  <verify>
Read src/main/kotlin/geo/GeoJSON.kt and verify:
1. features() function exists and returns Sequence<Feature>
2. featuresString() function exists and returns Sequence<Feature>
3. Both functions delegate to load().features and loadString().features
</verify>
  <done>GeoJSON object now provides convenience functions for direct feature access</done>
</task>

<task type="auto">
  <name>Add convenience function to GeoPackage object</name>
  <files>src/main/kotlin/geo/GeoPackage.kt</files>
  <action>
Add a convenience function to the GeoPackage object:
1. `fun features(path: String, maxFeatures: Int = Int.MAX_VALUE): Sequence<Feature>` - Loads file and returns features directly

Implementation pattern (delegation to existing API):
```kotlin
fun features(path: String, maxFeatures: Int = Int.MAX_VALUE): Sequence<Feature> = load(path, maxFeatures).features
```

Add KDoc documentation explaining that this is a convenience function for the common case of just getting features directly. Note that GeoPackage's spatial indexing is not available when using this function - use load() directly if you need queryByBounds().

Location: Add after the load() function, before determineCRS().
</action>
  <verify>
Read src/main/kotlin/geo/GeoPackage.kt and verify:
1. features() function exists and returns Sequence<Feature>
2. Function delegates to load(path, maxFeatures).features
3. KDoc clarifies that spatial indexing is not available with this convenience function
</verify>
  <done>GeoPackage object now provides convenience function for direct feature access</done>
</task>

<task type="auto">
  <name>Add tests for convenience functions</name>
  <files>
    src/test/kotlin/geo/GeoJSONTest.kt
    src/test/kotlin/geo/GeoPackageTest.kt
  </files>
  <action>
Add unit tests for the new convenience functions:

**GeoJSON tests:**
- Test that `GeoJSON.features(path)` returns the same features as `GeoJSON.load(path).features`
- Test that `GeoJSON.featuresString(content)` returns the same features as `GeoJSON.loadString(content).features`
- Verify that empty GeoJSON returns empty sequence
- Test with a sample GeoJSON file that has multiple features

**GeoPackage tests:**
- Test that `GeoPackage.features(path)` returns the same features as `GeoPackage.load(path).features`
- Verify maxFeatures parameter works correctly (limits feature count)
- Verify that features can be iterated normally

Use JUnit 4 assertions (org.junit.Test, org.junit.Assert.*).
Follow existing test patterns in both test files.
</action>
  <verify>
Run tests:
```bash
./gradlew test
```
Verify:
- All existing tests still pass
- New convenience function tests pass
- Test count increases appropriately
</verify>
  <done>Convenience functions have comprehensive test coverage</done>
</task>

</tasks>

<verification>
1. Verify UAT gap closes: The user expectation from test 1 ("GeoJSON.load() returns Sequence<Feature>") can now be met with `GeoJSON.features(path)` API
2. Run existing tests to ensure no regressions
3. Verify the convenience functions return identical results to the Source-based API
4. Confirm KDoc clarifies when to use convenience vs Source-based API
</verification>

<success_criteria>
1. GeoJSON.features() and GeoJSON.featuresString() functions exist and work correctly
2. GeoPackage.features() function exists and works correctly
3. Convenience functions return Sequence<Feature> directly (not Source objects)
4. Convenience functions delegate to existing load() methods
5. All tests pass (existing + new convenience function tests)
6. UAT test 1 now has a passing expectation when using the new convenience function
7. KDoc explains tradeoffs (convenience vs advanced features like spatial querying)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-layer/01-04-SUMMARY.md`
</output>