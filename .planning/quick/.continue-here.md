---
phase: quick-tasks
task: 002
total_tasks: 2
status: completed
last_updated: 2026-02-22T23:58:00Z
---

<current_state>
Post-v1.0.0 release quick task session. Main project is shipped, working on ad-hoc improvements.

Quick task 002 completed: Extract bbox from GeoJSON, add fitBounds()
</current_state>

<completed_work>

- Quick Task 001: Write v1.0.0 API docs - Done (previous session)
- Quick Task 002: Extract bbox from GeoJSON, add fitBounds - Done
  - Added `bbox` field parsing to `GeoJSONFeatureCollection`
  - Added `GeoJSONSource.boundingBox()` method (uses pre-computed bbox if available)
  - Added `ProjectionFactory.fitBounds()` with ProjectionType parameter
  - Fixed `ProjectionEquirectangularInternal` to respect center and scale
  - Fixed `fitBounds()` Mercator scale calculation (must use projected coords)
  - Simplified `anim_LineStringColor.kt` example (45 lines → 2 lines)
</completed_work>

<remaining_work>
None - quick task 002 complete. User may have additional quick tasks.
</remaining_work>

<decisions_made>

- `fitBounds()` takes a `projection: ProjectionType` parameter (default EQUIRECTANGULAR)
- Mercator scale must be calculated on projected coordinates, not raw degrees
- `GeoJSONSource.boundingBox()` returns pre-computed bbox if available, falls back to computing from features
- Padding default 0.8 (80%) for fitBounds
</decisions_made>

<blockers>
None
</blockers>

<context>
User noticed `fitBounds()` wasn't filling the frame properly. Root cause: `ProjectionEquirectangularInternal` was ignoring `center` and `scale` config values - always mapping full world to screen. Fixed both the projection and the Mercator scale calculation.

Commits:
- `910c34e` feat: add ProjectionType parameter to fitBounds()
- `2d06bb9` fix: equirectangular projection now respects center and scale
- `877f7ba` fix: fitBounds() now handles Mercator projection correctly
</context>

<next_action>
No pending work. Ready for new quick task via /gsd-quick or new feature requests.
</next_action>
