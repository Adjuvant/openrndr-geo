---
phase: 17-performance-fixes
plan: 03
type: execute
wave: 2
depends_on: [17-02]
files_modified: [src/main/kotlin/geo/render/DrawerGeoExtensions.kt, src/test/kotlin/geo/render/DrawerGeoExtensionsCacheTest.kt]
autonomous: true
requirements: [PERF-11]
gap_closure: true
must_haves:
  truths:
    - "Line geometries cached via ShapeContour use open contours (no closing segment)"
    - "Point geometries cached via ShapeContour use open contours"
  artifacts:
    - path: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      contains: "ShapeContour.fromPoints(projectedPoints, closed = false)"
    - path: "src/test/kotlin/geo/render/DrawerGeoExtensionsCacheTest.kt"
      contains: "assertFalse(contour.first() == contour.last())  # line not closed"
  key_links:
    - from: "Drawer.geo cache lambda"
      to: "ShapeContour.fromPoints"
      via: "closed = false flag for non-polygon geometries"
---

<objective>
Ensure that line and point geometries use open contours in the caching path, preventing unwanted closing segments for non-polygon shapes.

Purpose: Users must see accurate line and point renderings without implicit closure; this fixes the cache path to match standard drawing behavior.
Output: Updated `DrawerGeoExtensions.kt` and new unit tests in `DrawerGeoExtensionsCacheTest.kt` validating open contours for lines and points.
</objective>

<tasks>

<task type="auto">
  <name>task 1: update cache path to use open contours for lines and points</name>
  <files>src/main/kotlin/geo/render/DrawerGeoExtensions.kt</files>
  <action>
    In the `Drawer.geo` caching block (inside `drawerGeoCache.get` lambda), change:
    ```kotlin
    contours.add(ShapeContour.fromPoints(projectedPoints, closed = true))
    ```
    to:
    ```kotlin
    contours.add(ShapeContour.fromPoints(projectedPoints, closed = false))
    ```
    so that lines and points are not rendered as closed loops.
  </action>
  <verify>
    <automated>grep -R "ShapeContour.fromPoints.*closed = false" src/main/kotlin/geo/render/DrawerGeoExtensions.kt</automated>
  </verify>
  <done>Cache contour creation uses `closed = false` for non-polygon geometries</done>
</task>

<task type="auto">
  <name>task 2: add tests for open contours on lines and points</name>
  <files>src/test/kotlin/geo/render/DrawerGeoExtensionsCacheTest.kt</files>
  <action>
    Create a unit test file with two tests:
    1. Line contour test:
       - Input: a geometry with two distinct points (e.g., [(0.0,0.0), (1.0,1.0)])
       - Invoke the cache lambda to obtain `List<ShapeContour>`; get the first contour's points
       - Assert that `first() != last()` to confirm the contour is open
    2. Point contour test:
       - Input: a single-point geometry [(2.0,3.0)]
       - Invoke the cache lambda; get the first contour's points
       - Assert that the list size is 1
    Use an identity projection that returns input coordinates.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.render.DrawerGeoExtensionsCacheTest"</automated>
  </verify>
  <done>Unit tests pass, confirming open contours for line and point geometries</done>
</task>

</tasks>

<verification>
Run `./gradlew test` to ensure all tests, including the new cache tests, pass.  
Render a line and a point in an example application to manually verify no closing segment appears.
</verification>

<success_criteria>
- `DrawerGeoExtensions.kt` uses `closed = false` in the cache path
- New tests in `DrawerGeoExtensionsCacheTest.kt` exist and pass
- Visual rendering of line and point geometries shows no closed shapes
</success_criteria>

<output>
After completion, create `.planning/phases/17-performance-fixes/17-03-SUMMARY.md`
</output>