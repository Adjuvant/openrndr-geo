---
phase: 17-performance-fixes
plan: 05
type: execute
wave: 4
depends_on: ["17-04"]
files_modified: [
  "src/main/kotlin/geo/render/DrawerGeoExtensions.kt",
  "src/main/kotlin/geo/render/GeoRenderConfig.kt",
  "src/test/kotlin/geo/render/DrawerGeoExtensionsTest.kt",
  "src/test/kotlin/geo/render/OptimizedStyleResolutionTest.kt",
  "src/test/kotlin/geo/internal/cache/ViewportCacheIntegrationTest.kt"
]
autonomous: true
gap_closure: true
requirements: [PERF-14, PERF-15, PERF-16]

must_haves:
  truths:
    - "toScreenCoordinates extension for OptimizedFeature has real unit tests."
    - "ViewportCache integration tests verify shared cache behavior."
    - "styleByFeature works for OptimizedFeature."
  artifacts:
    - path: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      provides: "resolveOptimizedStyle with styleByFeature for OptimizedFeature"
    - path: "src/test/kotlin/geo/render/DrawerGeoExtensionsTest.kt"
      provides: "Unit tests for toScreenCoordinates on OptimizedFeature"
    - path: "src/test/kotlin/geo/render/OptimizedStyleResolutionTest.kt"
      provides: "Unit tests for styleByFeature with OptimizedFeature"
    - path: "src/test/kotlin/geo/internal/cache/ViewportCacheIntegrationTest.kt"
      provides: "Integration tests for cache eviction and size limit"
  key_links:
    - from: "src/main/kotlin/geo/internal/cache/ViewportCache.kt"
      to: "src/test/kotlin/geo/internal/cache/ViewportCacheIntegrationTest.kt"
      via: "integration test invoking cache across viewports"
      pattern: "ViewportCacheIntegrationTest"
    - from: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      to: "src/test/kotlin/geo/render/DrawerGeoExtensionsTest.kt"
      via: "toScreenCoordinates extension invocation"
      pattern: "toScreenCoordinates"
    - from: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      to: "src/test/kotlin/geo/render/OptimizedStyleResolutionTest.kt"
      via: "styleByFeature for OptimizedFeature invocation"
      pattern: "styleByFeature"

---

<objective>
Add missing tests and implement styleByFeature for OptimizedFeature.

Purpose: Complete test coverage for Phase 17 and enable styleByFeature for optimized rendering path.
Output: All new tests pass and styleByFeature works for OptimizedFeature.
</objective>

<tasks>

<task type="auto">
  <name>Implement real tests for toScreenCoordinates</name>
  <files>src/test/kotlin/geo/render/DrawerGeoExtensionsTest.kt</files>
  <action>
    Replace placeholder tests with real unit tests for toScreenCoordinates extension on OptimizedFeature. Test with points and polygons with holes. Make extension internal if needed for testing or test through public API.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.render.DrawerGeoExtensionsTest"</automated>
  </verify>
  <done>DrawerGeoExtensionsTest contains real tests that pass</done>
</task>

<task type="auto">
  <name>Add ViewportCache integration tests</name>
  <files>src/test/kotlin/geo/internal/cache/ViewportCacheIntegrationTest.kt</files>
  <action>
    Create comprehensive integration tests for ViewportCache: verify shared cache instance usage across rendering paths, test eviction on viewport changes, test size limit enforcement (500 entries), test cache hit/miss behavior.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.internal.cache.ViewportCacheIntegrationTest"</automated>
  </verify>
  <done>All integration tests pass</done>
</task>

<task type="auto">
  <name>Implement styleByFeature for OptimizedFeature</name>
  <files>src/main/kotlin/geo/render/DrawerGeoExtensions.kt, src/main/kotlin/geo/render/GeoRenderConfig.kt, src/test/kotlin/geo/render/OptimizedStyleResolutionTest.kt</files>
  <action>
    Implement styleByFeature support for OptimizedFeature. Either create adapter from OptimizedFeature to Feature interface, or add separate styleByOptimizedFeature callback. Remove TODO comment. Add unit tests verifying style resolution works for optimized features.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.render.OptimizedStyleResolutionTest"</automated>
  </verify>
  <done>styleByFeature works for OptimizedFeature with passing tests</done>
</task>

</tasks>

<verification>
```
./gradlew test
```
</verification>

<success_criteria>
- `./gradlew test` completes with zero failures.
- All new tests pass (DrawerGeoExtensionsTest, ViewportCacheIntegrationTest, OptimizedStyleResolutionTest).
- styleByFeature callback is invoked for OptimizedFeature.
</success_criteria>

<output>
After completion, create `.planning/phases/17-performance-fixes/17-05-SUMMARY.md`
</output>
