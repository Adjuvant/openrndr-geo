---
phase: 17-performance-fixes
plan: 04
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03"]
files_modified: [
  "src/main/kotlin/geo/internal/cache/ViewportCache.kt",
  "src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt",
  "uat/Uat_ShapeCacheVerification.kt",
  "src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt",
  "src/test/kotlin/geo/render/geometry/OptimizedGeometryNormalizerTest.kt"
]
autonomous: true
gap_closure: true
requirements: [PERF-11, PERF-12, PERF-13]

must_haves:
  truths:
    - "All unit tests pass including ViewportCache and geometry tests."
    - "UAT ShapeCacheVerification shows correct elevation-based coloring."
  artifacts:
    - path: "src/main/kotlin/geo/internal/cache/ViewportCache.kt"
      provides: "Cache invalidation logic based on viewport dirty state"
    - path: "src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt"
      provides: "splitAtAntimeridian function that fragments rings correctly"
    - path: "uat/Uat_ShapeCacheVerification.kt"
      provides: "Visual test with fixed coloring and layout"
    - path: "src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt"
      provides: "Unit test for dirty-flag cache invalidation"
    - path: "src/test/kotlin/geo/render/geometry/OptimizedGeometryNormalizerTest.kt"
      provides: "Unit tests for splitAtAntimeridian behavior"
  key_links:
    - from: "src/main/kotlin/geo/internal/cache/ViewportCache.kt"
      to: "src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt"
      via: "dirty flag check and equals comparison"
      pattern: "dirty"
    - from: "src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt"
      to: "src/test/kotlin/geo/render/geometry/OptimizedGeometryNormalizerTest.kt"
      via: "splitAtAntimeridian invocation"
      pattern: "splitAtAntimeridian"

---

<objective>
Fix test failures and UAT coloring issue from Phase 17 UAT.

Purpose: Resolve the blocking test failures and fix elevation coloring in UAT.
Output: All tests pass and UAT shows correct visual output.
</objective>

<tasks>

<task type="auto">
  <name>Fix ViewportCache dirty flag invalidation</name>
  <files>src/main/kotlin/geo/internal/cache/ViewportCache.kt, src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt</files>
  <action>
    Fix ViewportCache logic to properly invalidate cache entries when viewport dirty flag changes. Update viewport state equality comparison to consider dirty flag transitions. Ensure cache clears when viewport changes state.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.internal.cache.ViewportCacheTest.testDirtyFlagInvalidatesCacheEntry"</automated>
  </verify>
  <done>testDirtyFlagInvalidatesCacheEntry passes</done>
</task>

<task type="auto">
  <name>Fix splitAtAntimeridian algorithm</name>
  <files>src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt, src/test/kotlin/geo/render/geometry/OptimizedGeometryNormalizerTest.kt</files>
  <action>
    Correct splitAtAntimeridian to properly detect longitude crossings, insert interpolated antimeridian points, and produce correct number of fragments. Single crossing should produce 2 fragments, not 3.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.render.geometry.OptimizedGeometryNormalizerTest.splitAtAntimeridian*"</automated>
  </verify>
  <done>splitAtAntimeridian produces expected fragment counts</done>
</task>

<task type="auto">
  <name>Fix UAT ShapeCacheVerification</name>
  <files>uat/Uat_ShapeCacheVerification.kt</files>
  <action>
    Fix two issues in UAT:
    1. Fix elevation-based coloring - check property name access (property_value), ensure styleByFeature is correctly configured
    2. Redesign layout from top-bottom to side-by-side with independent projections for each half
  </action>
  <verify>
    <manual>Run ./gradlew run -Popenrndr.application=uat.ShapeCacheVerificationJvm and visually confirm: (1) color gradient from blue to red based on elevation, (2) side-by-side layout with standard on left, optimized on right</manual>
  </verify>
  <done>UAT shows elevation colors and side-by-side layout correctly</done>
</task>

</tasks>

<verification>
```
./gradlew test && ./gradlew run -Popenrndr.application=uat.ShapeCacheVerificationJvm
```
</verification>

<success_criteria>
- `./gradlew test` completes with zero failures.
- UAT ShapeCacheVerificationJvm shows correct elevation gradient and side-by-side layout.
</success_criteria>

<output>
After completion, create `.planning/phases/17-performance-fixes/17-04-SUMMARY.md`
</output>
