---
phase: 17-performance-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/internal/cache/ViewportCache.kt
  - src/main/kotlin/geo/render/DrawerGeoExtensions.kt
  - src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt
  - src/test/kotlin/geo/render/OptimizedStyleResolutionTest.kt
autonomous: true
requirements:
  - PERF-11

must_haves:
  truths:
    - "ViewportCache composites viewport state internally with feature reference as key"
    - "Single shared cache instance (ViewportCache<Any, List<Shape>>) serves both standard and optimized paths"
    - "styleByFeature is invoked in resolveOptimizedStyle before styleByType check"
    - "MAX_CACHE_ENTRIES default is 500 and can be configured via constructor parameter"
  artifacts:
    - path: "src/main/kotlin/geo/internal/cache/ViewportCache.kt"
      provides: "Generic ViewportCache<K,V> storing List<Shape>"
      contains: "class ViewportCache<K, V>"
    - path: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      provides: "Drawer.geo() caching integration and styleByFeature support"
      contains: "resolveOptimizedStyle()"
    - path: "src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt"
      provides: "Test scaffold for cache get/put behavior and styleByFeature"
      min_lines: 30
    - path: "src/test/kotlin/geo/render/OptimizedStyleResolutionTest.kt"
      provides: "Test that resolveOptimizedStyle invokes styleByFeature"
      min_lines: 15
  key_links:
    - from: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      to: "src/main/kotlin/geo/internal/cache/ViewportCache.kt"
      via: "cache.get calls in Drawer.geo() render loop"
    - from: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      to: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      via: "styleByFeature?.invoke(optFeature) in resolveOptimizedStyle()"
      pattern: "styleByFeature"
---

<objective>
Extend and unify shape-level caching for both standard and optimized render paths, and add styleByFeature support for optimized geometries.

Purpose: Eliminates per-frame projection/construction overhead by caching Shape lists; ensures consistent styling across both paths.
Output: Generic Shape cache implementation, Drawer.geo() integration, and cache behavior tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-performance-fixes/17-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: scaffold ViewportCache tests</name>
  <files>src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt</files>
  <behavior>
    - Given an empty cache, get(key) returns null
    - After put(key, value), get(key) returns the same value List<Shape>
    - After cache miss and put, subsequent get(key) returns cached list without reconstruction
  </behavior>
  <action>Create a JUnit5 test file ViewportCacheTest.kt with basic get/put tests for a dummy key and List<Shape> placeholder. Use mock Shape objects or ShapeContour stubs. Tests should compile even if ViewportCache isn't fully implemented yet.</action>
  <verify>
    <automated>./gradlew compileTestKotlin --tests "geo.internal.cache.ViewportCacheTest"</automated>
  </verify>
  <done>Test file exists with three @Test methods named testGetEmpty, testPutAndGet, testCacheHit and compiles successfully</done>
</task>

<task type="auto">
  <name>task 2: genericize ViewportCache for Shape lists with internal viewport tracking</name>
  <files>src/main/kotlin/geo/internal/cache/ViewportCache.kt</files>
  <action>
    - Change class signature to `class ViewportCache<K, V>(private val maxEntries: Int = DEFAULT_MAX_ENTRIES)` where K is the feature reference (Geometry or OptimizedFeature)
    - Internally composite the key: store `Map<CompositeKey, V>` where CompositeKey = Pair<ViewportState, K>
    - Maintain internal `currentViewportState` field; on viewport change, clear entire cache and update current state
    - Expose API: `get(feature: K, viewportState: ViewportState, factory: () -> V): V`
    - Rename DEFAULT_MAX_ENTRIES to 500; expose as configurable constructor parameter
    - Adapt eviction logic unchanged (clear-on-change, size limit enforcement)
    - Ensure V will be List<Shape> for forward compatibility
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.internal.cache.ViewportCacheTest"</automated>
  </verify>
  <done>ViewportCache compiles with generic signature, DEFAULT_MAX_ENTRIES is 500, viewport state cleared on change, tests testGetEmpty and testPutAndGet pass</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: add styleByFeature support to resolveOptimizedStyle</name>
  <files>
    src/main/kotlin/geo/render/DrawerGeoExtensions.kt,
    src/test/kotlin/geo/render/OptimizedStyleResolutionTest.kt
  </files>
  <behavior>
    - When config.styleByFeature is set, resolveOptimizedStyle should invoke it and return the result
    - styleByFeature check should happen BEFORE styleByType check (same order as standard path)
    - If styleByFeature returns null, fall through to existing resolution chain
  </behavior>
  <action>
    - In resolveOptimizedStyle() (DrawerGeoExtensions.kt), add as first check:
      `config.styleByFeature?.invoke(optFeature)?.let { return it }`
    - Create OptimizedStyleResolutionTest.kt with test:
      - Given a config with styleByFeature set, when resolveOptimizedStyle is called, it returns the feature-specific style
      - Given a config with styleByFeature returning null, it falls through to styleByType
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.render.OptimizedStyleResolutionTest"</automated>
  </verify>
  <done>resolveOptimizedStyle invokes styleByFeature when provided; test passes verifying the fix</done>
</task>

<task type="auto">
  <name>task 4: integrate shape caching into Drawer.geo()</name>
  <files>src/main/kotlin/geo/render/DrawerGeoExtensions.kt</files>
  <action>
    - Cache location: Use a private top-level/internal val drawerGeoCache = ViewportCache<Any, List<Shape>>() — single cache instance for both paths using Any as common key type
    - In Drawer.geo() extension:
      - Create ViewportState from projection
      - For standard path: check cache with feature.geometry as key; on miss project + build Shape list; cache and render
      - For optimized path: check cache with optFeature as key; on miss project + build Shape list; cache and render
      - On cache hit for either path: retrieve List<Shape> and render directly via renderToDrawer
    - Shape construction: Use ShapeContour.fromPoints() with appropriate winding (.clockwise/.counterClockwise), wrap in Shape, return List<Shape>
    - Preserve clear-on-change: ViewportCache internally handles viewport state changes
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.internal.cache.ViewportCacheTest" && ./gradlew run -Popenrndr.application=examples.render.Example</automated>
  </verify>
  <done>Both paths use single shared cache; cache hits return pre-built Shapes; example runs successfully</done>
</task>

</tasks>

<verification>
Automated (agent runs these):
1. ./gradlew test — all pass:
   - ViewportCacheTest: get/put, viewport-change clearing, size limits
   - OptimizedStyleResolutionTest: styleByFeature invoked on optimized path
2. ./gradlew regressionTest — existing examples unbroken

UAT (agent creates, user runs manually):
3. Create uat/Uat_ShapeCacheVerification.kt:
    - Renders a GeoJSON dataset using Drawer.geo() with styleByFeature
      - File to use (large, has properties for height): "data/geo/catchment-topo.geojson"
    - Uses both standard and optimized source paths
    - User visually confirms: geometry renders correctly,
      property-based styling applied (e.g. color varies by feature property)
      - Property name for file provided: property_value
    - No cache stats, no assertions — just a visual sanity check
</verification>

<success_criteria>
- `./gradlew test` passes all tests (ViewportCacheTest, OptimizedStyleResolutionTest)
- `resolveOptimizedStyle` invokes `styleByFeature` when provided (verified by test)
- Both standard and optimized paths use shared Shape cache
- Cache clears correctly on viewport state change
- Example renders without errors using cached shapes
</success_criteria>

<output>
After completion, create `.planning/phases/17-performance-fixes/17-01-SUMMARY.md`
</output>
