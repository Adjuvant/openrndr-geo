---
phase: 17-performance-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/internal/cache/ViewportCache.kt
  - src/main/kotlin/geo/internal/DrawerGeoExtensions.kt
  - src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt
autonomous: true
requirements:
  - PERF-11

must_haves:
  truths:
    - "Optimized geometries miss cache, trigger shape construction, then hit cache on repeat"
    - "Both standard and optimized render paths use Shape-level caching via generic ViewportCache<K,V>"
    - "styleByFeature is respected in optimized rendering path"
    - "MAX_CACHE_ENTRIES default is 500 and can be configured"
  artifacts:
    - path: "src/main/kotlin/geo/internal/cache/ViewportCache.kt"
      provides: "Generic ViewportCache<K,V> storing List<Shape>"
      contains: "class ViewportCache<K, V>"
    - path: "src/main/kotlin/geo/internal/DrawerGeoExtensions.kt"
      provides: "Drawer.geo() caching integration and styleByFeature support"
      contains: "resolveOptimizedStyle()"
    - path: "src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt"
      provides: "Test scaffold for cache get/put behavior"
      min_lines: 20
  key_links:
    - from: "src/main/kotlin/geo/internal/DrawerGeoExtensions.kt"
      to: "src/main/kotlin/geo/internal/cache/ViewportCache.kt"
      via: "cache.get/put calls in Drawer.geo()"
    - from: "src/main/kotlin/geo/internal/DrawerGeoExtensions.kt"
      to: "styleByFeature invocation in resolveOptimizedStyle()"
      pattern: "styleByFeature"
---

<objective>
Extend and unify shape-level caching for both standard and optimized render paths, and add styleByFeature support for optimized geometries.

Purpose: Eliminates per-frame projection/construction overhead by caching Shape lists; ensures consistent styling across both paths.
Output: Generic Shape cache implementation, Drawer.geo() integration, and cache behavior tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-performance-fixes/17-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: scaffold ViewportCache tests</name>
  <files>src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt</files>
  <behavior>
    - Given an empty cache, get(key) returns null
    - After put(key, value), get(key) returns the same value List<Shape>
    - After cache miss and put, subsequent get(key) returns cached list without reconstruction
  </behavior>
  <action>Create a JUnit5 test file ViewportCacheTest.kt with basic get/put tests for a dummy key and List<Shape> placeholder. Import ShapeContour as stub or mock shapes.</action>
  <verify>
    <automated>MISSING — Wave 0 must create src/test/kotlin/geo/internal/cache/ViewportCacheTest.kt first</automated>
  </verify>
  <done>Test file exists with three @Test methods named testGetEmpty, testPutAndGet, testCacheHit</done>
</task>

<task type="auto">
  <name>task 2: genericize ViewportCache for Shape lists</name>
  <files>src/main/kotlin/geo/internal/cache/ViewportCache.kt</files>
  <action>
    - Change class signature to `class ViewportCache<K, V>(private val maxEntries: Int = DEFAULT_MAX_ENTRIES)`
    - Replace internal map to Map<CacheKey, V> and remove geometry-specific code
    - Rename DEFAULT_MAX_ENTRIES to 500; expose as configurable constructor parameter
    - Adapt eviction logic unchanged (clear-on-change)
    - Ensure V will be List<Shape> for forward compatibility
  </action>
  <verify>
    <automated>gradle test --tests "geo.internal.cache.ViewportCacheTest"</automated>
  </verify>
  <done>ViewportCache compiles, DEFAULT_MAX_ENTRIES is 500, tests testGetEmpty and testPutAndGet pass</done>
</task>

<task type="auto">
  <name>task 3: integrate shape caching and styleByFeature in Drawer.geo()</name>
  <files>src/main/kotlin/geo/internal/DrawerGeoExtensions.kt</files>
  <action>
    - In Drawer.geo() extension, import and instantiate `ViewportCache<Geometry, List<Shape>>` for standard path and `ViewportCache<OptimizedFeature, List<Shape>>` for optimized path
    - On cache miss: project feature -> build List<Shape> via ShapeContour.fromPoints().wrap() ; cache.put(key, shapes)
    - On cache hit: retrieve shapes and render each via renderToDrawer
    - In optimized branch, before rendering shapes, call `config.styleByFeature?.invoke(feature)` and apply returned style
    - Preserve existing clear-on-change behavior
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.internal.cache.ViewportCacheTest" && sampleApp --benchmark-cache-hit</automated>
  </verify>
  <done>Optimized path uses cache; styleByFeature invoked on each feature; benchmark shows cache hit path faster than uncached run</done>
</task>

</tasks>

<verification>
Run full test suite and sample performance benchmark to confirm caching behavior.
</verification>

<success_criteria>
- `gradle test` passes all cache tests
- Benchmark report shows >=10x speedup on cache-hit scenario for optimized rendering
</success_criteria>

<output>
After completion, create `.planning/phases/17-performance-fixes/17-01-SUMMARY.md`
</output>
