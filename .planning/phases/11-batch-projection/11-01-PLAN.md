---
phase: 11-batch-projection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/main/kotlin/geo/internal/batch/CoordinateBatch.kt",
  "src/main/kotlin/geo/internal/batch/BatchProjectionUtils.kt",
  "src/main/kotlin/geo/internal/geometry/OptimizedGeometries.kt",
  "src/main/kotlin/geo/Geometry.kt",
  "src/main/kotlin/geo/ProjectionExtensions.kt"
]
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "Coordinate arrays are stored as DoubleArray pairs (x[], y[]) for contiguous memory"
    - "Batch transformation uses indexed loops instead of per-point map{}"
    - "All geometry types (Point, LineString, Polygon, Multi*) have optimized variants"
    - "Optimized geometries are convertible to Array<Vector2> for OPENRNDR compatibility"
    - "Internal projection uses batch operations throughout"
  artifacts:
    - path: "src/main/kotlin/geo/internal/batch/CoordinateBatch.kt"
      provides: "DoubleArray-based coordinate storage with Vector2 conversion"
      exports: ["CoordinateBatch", "toVector2Array"]
    - path: "src/main/kotlin/geo/internal/batch/BatchProjectionUtils.kt"
      provides: "Batch transformation functions with inline loops"
      exports: ["batchTransform", "batchProject"]
    - path: "src/main/kotlin/geo/internal/geometry/OptimizedGeometries.kt"
      provides: "Optimized geometry subclasses using coordinate arrays"
      exports: ["OptimizedPoint", "OptimizedLineString", "OptimizedPolygon", "OptimizedMultiPoint", "OptimizedMultiLineString", "OptimizedMultiPolygon"]
    - path: "src/main/kotlin/geo/Geometry.kt"
      provides: "Geometry factory methods for optimized variants"
      exports: ["Geometry.toOptimized()"]
  key_links:
    - from: "CoordinateBatch.toVector2Array()"
      to: "OPENRNDR Drawer"
      via: "Array<Vector2> return type"
      pattern: "Array\\(size\\)\\s*\\{\\s*i\\s*->\\s*Vector2\\(x\\[i\\], y\\[i\\]\\)"
---

<objective>
Implement core batch projection infrastructure: DoubleArray-based coordinate storage, batch transformation utilities, and optimized geometry subclasses that use arrays internally while maintaining API compatibility.

Purpose: Eliminate allocation churn from `.map { projection.project(it) }` by using contiguous memory and indexed loops.
Output: Internal batch utilities and optimized geometry classes ready for integration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-batch-projection/11-CONTEXT.md
@.planning/phases/11-batch-projection/11-RESEARCH.md

@src/main/kotlin/geo/Geometry.kt
@src/main/kotlin/geo/ProjectionExtensions.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create batch coordinate storage and transformation utilities</name>
  <files>
    src/main/kotlin/geo/internal/batch/CoordinateBatch.kt
    src/main/kotlin/geo/internal/batch/BatchProjectionUtils.kt
  </files>
  <action>
Create internal batch utilities package:

1. **CoordinateBatch.kt** - Storage class:
   - `internal class CoordinateBatch(val x: DoubleArray, val y: DoubleArray)`
   - `val size: Int get() = x.size`
   - `fun toVector2Array(): Array<Vector2>` - Convert to OPENRNDR-compatible format
   - `fun toVector2List(): List<Vector2>` - For APIs requiring List
   - Factory method `fromPoints(points: List<Vector2>): CoordinateBatch`
   - Factory method `fromPoint(point: Vector2): CoordinateBatch` (size 1)

2. **BatchProjectionUtils.kt** - Transformation functions:
   - `internal inline fun batchTransform(x: DoubleArray, y: DoubleArray, outX: DoubleArray, outY: DoubleArray, transform: (Double, Double) -> Pair<Double, Double>)` - Generic batch transformer using inline to eliminate lambda overhead
   - `internal fun batchProject(x: DoubleArray, y: DoubleArray, projection: GeoProjection, output: Array<Vector2>): Array<Vector2>` - Project to screen coordinates, filling pre-allocated output array
   - `internal fun batchProjectToArrays(x: DoubleArray, y: DoubleArray, projection: GeoProjection, outX: DoubleArray, outY: DoubleArray)` - Project to output arrays (for intermediate transforms)
   
Key implementation details:
- Use `require()` to validate array sizes match
- Use indexed `for (i in x.indices)` loops (not for-each)
- Mark functions as `internal` (not public)
- Create `src/main/kotlin/geo/internal/` package structure

Per RESEARCH.md Pattern 1 & 2: DoubleArray pairs for contiguous memory, inline functions for zero lambda overhead.
  </action>
  <verify>
    <automated>./gradlew compileKotlin --quiet && echo "Batch utilities compile successfully"</automated>
    <manual>Verify files exist: src/main/kotlin/geo/internal/batch/CoordinateBatch.kt and BatchProjectionUtils.kt</manual>
  </verify>
  <done>
    - CoordinateBatch class exists with toVector2Array() method
    - BatchProjectionUtils has batchTransform() and batchProject() functions
    - All functions use indexed loops, not map/forEach
    - Code compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create optimized geometry subclasses</name>
  <files>
    src/main/kotlin/geo/internal/geometry/OptimizedGeometries.kt
    src/main/kotlin/geo/Geometry.kt
  </files>
  <action>
Create optimized geometry implementations that store coordinates as DoubleArray internally:

1. **OptimizedGeometries.kt** - New file with internal classes:
   - `internal class OptimizedPoint(val coord: CoordinateBatch) : Geometry` 
   - `internal class OptimizedLineString(val coords: CoordinateBatch) : Geometry`
   - `internal class OptimizedPolygon(val rings: List<CoordinateBatch>) : Geometry` (outer ring + holes)
   - `internal class OptimizedMultiPoint(val coords: CoordinateBatch) : Geometry`
   - `internal class OptimizedMultiLineString(val lines: List<CoordinateBatch>) : Geometry`
   - `internal class OptimizedMultiPolygon(val polygons: List<List<CoordinateBatch>>) : Geometry`

Each optimized class must:
- Extend the sealed Geometry class appropriately
- Store coordinates in CoordinateBatch (DoubleArray pairs)
- Provide `fun toScreenCoordinates(projection: GeoProjection): Array<Vector2>` using batch projection
- Provide `fun toScreenCoordinatesList(projection: GeoProjection): List<Vector2>` for List-based APIs
- Override `val coordinateCount: Int` to report total coordinates
- Maintain immutability (no public setters)

2. **Geometry.kt** - Add factory/conversion:
   - Add `fun Geometry.toOptimized(): Geometry` extension that converts standard geometry to optimized variant
   - For Point: create single-element CoordinateBatch
   - For LineString: batch all points
   - For Polygon: batch each ring separately
   - For Multi*: recursively optimize components

Implementation approach:
- Use `when (this)` in toOptimized() to handle each geometry type
- For Polygon, preserve ring structure (outer first, then holes)
- For MultiPolygon, preserve polygon->rings nesting

Per RESEARCH.md Pattern 3: Geometry-type-agnostic approach using CoordinateBatch for all types.
  </action>
  <verify>
    <automated>./gradlew compileKotlin --quiet && echo "Optimized geometries compile successfully"</automated>
    <manual>Verify OptimizedGeometries.kt exists and Geometry.kt has toOptimized() extension</manual>
  </verify>
  <done>
    - All 6 optimized geometry classes exist and compile
    - Each has toScreenCoordinates() using batch projection
    - Geometry.toOptimized() extension handles all geometry types
    - Optimized classes properly extend Geometry sealed class
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ProjectionExtensions to use batch internally</name>
  <files>
    src/main/kotlin/geo/ProjectionExtensions.kt
  </files>
  <action>
Migrate ProjectionExtensions.kt to use batch projection internally while maintaining existing API signatures:

1. **Analyze current patterns** (from RESEARCH.md):
   - Look for `.map { projection.project(it) }` patterns
   - These create new List<Vector2> on every call

2. **Update existing extension functions** to use batch internally:
   - Keep existing signatures: `fun LineString.projectToScreen(projection): List<Vector2>`
   - Change internal implementation to:
     a) Convert points to CoordinateBatch (or use cached batch)
     b) Call batchProject to fill pre-allocated Array<Vector2>
     c) Convert Array to List<Vector2> for return
   
3. **Add optimized variants** for internal use:
   - `internal fun Geometry.projectToScreenBatch(projection): Array<Vector2>` - Returns array for zero-copy pipeline
   - `internal fun CoordinateBatch.project(projection, output: Array<Vector2>)` - Direct batch projection

4. **Ensure backward compatibility**:
   - All public signatures remain unchanged
   - Default behavior unchanged (optimization opt-in at load time, not projection time)
   - Internal migration only—no dual public APIs

Key changes:
- Replace `points.map { projection.project(it) }` with:
  ```kotlin
  val batch = CoordinateBatch.fromPoints(points)
  val output = Array(points.size) { Vector2.ZERO }
  batchProject(batch.x, batch.y, projection, output)
  return output.toList()
  ```

Per CONTEXT.md: Integration happens at load time, but projection extensions should be batch-ready internally.
  </action>
  <verify>
    <automated>./gradlew compileKotlin --quiet && echo "ProjectionExtensions updated successfully"</automated>
    <manual>Verify existing public signatures unchanged; internal implementation uses batch utilities</manual>
  </verify>
  <done>
    - All existing public API signatures preserved
    - Internal implementation uses batch projection
    - No .map { projection.project(it) } patterns remain in hot paths
    - Extensions compile and pass existing tests
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. **Compile check**: `./gradlew compileKotlin` passes
2. **Test check**: Existing tests still pass (backward compatibility)
3. **Structure check**: Internal packages created under `geo/internal/`
4. **Pattern check**: No per-point projection in batch utilities

**Observable behaviors:**
- DoubleArray-based storage exists and compiles
- Batch transformation functions use indexed loops
- All geometry types have optimized variants
- ProjectionExtensions use batch internally
- Existing public APIs unchanged
</verification>

<success_criteria>
Phase 11 Plan 01 complete when:
- [ ] Batch utilities package exists with CoordinateBatch and transformation functions
- [ ] All 6 geometry types have optimized subclasses
- [ ] Geometry.toOptimized() extension converts standard to optimized
- [ ] ProjectionExtensions use batch projection internally
- [ ] All code compiles and existing tests pass
- [ ] No public API signatures changed
</success_criteria>

<output>
After completion, create `.planning/phases/11-batch-projection/11-01-SUMMARY.md`
</output>
