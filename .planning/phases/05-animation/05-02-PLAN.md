---
phase: 05-animation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/animation/Tweening.kt
  - src/main/kotlin/geo/animation/interpolators/LinearInterpolator.kt
  - src/main/kotlin/geo/animation/interpolators/HaversineInterpolator.kt
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can tween properties (position, color, size) with ::property.animate()"
    - "User can use linear interpolation for short distances and great-circle for long paths"
    - "User can apply per-property easing with different curves"
  artifacts:
    - path: "src/main/kotlin/geo/animation/Tweening.kt"
      provides: "Property tweening primitives"
      exports: ["animate()", "interpolatePosition", "interpolateColor", "interpolateSize"]
    - path: "src/main/kotlin/geo/animation/interpolators/LinearInterpolator.kt"
      provides: "Linear interpolation for screen coordinates"
      contains: "linearInterpolate"
    - path: "src/main/kotlin/geo/animation/interpolators/HaversineInterpolator.kt"
      provides: "Great-circle interpolation for geo paths"
      contains: "haversine", "interpolatePosition"
  key_links:
    - from: "Tweening.kt"
      to: "GeoAnimator"
      via: "animation registration"
      pattern: "animator\\.animations\\.add"
    - from: "Tweening.kt"
      to: "property references"
      via: "::PropertyName.animate() syntax"
      pattern: "::.*\\.animate"
    - from: "HaversineInterpolator.kt"
      to: "EARTH_RADIUS"
      via: "spherical interpolation"
      pattern: "EARTH_RADIUS|6371000"
    - from: "LinearInterpolator.kt"
      to: "Vector2"
      via: "screen coordinate interpolation"
      pattern: "Vector2"
---

<objective>
Create property tweening system with interpolators for position, color, and size animations.

Purpose: Enable users to animate geometry properties over time using Kotlin property references, supporting both linear interpolation (performance) and great-circle interpolation (accuracy) with per-property easing.

Output: Tweening primitives, interpolator implementations (linear + Haversine), and property reference extension functions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-animation/05-CONTEXT.md
@.planning/phases/05-animation/05-RESEARCH.md
@.planning/ROADMAP.md
@src/main/kotlin/geo/animation/GeoAnimator.kt
@src/main/kotlin/geo/render/Style.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create interpolator implementations (linear and Haversine)</name>
  <files>
    src/main/kotlin/geo/animation/interpolators/LinearInterpolator.kt
    src/main/kotlin/geo/animation/interpolators/HaversineInterpolator.kt
  </files>
  <action>
Create package `geo.animation.interpolators` and implement two interpolators:

1. LinearInterpolator.kt:
   - Function signature: `fun linearInterpolate(from: Vector2, to: Vector2, t: Double): Vector2`
   - Implementation: Linear interpolation: `from + (to - from) * t`
   - Use case: Projected screen coordinates (fast, default for performance)
   - Package: `geo.animation.interpolators`
   - Import: `org.openrndr.math.Vector2`

2. HaversineInterpolator.kt:
   - Function signature: `fun haversineInterpolate(from: Position, to: Position, t: Double): Position`
   - Implementation: Haversine formula (great-circle interpolation) - approximately 8 lines:
     - Convert lat/lng to radians
     - Calculate angular distance using Haversine formula
     - Interpolate using spherical linear interpolation (SLERP-like)
     - Convert back to degrees
   - Use case: Geographic coordinates (accurate for long-distance paths)
   - Package: `geo.animation.interpolators`
   - Import: `org.openrndr.math.Vector2` for return type or use custom Position type from Geometry.kt

Note: If Position type doesn't exist, use Vector2 or create alias. Check Geometry.kt for Position type.

Do NOT:
- Implement full great-circle library (inline ~5-8 lines is sufficient)
- Use complex geodesy libraries (overkill for creative coding)
- Store geometry types in interpolators (pure functions only)
  </action>
  <verify>
Both files compile successfully with gradle build.
LinearInterpolator returns Vector2 with correct linear interpolation.
HaversineInterpolator implements valid Haversine formula for great-circle paths.
  </verify>
  <done>
LinearInterpolator provides linearInterpolate(Vector2, Vector2, Double) -> Vector2
HaversineInterpolator provides haversineInterpolate(from, to, t) for great-circle paths
Both are pure functions (no state, no geometry storage)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Tweening.kt with property tweening primitives</name>
  <files>src/main/kotlin/geo/animation/Tweening.kt</files>
  <action>
Create Tweening.kt in geo.animation package with property tweening API:

1. Create extension function `fun MutableReference<T>.animate(to: T, duration: Long, easing: Easing = Easing.CubicInOut)`:
   - Use Kotlin property reference syntax: `::x.animate(targetX, duration, easing)`
   - Duration in milliseconds (Long)
   - Default easing: CubicInOut (smooth acceleration/deceleration)
   - Store animation in GeoAnimator.animations list

2. Create Animation data class (internal):
   - Properties: `targetValue: T`, `duration: Long`, `easing: Easing`, `startTime: Long`
   - Track start time when animation added
   - Calculate progress based on elapsed time and easing

3. Create GeoAnimator.animateAnimation() extension method:
   - Called during updateAnimation() loop
   - Remove completed animations from list
   - Apply easing and update property values

4. Implement per-property easing examples in KDoc:
   - Position uses easeOut (natural deceleration)
   - Color uses linear (smooth color blend)
   - Size uses easeIn (grow effect)

Do NOT:
- Use reflection or string-based property names (use ::propertyName)
- Implement manual easing math (use Easing enum)
- Create thousands of animation objects for many features (will cause GC pressure - reuse in global animator)
  </action>
  <verify>
Property reference extension compiles (::x.animate() syntax works).
Animation stores start time and calculates progress correctly.
Completed animations removed from list.
  </verify>
  <done>
Property tweening via ::property.animate(to, duration, easing) extension function
Animation data class tracks target, duration, easing, startTime
GeoAnimator.animateAnimation() updates animations in loop
Per-property easing documented in KDoc
  </done>
</task>

</tasks>

<verification>
- Synthetic test: Create dummy property and call ::property.animate()
- LinearInterpolator and HaversineInterpolator produce correct intermediate values
- Animation added to GeoAnimator.animations and stores start time
- Animation calculates progress correctly over duration
- Completed animations removed from GeoAnimator.animations
- Per-property easing applied correctly (easeOut, linear, easeIn)
</verification>

<success_criteria>
Property tweening system with ::property.animate() extension function
Interpolator implementations (linear for performance, Haversine for accuracy)
Animation tracking with progress calculation and cleanup
Per-property easing with different easing curves per property
All code compiles without external dependencies beyond openrndr-animatable
</success_criteria>

<output>
After completion, create `.planning/phases/05-animation/05-02-SUMMARY.md` with:
- Tech-stack.added: [] (all built-in: property references, linear, Haversine)
- Key files created: ["Tweening.kt", "LinearInterpolator.kt", "HaversineInterpolator.kt"]
- Patterns established: ["Property reference tweening", "Linear vs Haversine interpolation", "Per-property easing"]
</output>