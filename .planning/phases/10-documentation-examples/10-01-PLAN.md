---
phase: 10-documentation-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/README.md
  - examples/core/README.md
  - examples/render/README.md
  - examples/proj/README.md
  - examples/anim/README.md
  - examples/layer/README.md
  - examples/data/geo/sample.geojson
  - examples/data/geo/coastline.geojson
  - examples/data/geo/ocean.geojson
  - examples/data/geo/rivers_lakes.geojson
  - examples/data/geo/populated_places.geojson
  - examples/data/geo/catchment-topo.geojson
  - examples/data/geo/UK-terr50-land_water_boundary.gpkg
  - examples/data/geo/ness-vectors.gpkg
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "User can find examples directory at project root"
    - "User sees examples organized into category folders (core, render, proj, anim, layer)"
    - "User finds sample data files in examples/data/geo/"
    - "User understands example structure from root README"
  artifacts:
    - path: "examples/README.md"
      provides: "Root examples guide"
      min_lines: 30
    - path: "examples/data/geo/"
      provides: "Sample data files for examples"
      contains: "*.geojson, *.gpkg"
    - path: "examples/core/README.md"
      provides: "Core examples overview"
      min_lines: 15
  key_links:
    - from: "examples/README.md"
      to: "examples/{category}/"
      via: "directory listing"
      pattern: "\\[.*\\]\\(.*\\)"
---

<objective>
Create the examples directory structure with category folders, copy sample data files, and establish README documentation templates.

Purpose: Establish the foundation for runnable examples so users can browse and understand the library structure before diving into code.
Output: `examples/` directory with categories, data files, and documentation templates
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation-examples/10-CONTEXT.md
@.planning/phases/10-documentation-examples/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Create directory structure and copy data files</name>
  <files>
    examples/core/
    examples/render/
    examples/proj/
    examples/anim/
    examples/layer/
    examples/data/geo/
  </files>
  <action>
Create the examples directory structure and copy all data files from `data/geo/` to `examples/data/geo/`.

**Directory structure to create:**
```
examples/
├── data/
│   └── geo/
├── core/
├── render/
├── proj/
├── anim/
└── layer/
```

**Data files to copy (from `data/geo/` to `examples/data/geo/`):**
- sample.geojson
- coastline.geojson
- ocean.geojson
- rivers_lakes.geojson
- populated_places.geojson
- catchment-topo.geojson
- UK-terr50-land_water_boundary.gpkg
- ness-vectors.gpkg

Also copy `data/sample.geojson` to `examples/data/geo/sample.geojson` if it differs.

Use `mkdir -p` and `cp` commands. Do NOT use symbolic links (examples must be self-contained).
  </action>
  <verify>
    <automated>ls -la examples/data/geo/ | grep -c ".geojson\|.gpkg" | xargs -I {} test {} -ge 7 && echo "PASS: Data files copied" || echo "FAIL: Missing data files"</automated>
    <manual>Verify all 8 data files exist in examples/data/geo/</manual>
  </verify>
  <done>examples/ directory exists with all category subdirectories and examples/data/geo/ contains all sample data files</done>
</task>

<task type="auto">
  <name>task 2: Create root README and category README templates</name>
  <files>
    examples/README.md
    examples/core/README.md
    examples/render/README.md
    examples/proj/README.md
    examples/anim/README.md
    examples/layer/README.md
  </files>
  <action>
Create README files for the examples directory structure.

**Root README (examples/README.md):**
- Title: "OPENRNDR Geo Examples"
- Brief description of the library
- List of categories with links and brief descriptions:
  - **core/**: Data loading and inspection
  - **render/**: Rendering geographic features
  - **proj/**: Projections and coordinate systems
  - **anim/**: Animation patterns
  - **layer/**: Layer composition and overlays
- How to run examples: `./gradlew run -Popenrndr.application=examples.category.FileNameKt`
- Note about data files in `data/geo/`

**Category README templates:**
Each category README should have:
- Title: "{Category Name} Examples"
- Brief description of what this category covers
- Placeholder "## Examples" section (will be filled in subsequent plans)
- "## Key Concepts" section with 2-3 bullet points

Category names:
- core → "Core Examples"
- render → "Render Examples"
- proj → "Projection Examples"
- anim → "Animation Examples"
- layer → "Layer Examples"

Do NOT use emojis in README files.
  </action>
  <verify>
    <automated>test -f examples/README.md && grep -q "OPENRNDR Geo Examples" examples/README.md && echo "PASS: Root README exists" || echo "FAIL: Root README missing"</automated>
    <manual>Verify each category has a README with title and structure</manual>
  </verify>
  <done>All README files exist with proper structure and content</done>
</task>

</tasks>

<verification>
- [ ] `examples/` directory exists with all 5 category subdirectories
- [ ] `examples/data/geo/` contains all 8 data files
- [ ] `examples/README.md` exists with category links and run instructions
- [ ] Each category folder has a README.md template
</verification>

<success_criteria>
- Directory structure matches planned layout
- All data files copied (not symlinked)
- Root README explains how to run examples
- Category READMEs have consistent structure
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-examples/10-01-SUMMARY.md`
</output>
