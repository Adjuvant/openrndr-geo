---
phase: 10-documentation-examples
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - examples/core/01-load-geojson.kt
  - examples/core/02-load-geopackage.kt
  - examples/core/03-print-summary.kt
  - examples/core/README.md
  - examples/render/01-points.kt
  - examples/render/02-linestrings.kt
  - examples/render/03-polygons.kt
  - examples/render/04-multipolygons.kt
  - examples/render/05-style-dsl.kt
  - examples/render/README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "User can load GeoJSON and see features loaded"
    - "User can load GeoPackage and see features loaded"
    - "User can print summary of data source"
    - "User can render points, lines, polygons with visible output"
    - "Each example demonstrates exactly ONE primary concept"
  artifacts:
    - path: "examples/core/01-load-geojson.kt"
      provides: "GeoJSON loading example"
      min_lines: 25
    - path: "examples/core/03-print-summary.kt"
      provides: "Data inspection example"
      min_lines: 30
    - path: "examples/render/01-points.kt"
      provides: "Point rendering example"
      min_lines: 40
    - path: "examples/render/05-style-dsl.kt"
      provides: "Style DSL example"
      min_lines: 50
  key_links:
    - from: "examples/core/*.kt"
      to: "examples/data/geo/"
      via: "GeoJSON.load() and GeoPackage.load()"
      pattern: "GeoJSON\\.load\\(.*examples/data"
    - from: "examples/render/*.kt"
      to: "geo.render.Style"
      via: "Style { } DSL"
      pattern: "Style\\s*\\{"
---

<objective>
Create core examples (data loading, inspection) and render examples (point, line, polygon rendering, Style DSL).

Purpose: Provide minimal, single-concept examples that teach users the fundamental operations of the library.
Output: 8 example files with KDoc documentation, updated category READMEs
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation-examples/10-CONTEXT.md
@.planning/phases/10-documentation-examples/10-RESEARCH.md
@src/main/kotlin/geo/examples/core_printSummary.kt
@src/main/kotlin/geo/examples/render_BasicRendering.kt
@src/main/kotlin/geo/render/Style.kt
</context>

<tasks>

<task type="auto">
  <name>task 1: Create core examples (01-03)</name>
  <files>
    examples/core/01-load-geojson.kt
    examples/core/02-load-geopackage.kt
    examples/core/03-print-summary.kt
  </files>
  <action>
Create three core examples with KDoc headers. Adapt from existing `src/main/kotlin/geo/examples/core_*.kt` files.

**01-load-geojson.kt:**
- Package: `examples.core`
- KDoc header with: title, description, concepts covered, run command
- Demonstrates: Loading GeoJSON with `GeoJSON.load()`
- Data: `examples/data/geo/sample.geojson`
- Output: Print feature count to console
- ONE concept: GeoJSON loading

**02-load-geopackage.kt:**
- Package: `examples.core`
- KDoc header with: title, description, concepts covered, run command
- Demonstrates: Loading GeoPackage with `GeoPackage.load()`
- Data: `examples/data/geo/ness-vectors.gpkg`
- Output: Print feature count to console
- ONE concept: GeoPackage loading

**03-print-summary.kt:**
- Package: `examples.core`
- KDoc header with: title, description, concepts covered, run command
- Demonstrates: Using `printSummary()` for data inspection
- Data: Both GeoJSON and GeoPackage
- Output: Formatted summary to console (uses existing printSummary implementation)
- ONE concept: Data inspection

KDoc format for each file:
```kotlin
/**
 * ## {NN} - {Title}
 *
 * {One-line description}
 *
 * ### Concepts
 * - {Concept 1}
 * - {Concept 2}
 *
 * ### To Run
 * ```
 * ./gradlew run -Popenrndr.application=examples.core.{ClassName}Kt
 * ```
 */
```

Run command format: `./gradlew run -Popenrndr.application=examples.core.FileNameKt`
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -q "BUILD SUCCESSFUL" && echo "PASS: Core examples compile" || echo "FAIL: Compilation error"</automated>
    <manual>Verify each core example has KDoc with title, concepts, and run command</manual>
  </verify>
  <done>Three core example files exist with KDoc, compile successfully, use correct data paths</done>
</task>

<task type="auto">
  <name>task 2: Create render examples (01-05)</name>
  <files>
    examples/render/01-points.kt
    examples/render/02-linestrings.kt
    examples/render/03-polygons.kt
    examples/render/04-multipolygons.kt
    examples/render/05-style-dsl.kt
  </files>
  <action>
Create five render examples with KDoc headers. Adapt from existing `src/main/kotlin/geo/examples/render_BasicRendering.kt` - SPLIT into focused single-concept examples.

**01-points.kt:**
- Package: `examples.render`
- Demonstrates: Loading data, creating projection, rendering points with `drawPoint()`
- Data: `examples/data/geo/populated_places.geojson` (points)
- ONE concept: Point rendering

**02-linestrings.kt:**
- Package: `examples.render`
- Demonstrates: Rendering LineString features with `drawLineString()`
- Data: `examples/data/geo/rivers_lakes.geojson` or `coastline.geojson`
- ONE concept: LineString rendering

**03-polygons.kt:**
- Package: `examples.render`
- Demonstrates: Rendering Polygon features with `drawPolygon()`, including holes
- Data: `examples/data/geo/sample.geojson` or create synthetic polygon
- ONE concept: Polygon rendering

**04-multipolygons.kt:**
- Package: `examples.render`
- Demonstrates: Rendering MultiPolygon features with `drawMultiPolygon()`
- Data: `examples/data/geo/ocean.geojson` (world ocean polygons)
- ONE concept: MultiPolygon rendering

**05-style-dsl.kt:**
- Package: `examples.render`
- Demonstrates: Using `Style { }` DSL for styling configuration
- Shows: fill, stroke, strokeWeight, size, shape properties
- Data: Any point/polygon data
- ONE concept: Style DSL configuration

Each example MUST:
- Use `application { program { ... } }` pattern
- Use `ProjectionFactory.fitBounds()` or `fitWorldMercator()`
- Use `Style { }` DSL (not inline Style() constructor)
- Have KDoc header with concepts and run command
- Use `examples/data/geo/` paths (NOT `data/`)

Do NOT include animation, layers, or advanced features in render examples.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -q "BUILD SUCCESSFUL" && echo "PASS: Render examples compile" || echo "FAIL: Compilation error"</automated>
    <manual>Verify each render example demonstrates ONE concept, uses Style DSL, has correct paths</manual>
  </verify>
  <done>Five render example files exist with KDoc, compile successfully, each demonstrates single concept</done>
</task>

<task type="auto">
  <name>task 3: Update core and render READMEs</name>
  <files>
    examples/core/README.md
    examples/render/README.md
  </files>
  <action>
Update the category READMEs with complete example listings and key concepts.

**examples/core/README.md:**
Update the placeholder with:
- Overview: Core functionality for loading and inspecting geo data
- Examples table:
  | File | Concept | Data Used |
  |------|---------|-----------|
  | 01-load-geojson.kt | GeoJSON loading | sample.geojson |
  | 02-load-geopackage.kt | GeoPackage loading | ness-vectors.gpkg |
  | 03-print-summary.kt | Data inspection | sample.geojson, ness-vectors.gpkg |
- Key Concepts section:
  1. `GeoJSON.load()` and `GeoPackage.load()` for data loading
  2. `printSummary()` for runtime inspection
  3. Console output (no window) for core examples

**examples/render/README.md:**
Update the placeholder with:
- Overview: Rendering geographic features with OPENRNDR drawer
- Examples table with all 5 examples
- Key Concepts section:
  1. `Style { }` DSL for styling configuration
  2. Projection transforms coordinates to screen space
  3. Geometry-specific draw functions: `drawPoint`, `drawLineString`, `drawPolygon`, `drawMultiPolygon`

Do NOT use emojis. Keep tables aligned.
  </action>
  <verify>
    <automated>grep -q "01-load-geojson" examples/core/README.md && grep -q "01-points" examples/render/README.md && echo "PASS: READMEs updated" || echo "FAIL: READMEs missing examples"</automated>
    <manual>Verify README tables are complete and formatted correctly</manual>
  </verify>
  <done>Core and render READMEs have complete example listings and key concepts</done>
</task>

</tasks>

<verification>
- [ ] Three core examples exist with KDoc and correct data paths
- [ ] Five render examples exist with KDoc, Style DSL, and projection setup
- [ ] All examples compile: `./gradlew compileKotlin`
- [ ] Core README has complete example table
- [ ] Render README has complete example table and key concepts
</verification>

<success_criteria>
- All 8 example files compile without errors
- Each example has KDoc with title, concepts, and run command
- Each example demonstrates exactly ONE primary concept
- READMEs accurately document the examples
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-examples/10-02-SUMMARY.md`
</output>
