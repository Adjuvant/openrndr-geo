---
phase: 10-documentation-examples
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - examples/proj/01-mercator.kt
  - examples/proj/02-fit-bounds.kt
  - examples/proj/03-crs-transform.kt
  - examples/proj/README.md
  - examples/anim/01-basic-animation.kt
  - examples/anim/02-geo-animator.kt
  - examples/anim/03-timeline.kt
  - examples/anim/README.md
  - examples/layer/01-graticule.kt
  - examples/layer/02-composition.kt
  - examples/layer/README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "User can create Mercator projection for world-scale rendering"
    - "User can fit projection to data bounds"
    - "User can transform coordinates between CRS"
    - "User can animate geo features with basic tweening"
    - "User can compose multiple layers (graticule + data)"
    - "Each example demonstrates exactly ONE primary concept"
  artifacts:
    - path: "examples/proj/01-mercator.kt"
      provides: "Mercator projection example"
      min_lines: 40
    - path: "examples/proj/02-fit-bounds.kt"
      provides: "Fit bounds projection example"
      min_lines: 45
    - path: "examples/anim/01-basic-animation.kt"
      provides: "Basic animation example"
      min_lines: 50
    - path: "examples/layer/01-graticule.kt"
      provides: "Graticule layer example"
      min_lines: 50
  key_links:
    - from: "examples/proj/*.kt"
      to: "geo.projection.ProjectionFactory"
      via: "ProjectionFactory.fitBounds() / fitWorldMercator()"
      pattern: "ProjectionFactory\\."
    - from: "examples/anim/*.kt"
      to: "org.openrndr.animatable"
      via: "Animatable animation methods"
      pattern: "::\\w+\\.animate"
    - from: "examples/layer/*.kt"
      to: "geo.layer"
      via: "Layer composition"
      pattern: "layer|Layer"
---

<objective>
Create projection, animation, and layer examples with KDoc documentation and update category READMEs.

Purpose: Complete the example library with examples for projections, animations, and layer composition.
Output: 8 example files with KDoc documentation, updated category READMEs
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation-examples/10-CONTEXT.md
@.planning/phases/10-documentation-examples/10-RESEARCH.md
@src/main/kotlin/geo/examples/proj_ProjectionTest.kt
@src/main/kotlin/geo/examples/proj_HaversineDemo.kt
@src/main/kotlin/geo/examples/anim_BasicAnimation.kt
@src/main/kotlin/geo/examples/anim_TimelineDemo.kt
@src/main/kotlin/geo/examples/layer_Graticule.kt
@src/main/kotlin/geo/examples/layer_Composition.kt
@src/main/kotlin/geo/projection/ProjectionFactory.kt
</context>

<tasks>

<task type="auto">
  <name>task 1: Create projection examples (01-03)</name>
  <files>
    examples/proj/01-mercator.kt
    examples/proj/02-fit-bounds.kt
    examples/proj/03-crs-transform.kt
  </files>
  <action>
Create three projection examples with KDoc headers. Adapt from existing `src/main/kotlin/geo/examples/proj_*.kt` files.

**01-mercator.kt:**
- Package: `examples.proj`
- Demonstrates: Using `ProjectionFactory.fitWorldMercator()` for world-scale rendering
- Shows: How Mercator projection works, zoom levels
- Data: `examples/data/geo/coastline.geojson`
- ONE concept: World Mercator projection

**02-fit-bounds.kt:**
- Package: `examples.proj`
- Demonstrates: Using `ProjectionFactory.fitBounds()` to fit data to viewport
- Shows: Auto-fitting projection to data extent, padding parameter
- Data: `examples/data/geo/sample.geojson` or `populated_places.geojson`
- ONE concept: Fit bounds projection

**03-crs-transform.kt:**
- Package: `examples.proj`
- Demonstrates: CRS transformation using Proj4J
- Shows: Converting coordinates between EPSG codes (e.g., WGS84 to British National Grid)
- Data: May need synthetic coordinates or use existing data
- ONE concept: CRS transformation

Each example MUST:
- Have KDoc header with title, concepts, run command
- Use `examples/data/geo/` paths
- Show the key API call clearly
- Include visual output where applicable (render projected data)

Do NOT combine multiple projection concepts in one file.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -q "BUILD SUCCESSFUL" && echo "PASS: Projection examples compile" || echo "FAIL: Compilation error"</automated>
    <manual>Verify each projection example has KDoc and demonstrates single concept</manual>
  </verify>
  <done>Three projection example files exist with KDoc, compile successfully, each demonstrates single concept</done>
</task>

<task type="auto">
  <name>task 2: Create animation examples (01-03)</name>
  <files>
    examples/anim/01-basic-animation.kt
    examples/anim/02-geo-animator.kt
    examples/anim/03-timeline.kt
  </files>
  <action>
Create three animation examples with KDoc headers. Adapt from existing `src/main/kotlin/geo/examples/anim_*.kt` files.

**01-basic-animation.kt:**
- Package: `examples.anim`
- Demonstrates: Basic property animation using OPENRNDR Animatable
- Shows: `::property.animate()` syntax, easing functions
- Data: Synthetic point or simple geometry
- ONE concept: Basic property animation

**02-geo-animator.kt:**
- Package: `examples.anim`
- Demonstrates: Animating geo-specific properties (coordinates, styles)
- Shows: Animating point position, style properties (size, color)
- Data: `examples/data/geo/populated_places.geojson` or synthetic
- ONE concept: Geo property animation

**03-timeline.kt:**
- Package: `examples.anim`
- Demonstrates: Timeline-based animation sequencing
- Shows: Keyframes, sequencing multiple animations
- Data: Synthetic or simple dataset
- ONE concept: Timeline animation

Each example MUST:
- Have KDoc header with title, concepts, run command
- Use `extend { }` block for animation updates
- Call `animation.updateAnimation()` if using Animatable
- Show smooth visual animation in OPENRNDR window

Do NOT include layer composition or advanced effects in animation examples.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -q "BUILD SUCCESSFUL" && echo "PASS: Animation examples compile" || echo "FAIL: Compilation error"</automated>
    <manual>Verify each animation example has KDoc and demonstrates single concept</manual>
  </verify>
  <done>Three animation example files exist with KDoc, compile successfully, each demonstrates single concept</done>
</task>

<task type="auto">
  <name>task 3: Create layer examples (01-02)</name>
  <files>
    examples/layer/01-graticule.kt
    examples/layer/02-composition.kt
  </files>
  <action>
Create two layer examples with KDoc headers. Adapt from existing `src/main/kotlin/geo/examples/layer_*.kt` files.

**01-graticule.kt:**
- Package: `examples.layer`
- Demonstrates: Drawing graticule (grid of latitude/longitude lines)
- Shows: How to render reference grid over map
- Data: May use `examples/data/geo/coastline.geojson` as background
- ONE concept: Graticule layer

**02-composition.kt:**
- Package: `examples.layer`
- Demonstrates: Composing multiple layers (data + graticule + background)
- Shows: Layer ordering, blend modes, ColorBuffer composition
- Data: Multiple data files combined
- ONE concept: Layer composition

Each example MUST:
- Have KDoc header with title, concepts, run command
- Use `examples/data/geo/` paths
- Show clear layer separation in code

Do NOT include animation in layer examples.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -q "BUILD SUCCESSFUL" && echo "PASS: Layer examples compile" || echo "FAIL: Compilation error"</automated>
    <manual>Verify each layer example has KDoc and demonstrates single concept</manual>
  </verify>
  <done>Two layer example files exist with KDoc, compile successfully, each demonstrates single concept</done>
</task>

<task type="auto">
  <name>task 4: Update proj, anim, and layer READMEs</name>
  <files>
    examples/proj/README.md
    examples/anim/README.md
    examples/layer/README.md
  </files>
  <action>
Update the category READMEs with complete example listings and key concepts.

**examples/proj/README.md:**
- Overview: Projection and coordinate system examples
- Examples table with all 3 examples
- Key Concepts:
  1. `ProjectionFactory.fitWorldMercator()` for world-scale maps
  2. `ProjectionFactory.fitBounds()` for data-specific projections
  3. CRS transformation for coordinate conversion

**examples/anim/README.md:**
- Overview: Animation patterns for geographic data
- Examples table with all 3 examples
- Key Concepts:
  1. OPENRNDR Animatable for property animation
  2. Easing functions for smooth motion
  3. Timeline for sequenced animations

**examples/layer/README.md:**
- Overview: Layer composition and visual overlays
- Examples table with both examples
- Key Concepts:
  1. Graticules for map reference
  2. Layer composition with blend modes
  3. ColorBuffer for offscreen rendering

Do NOT use emojis. Keep tables aligned. Match the format from core and render READMEs.
  </action>
  <verify>
    <automated>grep -q "01-mercator" examples/proj/README.md && grep -q "01-basic-animation" examples/anim/README.md && grep -q "01-graticule" examples/layer/README.md && echo "PASS: READMEs updated" || echo "FAIL: READMEs missing examples"</automated>
    <manual>Verify all README tables are complete and formatted correctly</manual>
  </verify>
  <done>All three category READMEs have complete example listings and key concepts</done>
</task>

</tasks>

<verification>
- [ ] Three projection examples exist with KDoc and correct imports
- [ ] Three animation examples exist with KDoc and animation patterns
- [ ] Two layer examples exist with KDoc and layer composition
- [ ] All examples compile: `./gradlew compileKotlin`
- [ ] All three READMEs have complete example tables
</verification>

<success_criteria>
- All 8 example files compile without errors
- Each example has KDoc with title, concepts, and run command
- Each example demonstrates exactly ONE primary concept
- READMEs accurately document the examples
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-examples/10-03-SUMMARY.md`
</output>
