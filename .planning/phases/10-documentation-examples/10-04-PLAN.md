---
phase: 10-documentation-examples
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified: []
autonomous: false
requirements:
  - DOC-04

must_haves:
  truths:
    - "All examples compile without errors"
    - "All examples run without exceptions"
    - "User can visually verify rendered output"
    - "Examples validate that framework features work correctly"
  artifacts:
    - path: "examples/"
      provides: "Complete runnable example library"
      contains: "16+ example files"
  key_links:
    - from: "examples/"
      to: "src/main/kotlin/geo/"
      via: "Import statements"
      pattern: "import geo\\."
---

<objective>
Validate all examples compile and run correctly, serving as UAT for the framework.

Purpose: Ensure examples are not just documentation but also validation that the library features work correctly.
Output: Verified working example library
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation-examples/10-CONTEXT.md
@.planning/phases/10-documentation-examples/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Compile all examples</name>
  <files></files>
  <action>
Run full compilation to verify all examples compile without errors.

Execute:
```bash
./gradlew compileKotlin
```

If compilation fails:
1. Identify the failing example(s) from error output
2. Fix import statements, API usage, or syntax errors
3. Re-run compilation until successful

Common issues to check:
- Missing imports for geo classes
- Wrong data file paths (should be `examples/data/geo/`)
- Deprecated API usage (check against current source)
- Package name mismatches

The build MUST succeed before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -q "BUILD SUCCESSFUL" && echo "PASS: All examples compile" || echo "FAIL: Compilation errors exist"</automated>
    <manual>Review any compiler warnings and fix if significant</manual>
  </verify>
  <done>All 16+ example files compile successfully with `./gradlew compileKotlin`</done>
</task>

<task type="auto">
  <name>task 2: Run core examples (console output)</name>
  <files></files>
  <action>
Run the core examples to verify they execute without exceptions. Core examples produce console output, not visual windows.

Run each core example:
```bash
./gradlew run -Popenrndr.application=examples.core.LoadGeojsonKt
./gradlew run -Popenrndr.application=examples.core.LoadGeopackageKt
./gradlew run -Popenrndr.application=examples.core.PrintSummaryKt
```

For each example:
1. Verify it starts and completes without exceptions
2. Verify console output shows expected information
3. Note any errors for fixing

If an example fails:
1. Check the error message
2. Fix data path or API usage
3. Re-run

All three core examples MUST run successfully.
  </action>
  <verify>
    <automated>./gradlew run -Popenrndr.application=examples.core.PrintSummaryKt 2>&1 | grep -q "BUILD SUCCESSFUL\|inspection completed" && echo "PASS: Core examples run" || echo "FAIL: Core example failed"</automated>
    <manual>Verify console output shows expected data summary</manual>
  </verify>
  <done>All three core examples run without exceptions and produce expected console output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete example library with 16+ runnable examples across 5 categories (core, render, proj, anim, layer). All examples compile and core examples run successfully.</what-built>
  <how-to-verify>
**Visual UAT Verification**

1. **Run render examples** (opens OPENRNDR window):
   ```bash
   ./gradlew run -Popenrndr.application=examples.render.PointsKt
   ./gradlew run -Popenrndr.application=examples.render.PolygonsKt
   ./gradlew run -Popenrndr.application=examples.render.MultipolygonsKt
   ```
   - Verify points/polygons appear on screen
   - Verify colors match Style DSL configuration
   - Close window with ESC

2. **Run projection examples**:
   ```bash
   ./gradlew run -Popenrndr.application=examples.proj.MercatorKt
   ./gradlew run -Popenrndr.application=examples.proj.FitBoundsKt
   ```
   - Verify world coastline appears correctly in Mercator
   - Verify fit-bounds zooms to data extent

3. **Run animation examples**:
   ```bash
   ./gradlew run -Popenrndr.application=examples.anim.BasicAnimationKt
   ```
   - Verify smooth animation plays
   - Verify no flickering or performance issues

4. **Run layer examples**:
   ```bash
   ./gradlew run -Popenrndr.application=examples.layer.GraticuleKt
   ```
   - Verify graticule grid lines appear
   - Verify alignment with data

5. **Check README documentation**:
   - Open `examples/README.md` - verify run instructions are correct
   - Check each category README has complete example table

**Expected outcome**: All tested examples show correct visual output, no exceptions, documentation is accurate.
  </how-to-verify>
  <resume-signal>Type "approved" if all examples work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- [ ] All examples compile: `./gradlew compileKotlin` succeeds
- [ ] Core examples run and produce console output
- [ ] Render examples show correct visual output
- [ ] Animation examples show smooth motion
- [ ] Layer examples show correct composition
- [ ] README documentation is accurate
</verification>

<success_criteria>
- All 16+ examples compile without errors
- All tested examples run without exceptions
- Visual output matches expected behavior
- Documentation accurately describes examples
- User confirms examples work as expected
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-examples/10-04-SUMMARY.md`
</output>
