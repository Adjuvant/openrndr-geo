---
phase: 18-code-organization
plan: 03
type: execute
wave: 2
depends_on:
  - 18-02
files_modified:
  - src/main/kotlin/geo/animation/**/*.kt
  - src/main/kotlin/geo/crs/**/*.kt
  - src/main/kotlin/geo/examples/**/*.kt
  - src/main/kotlin/geo/internal/**/*.kt
  - src/main/kotlin/geo/layer/**/*.kt
  - src/main/kotlin/geo/projection/**/*.kt
  - src/main/kotlin/geo/render/**/*.kt
  - src/main/kotlin/geo/tools/**/*.kt
  - examples/**/*.kt
  - src/test/**/*.kt
autonomous: true
requirements:
  - ORG-03

must_haves:
  truths:
    - "All imports updated from geo.X to geo.core.X across codebase"
    - "Project compiles without import errors"
  artifacts:
    - path: "src/main/kotlin/geo/"
      provides: "All imports updated to geo.core.*"
    - path: "examples/"
      provides: "All imports updated to geo.core.*"
    - path: "src/test/"
      provides: "All imports updated to geo.core.*"
  key_links:
    - from: "src/main/kotlin/geo/animation/*.kt"
      to: "geo.core.GeoSource"
      via: "import statement"
    - from: "src/main/kotlin/geo/layer/*.kt"
      to: "geo.core.Feature"
      via: "import statement"
    - from: "src/main/kotlin/geo/render/*.kt"
      to: "geo.core.Geometry"
      via: "import statement"
---

<objective>
Update all import statements across the codebase from geo.X to geo.core.X.

Purpose: Maintain compilation after ORG-02 structural change. Hard break — no backward compatibility.
Output: All imports updated, project compiles
</objective>

<context>
@.planning/phases/18-code-organization/18-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Update imports in src/main/kotlin/geo subdirectories</name>
  <files>
    - src/main/kotlin/geo/animation/**/*.kt
    - src/main/kotlin/geo/crs/**/*.kt
    - src/main/kotlin/geo/internal/**/*.kt
    - src/main/kotlin/geo/layer/**/*.kt
    - src/main/kotlin/geo/projection/**/*.kt
    - src/main/kotlin/geo/render/**/*.kt
    - src/main/kotlin/geo/tools/**/*.kt
  </files>
  <action>
    Update all imports from `geo.X` to `geo.core.X` across src/main/kotlin/geo subdirectories.

    1. First, find all files with old imports:
    ```
    grep -rl "import geo\." src/main/kotlin/geo/ --include="*.kt" | grep -v "core/"
    ```

    2. Update import patterns:
    - `import geo\.Bounds` → `import geo.core.Bounds`
    - `import geo\.CachedGeoSource` → `import geo.core.CachedGeoSource`
    - `import geo\.Feature` → `import geo.core.Feature`
    - `import geo\.GeoJSON` → `import geo.core.GeoJSON`
    - `import geo\.Geometry` → `import geo.core.Geometry`
    - `import geo\.GeoPackage` → `import geo.core.GeoPackage`
    - `import geo\.GeoSource` → `import geo.core.GeoSource`
    - `import geo\.GeoSourceConvenience` → `import geo.core.GeoSourceConvenience`
    - `import geo\.GeoStack` → `import geo.core.GeoStack`
    - `import geo\.ProjectionExtensions` → `import geo.core.ProjectionExtensions`
    - `import geo\.SpatialIndex` → `import geo.core.SpatialIndex`
    - `import geo\.loadGeo` → `import geo.core.loadGeo`
    - `import geo\.project` → `import geo.core.project`

    Use sed to batch replace:
    ```
    find src/main/kotlin/geo -name "*.kt" ! -path "*/core/*" -exec sed -i '' \
      -e 's/import geo\.Bounds/import geo.core.Bounds/g' \
      -e 's/import geo\.CachedGeoSource/import geo.core.CachedGeoSource/g' \
      -e 's/import geo\.Feature/import geo.core.Feature/g' \
      -e 's/import geo\.GeoJSON/import geo.core.GeoJSON/g' \
      -e 's/import geo\.Geometry/import geo.core.Geometry/g' \
      -e 's/import geo\.GeoPackage/import geo.core.GeoPackage/g' \
      -e 's/import geo\.GeoSource/import geo.core.GeoSource/g' \
      -e 's/import geo\.GeoSourceConvenience/import geo.core.GeoSourceConvenience/g' \
      -e 's/import geo\.GeoStack/import geo.core.GeoStack/g' \
      -e 's/import geo\.ProjectionExtensions/import geo.core.ProjectionExtensions/g' \
      -e 's/import geo\.SpatialIndex/import geo.core.SpatialIndex/g' \
      -e 's/import geo\.loadGeo/import geo.core.loadGeo/g' \
      -e 's/import geo\.project/import geo.core.project/g' {} \;
    ```
  </action>
  <verify>
    <automated>grep -r "^import geo\." src/main/kotlin/geo/ --include="*.kt" | grep -v "geo.core" | wc -l | xargs -I {} sh -c 'if [ {} -eq 0 ]; then echo "All imports updated in src/main"; else echo "ERROR: {} files still have old imports"; fi'</automated>
  </verify>
  <done>All imports in src/main/kotlin/geo subdirectories updated to geo.core.*</done>
</task>

<task type="auto">
  <name>task 2: Update imports in examples/ directory</name>
  <files>
    - examples/**/*.kt
  </files>
  <action>
    Update imports in examples/ directory using same sed patterns as task 1.

    ```
    find examples -name "*.kt" -exec sed -i '' \
      -e 's/import geo\.Bounds/import geo.core.Bounds/g' \
      -e 's/import geo\.CachedGeoSource/import geo.core.CachedGeoSource/g' \
      -e 's/import geo\.Feature/import geo.core.Feature/g' \
      -e 's/import geo\.GeoJSON/import geo.core.GeoJSON/g' \
      -e 's/import geo\.Geometry/import geo.core.Geometry/g' \
      -e 's/import geo\.GeoPackage/import geo.core.GeoPackage/g' \
      -e 's/import geo\.GeoSource/import geo.core.GeoSource/g' \
      -e 's/import geo\.GeoSourceConvenience/import geo.core.GeoSourceConvenience/g' \
      -e 's/import geo\.GeoStack/import geo.core.GeoStack/g' \
      -e 's/import geo\.ProjectionExtensions/import geo.core.ProjectionExtensions/g' \
      -e 's/import geo\.SpatialIndex/import geo.core.SpatialIndex/g' \
      -e 's/import geo\.loadGeo/import geo.core.loadGeo/g' \
      -e 's/import geo\.project/import geo.core.project/g' {} \;
    ```
  </action>
  <verify>
    <automated>grep -r "^import geo\." examples/ --include="*.kt" | grep -v "geo.core" | wc -l | xargs -I {} sh -c 'if [ {} -eq 0 ]; then echo "All imports updated in examples"; else echo "ERROR: {} files still have old imports"; fi'</automated>
  </verify>
  <done>All imports in examples/ updated to geo.core.*</done>
</task>

<task type="auto">
  <name>task 3: Verify compilation</name>
  <files>
    - (compilation check)
  </files>
  <action>
    Run Gradle compile to verify all imports are correct:

    ```
    ./gradlew compileKotlin --no-daemon 2>&1
    ```

    If compilation fails, identify remaining import issues and fix them.
  </action>
  <verify>
    <automated>./gradlew compileKotlin --no-daemon 2>&1 | tail -20</automated>
  </verify>
  <done>Project compiles without import errors</done>
</task>

</tasks>

<verification>
- No `import geo.X` (without .core) remain in src/main or examples
- `./gradlew compileKotlin` passes
- All test files also compile
</verification>

<success_criteria>
- All imports updated from `geo.X` to `geo.core.X` across entire codebase
- Project compiles successfully
- No hardcoded references to old package path remain
</success_criteria>

<output>
After completion, create `.planning/phases/18-code-organization/18-03-SUMMARY.md`
</output>
