---
phase: 04-layer-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/layer/GeoLayer.kt
  - src/main/kotlin/geo/layer/Graticule.kt
  - src/main/kotlin/geo/examples/LayerComposition.kt
  - src/main/kotlin/geo/examples/LayerBlendModes.kt
  - src/main/kotlin/geo/examples/LayerGraticule.kt
  - src/main/kotlin/geo/examples/LayerOutput.kt
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can stack multiple data sources as visual layers in single composition"
    - "User can apply blend modes (multiply, overlay, screen, add) to layers"
    - "User can draw graticule/grid lines for lat/lng reference"
    - "User can capture rendered output as image files using screenshot()"
  artifacts:
    - path: "src/main/kotlin/geo/layer/GeoLayer.kt"
      provides: "Layer wrapper with Source + Style + blendMode"
      exports: ["GeoLayer", "layer DSL"]
    - path: "src/main/kotlin/geo/layer/Graticule.kt"
      provides: "Graticule generator function"
      exports: ["generateGraticule"]
    - path: "src/main/kotlin/geo/examples/Layer*.kt"
      provides: "4 layer composition examples with real data"
      min_lines: 50
  key_links:
    - from: "src/main/kotlin/geo/examples/LayerComposition.kt"
      to: "src/main/kotlin/geo/layer/GeoLayer.kt"
      via: "import geo.layer.*"
      pattern: "import geo\\.layer\\."
    - from: "src/main/kotlin/geo/examples/Layer*.kt"
      to: "org.openrndr.extra.compositor.compose"
      via: "fun main() = compose { }"
      pattern: "compose\\s*\\{"
    - from: "src/main/kotlin/geo/layer/GeoLayer.kt"
      to: "src/main/kotlin/geo/render/render.kt"
      via: "draw functions used in layer content"
      pattern: "draw(Feature|Point|Line|Polygon)"
---

<objective>
Integrate orx-compositor for layer composition, create graticule generator, and demonstrate workflow with examples

Purpose: Enable users to compose visualizations from multiple data sources with blend modes and screenshot capture
Output: GeoLayer wrapper, graticule generator, and 4 examples showing layer composition patterns
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-layer-system/04-CONTEXT.md

# Relevant prior work
@.planning/phases/03-core-rendering/03-03-SUMMARY.md
@src/main/kotlin/geo/GeoSource.kt
@src/main/kotlin/geo/Feature.kt
@src/main/kotlin/geo/render/Style.kt
@src/main/kotlin/geo/render/render.kt
</context>

<tasks>

<task type="auto">
  <name>Create GeoLayer wrapper and graticule generator</name>
  <files>src/main/kotlin/geo/layer/GeoLayer.kt, src/main/kotlin/geo/layer/Graticule.kt</files>
  <action>
    # GeoLayer.kt
    Create src/main/kotlin/geo/layer/GeoLayer.kt:
    - GeoLayer data class wrapping: source: GeoSource, style: Style, blendMode: BlendMode
    - Use openrndr.extra.compositor.blend modes: Add(), Multiply(), Overlay(), Screen()
    - Provide DSL function: fun layer(init: GeoLayer.() -> Unit): GeoLayer
    - DSL syntax: layer { source = loadData(...); style = Style { fill = RED }; blendMode = Overlay() }
    - Document in KDoc: "Compositional wrapper for Source with Style and BlendMode"
    - No complex layer management (visibility, ordering) from CONTEXT.md - handled by compose { } order

    # Graticule.kt
    Create src/main/kotlin/geo/layer/Graticule.kt:
    - generateGraticule(spacing: Double, bounds: Bounds): List<Feature>
    - Generate Point features for grid lines (horizontal and vertical)
    - Use spacing for interval (e.g., 1.0, 5.0, 10.0 degrees)
    - Return Feature objects with Point geometries only (simple grid)
    - No CRS conversion required - lat/lng coordinates used directly in Feature
    - Document usage example in KDoc

    # Implementation notes
    - orx-compositor already in build.gradle.kts (line 20)
    - Use imports: org.openrndr.extra.compositor.*, org.openrndr.extra.compositor.blend.*
    - Blend mode enum: reference openrndr blend mode classes (Add, Multiply, Overlay, Screen)
  </action>
  <verify>grep -r "class GeoLayer\|object GeoLayer\|fun layer" src/main/kotlin/geo/layer/GeoLayer.kt && grep -r "fun generateGraticule" src/main/kotlin/geo/layer/Graticule.kt</verify>
  <done>GeoLayer.kt and Graticule.kt compile successfully with layer DSL function</done>
</task>

<task type="auto">
  <name>Create layer composition examples</name>
  <files>src/main/kotlin/geo/examples/LayerComposition.kt, src/main/kotlin/geo/examples/LayerBlendModes.kt</files>
  <action>
    # LayerComposition.kt
    Create src/main/kotlin/geo/examples/LayerComposition.kt:
    - Demonstrate basic layer stacking using orx-compositor
    - Use compose { layer { draw { ... } } } DSL pattern
    - Layer 1: Graticule (from generateGraticule)
    - Layer 2: Real geo data (GeoPackage file from data/ directory)
    - Each layer has: source, style, blend mode (default Overlay or Multiply)
    - Example: compose { layer { source = generateGraticule(5.0, bounds); style = Style { ... } } }
    - Document: "Layer order matters - last drawn is top layer"

    # LayerBlendModes.kt
    Create src/main/kotlin/geo/examples/LayerBlendModes.kt:
    - Demonstrate each blend mode with visual comparison
    - 4 variants: Add, Multiply, Overlay, Screen
    - Use same base layer with different blend modes applied
    - Document: "Best blend modes for geo: Multiply for dark backgrounds, Overlay for detail, Screen for highlights"
    - Use real data from data/ directory (GeoPackage with polygons)
  </action>
  <verify>ls -la src/main/kotlin/geo/examples/LayerComposition.kt src/main/kotlin/geo/examples/LayerBlendModes.kt && grep -r "compose\s*{" src/main/kotlin/geo/examples/Layer*.kt</verify>
  <done>LayerComposition.kt and LayerBlendModes.kt compile with orx-compositor DSL</done>
</task>

<task type="auto">
  <name>Create graticule and output examples</name>
  <files>src/main/kotlin/geo/examples/LayerGraticule.kt, src/main/kotlin/geo/examples/LayerOutput.kt</files>
  <action>
    # LayerGraticule.kt
    Create src/main/kotlin/geo/examples/LayerGraticule.kt:
    - Demonstrate graticule usage with generateGraticule(spacing, bounds)
    - Layer 1: White graticule (spacing 5.0 degrees)
    - Layer 2: Geo data layer
    - Show different spacing values (1.0, 5.0, 10.0)
    - Document: "Graticule provides lat/lng reference for orientation"

    # LayerOutput.kt
    Create src/main/kotlin/geo/examples/LayerOutput.kt:
    - Demonstrate screenshot capture using OpenRNDR screenshot()
    - Use pattern: extendProgram {} { keyboard.keyDown.keys.add(Key.UP_LEFT_ARROW); screenshot(); }
    - Or simpler: in draw loop, check key press then trigger screenshot()
    - File naming: "layer-composition-${System.currentTimeMillis()}.png"
    - Document: "Screenshot preserves natural creative coding workflow - user captures when needed"
    - Reference: don't build separate batch infrastructure per CONTEXT.md
    - Use orx-compositor screenshot if available (check orx-compositor docs), else standard openrndr screenshot()
  </action>
  <verify>ls -la src/main/kotlin/geo/examples/LayerGraticule.kt src/main/kotlin/geo/examples/LayerOutput.kt && grep -r "screenshot\|generateGraticule" src/main/kotlin/geo/examples/LayerGraticule.kt src/main/kotlin/geo/examples/LayerOutput.kt</verify>
  <done>All 4 examples compile and demonstrate layer composition patterns</done>
</task>

</tasks>

<verification>
Phase 4 coverage verified:
- ✓ REND-05: GeoLayer wrapper + compose { } DSL enables layer stacking
- ✓ REND-06: Blend mode enum documented in LayerBlendModes.kt examples
- ✓ OUTP-01: Screenshot() shown in LayerOutput.kt
- ✓ REF-01: generateGraticule() in LayerGraticule.kt example

Compile verification:
- All examples use orx-compositor compose { } DSL
- Examples compile with openrndr-kotlin application pattern
- Reference existing geo.render functions for drawing

No new infrastructure: reuses orx-compositor, no custom layer management
</verification>

<success_criteria>
Four examples compile and run:
- LayerComposition.kt demonstrates basic multi-layer composition
- LayerBlendModes.kt shows each blend mode (Add, Multiply, Overlay, Screen)
- LayerGraticule.kt shows graticule generation and usage
- LayerOutput.kt demonstrates screenshot capture workflow

GeoLayer provides clean DSL:
- layer { source = ...; style = ...; blendMode = ... }
- Zero new infrastructure (uses orx-compositor)
- OpenCode's discretion: exact compositor DSL syntax, which blend modes to document

Documentation in examples covers:
- How to stack layers (order matters, last is top)
- Best blend modes for geo (Multiply, Overlay, Screen)
- Graticule usage for lat/lng reference
- Screenshot workflow (natural creative coding pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/04-layer-system/04-01-SUMMARY.md`
</output>