---
phase: 13-integration-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/test/kotlin/geo/regression/ExampleRegressionTest.kt
  - src/test/kotlin/geo/regression/ExampleRunner.kt
  - build.gradle.kts
autonomous: true
requirements:
  - PERF-10
must_haves:
  truths:
    - All 16 v1.2.0 examples compile and execute without errors
    - Examples produce the same visual output as v1.2.0 (no rendering differences)
    - No API changes break existing example code
    - Automated test harness can run all examples via Gradle
    - Memory usage remains bounded during extended example runs
  artifacts:
    - path: src/test/kotlin/geo/regression/ExampleRegressionTest.kt
      provides: JUnit test that runs all examples and validates they execute
      min_lines: 150
      exports:
        - ExampleRegressionTest
    - path: src/test/kotlin/geo/regression/ExampleRunner.kt
      provides: Test harness for executing example programs programmatically
      min_lines: 100
      exports:
        - ExampleRunner
        - runExample()
    - path: build.gradle.kts
      provides: Gradle configuration for example testing (test task)
      exports: []
  key_links:
    - from: ExampleRegressionTest
      to: ExampleRunner
      via: "ExampleRunner.runExample(exampleClass)"
      pattern: "ExampleRunner\\.runExample"
    - from: ExampleRegressionTest
      to: examples/
      via: "Discovers example classes from examples/ directory"
      pattern: "examples\\..*Example"
    - from: ExampleRunner
      to: examples/
      via: "Loads and executes example main methods"
      pattern: "Class\\.forName.*examples"
---

<objective>
Create automated regression tests that verify all 16 v1.2.0 examples continue to work unchanged with the Phase 11-12 optimizations.

Purpose: Ensure backward compatibility and catch any breaking changes before release.
Output: ExampleRegressionTest that automatically discovers, runs, and validates all examples.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-integration-validation/13-CONTEXT.md
@.planning/phases/11-batch-projection/11-02-SUMMARY.md
@.planning/phases/12-viewport-caching/12-03-SUMMARY.md

# Core requirement: All 16 examples work unchanged
# This validates PERF-07 (transparent to existing code) and PERF-10

# Example directory structure to scan
examples/anim/
examples/core/
examples/data/
examples/layer/
examples/proj/
examples/render/

# Current GeoStack with viewport caching
@src/main/kotlin/geo/GeoStack.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExampleRunner test harness</name>
  <files>src/test/kotlin/geo/regression/ExampleRunner.kt</files>
  <action>
    Create a test harness that can programmatically run examples:
    
    1. Discover all example classes in the examples/ package
    2. Load and execute example main() methods
    3. Support headless/test mode (don't block on window)
    4. Handle timeouts (examples shouldn't run forever in tests)
    5. Capture and report:
       - Compilation status
       - Execution success/failure
       - Any exceptions thrown
       - Execution time
    
    Key methods:
    - discoverExamples(): List<String> - finds all example class names
    - runExample(className: String, timeoutMs: Long = 5000): ExampleResult
    - runAllExamples(): List<ExampleResult>
    
    ExampleResult data class should contain:
    - className: String
    - success: Boolean
    - durationMs: Long
    - error: Throwable?
    
    Handle OPENRNDR's application lifecycle:
    - OPENRNDR programs extend Program and use application {}
    - In test mode, need to mock or skip actual window creation
    - Focus on verifying the geo library integration works
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.regression.ExampleRunnerTest" -x</automated>
    <manual>Verify ExampleRunner can discover and attempt to run examples</manual>
  </verify>
  <done>
    - ExampleRunner class exists with discoverExamples() and runExample() methods
    - Can discover all example classes from examples/ package
    - Can execute example code programmatically
    - Handles timeouts and exceptions gracefully
    - Returns ExampleResult with success/failure status
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ExampleRegressionTest with all examples</name>
  <files>src/test/kotlin/geo/regression/ExampleRegressionTest.kt</files>
  <action>
    Create the main regression test class:
    
    1. Discover and catalog all 16 (or however many exist) v1.2.0 examples
    2. For each example:
       - Verify it compiles (class loads successfully)
       - Run it in test mode (headless/short timeout)
       - Assert no exceptions thrown
       - Assert expected initialization completes
    
    3. Organize tests by example category:
       - Animation examples (examples/anim/)
       - Core examples (examples/core/)
       - Data loading examples (examples/data/)
       - Layer examples (examples/layer/)
       - Projection examples (examples/proj/)
       - Rendering examples (examples/render/)
    
    4. Test structure:
       ```kotlin
       @Test
       fun `example DataLoading01 loads successfully`() {
           val result = ExampleRunner.runExample("examples.data.DataLoading01")
           assertTrue(result.success, "Example failed: ${result.error}")
       }
       ```
    
    5. Include memory monitoring:
       - Track heap usage before/after running examples
       - Assert no unbounded memory growth
       - Memory should return to baseline after GC
    
    6. Run all examples in sequence to simulate extended session
    
    Run via: `./gradlew test --tests "geo.regression.ExampleRegressionTest"`
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.regression.ExampleRegressionTest" -x</automated>
    <manual>Verify all example tests pass (or identify specific failures)</manual>
  </verify>
  <done>
    - ExampleRegressionTest runs all discovered examples
    - Each example has an individual test method
    - All examples load and execute without errors
    - Memory usage remains bounded during test run
    - Test completes within reasonable time (< 5 minutes for all examples)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Gradle task for full regression suite</name>
  <files>build.gradle.kts</files>
  <action>
    Update build.gradle.kts to support regression testing:
    
    1. Add a dedicated test task for regression tests:
       ```kotlin
       tasks.register<Test>("regressionTest") {
           description = "Runs regression tests for all examples"
           group = "verification"
           testClassesDirs = sourceSets["test"].output.classesDirs
           classpath = sourceSets["test"].runtimeClasspath
           filter {
               includeTestsMatching("geo.regression.*")
           }
           testLogging {
               events("passed", "skipped", "failed")
               showStandardStreams = true
           }
           // Longer timeout for running actual examples
           timeout = Duration.ofMinutes(10)
       }
       ```
    
    2. Ensure test dependencies include what's needed for examples:
       - OPENRNDR dependencies (already present)
       - JUnit 5 (already present)
       
    3. Add regression test to CI/check task:
       ```kotlin
       tasks.named("check") {
           dependsOn("regressionTest")
       }
       ```
    
    4. Document usage in comments:
       - `./gradlew regressionTest` - run only regression tests
       - `./gradlew test` - run all tests including regression
       - `./gradlew test --tests "geo.regression.ExampleRegressionTest"` - run specific test
  </action>
  <verify>
    <automated>./gradlew regressionTest --dry-run</automated>
    <manual>Verify gradle task is registered and runnable</manual>
  </verify>
  <done>
    - Gradle task 'regressionTest' is registered and runnable
    - Task runs all geo.regression.* tests
    - Task is included in 'check' lifecycle
    - Task configuration has appropriate timeout (10 min)
    - Running `./gradlew regressionTest` executes without errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run: `./gradlew regressionTest` - all example tests pass
2. Verify: All 16 (or discovered count) examples are tested
3. Check: No compilation errors in any example
4. Confirm: Memory usage remains bounded throughout test run
5. Run: `./gradlew check` - includes regression tests
</verification>

<success_criteria>
- ExampleRunner can discover and execute all example classes
- ExampleRegressionTest validates all examples load and run without errors
- All v1.2.0 examples compile and execute unchanged (PERF-10 satisfied)
- No breaking API changes introduced by Phase 11-12 work
- Memory usage remains bounded during extended example runs
- Gradle regressionTest task available for CI/CD integration
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration-validation/13-02-SUMMARY.md`
</output>
