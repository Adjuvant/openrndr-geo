---
phase: 13-integration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/kotlin/geo/performance/PerformanceBenchmark.kt
  - src/test/kotlin/geo/performance/SyntheticDataGenerator.kt
  - src/test/kotlin/geo/performance/BaselineSimulator.kt
autonomous: true
requirements:
  - PERF-08
  - PERF-09
must_haves:
  truths:
    - Performance benchmark compares v1.2.0 baseline vs. current implementation
    - Benchmark tests with 10k, 50k, 100k, 250k feature datasets
    - Static camera scenarios show 8x-15x improvement (target: 10x+)
    - Pan operations show measurable improvement (2x+) from batch projection
    - Memory usage remains bounded during extended benchmark runs
    - Results are reported in a format easy to compare across runs
  artifacts:
    - path: src/test/kotlin/geo/performance/PerformanceBenchmark.kt
      provides: Main benchmark test class with timing-based measurements
      min_lines: 200
      exports:
        - PerformanceBenchmark
    - path: src/test/kotlin/geo/performance/SyntheticDataGenerator.kt
      provides: Synthetic test data generation (10k, 50k, 100k, 250k features)
      min_lines: 100
      exports:
        - SyntheticDataGenerator
        - createPointDataset
        - createLineStringDataset
        - createPolygonDataset
    - path: src/test/kotlin/geo/performance/BaselineSimulator.kt
      provides: Simulates v1.2.0 per-point projection behavior for comparison
      min_lines: 80
      exports:
        - BaselineSimulator
  key_links:
    - from: PerformanceBenchmark
      to: SyntheticDataGenerator
      via: "SyntheticDataGenerator.createXxxDataset()"
      pattern: "SyntheticDataGenerator\\.(createPoint|createLineString|createPolygon)"
    - from: PerformanceBenchmark
      to: BaselineSimulator
      via: "BaselineSimulator.perPointProjection()"
      pattern: "BaselineSimulator\\.perPointProjection"
    - from: PerformanceBenchmark
      to: src/main/kotlin/geo/GeoStack.kt
      via: "GeoStack.render() with optimization"
      pattern: "GeoStack.*render"
---

<objective>
Create comprehensive performance benchmarks that validate the optimization work from Phases 11-12 achieves the 10x+ improvement target for static camera scenarios.

Purpose: Provide objective, reproducible measurements proving the performance improvements work as designed.
Output: PerformanceBenchmark test class with synthetic data generators and baseline simulator.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-integration-validation/13-CONTEXT.md
@.planning/phases/11-batch-projection/11-02-SUMMARY.md
@.planning/phases/12-viewport-caching/12-03-SUMMARY.md

# Existing performance infrastructure to leverage
@src/test/kotlin/geo/performance/BatchProjectionBenchmark.kt

# Core classes to benchmark
@src/main/kotlin/geo/GeoStack.kt
@src/main/kotlin/geo/GeoJSON.kt
@src/main/kotlin/geo/GeoSource.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SyntheticDataGenerator for benchmark datasets</name>
  <files>src/test/kotlin/geo/performance/SyntheticDataGenerator.kt</files>
  <action>
    Create a utility class that generates synthetic geographic datasets for benchmarking:
    
    1. Generate datasets at specified sizes: 10k, 50k, 100k, 250k features
    2. Support multiple geometry types: Points, LineStrings, Polygons
    3. Create realistic spatial distributions (avoid clustering at single point)
    4. Generate within a reasonable bounding box (e.g., world bounds: -180 to 180, -90 to 90)
    5. Provide factory methods:
       - createPointDataset(count: Int): GeoSource
       - createLineStringDataset(count: Int, pointsPerLine: Int = 10): GeoSource  
       - createPolygonDataset(count: Int, pointsPerPolygon: Int = 20): GeoSource
    
    Use consistent random seed for reproducible benchmarks.
    Generated data should approximate real-world feature complexity.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.performance.SyntheticDataGeneratorTest" -x</automated>
    <manual>Verify generated datasets have correct feature counts</manual>
  </verify>
  <done>
    - SyntheticDataGenerator class exists with 3 dataset generation methods
    - Can generate datasets of exactly 10k, 50k, 100k, 250k features
    - Generated data is spatially distributed (not all at same coordinates)
    - Unit tests verify correct feature counts for each dataset type
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BaselineSimulator for v1.2.0 comparison</name>
  <files>src/test/kotlin/geo/performance/BaselineSimulator.kt</files>
  <action>
    Create a simulator that mimics v1.2.0 behavior (before optimization):
    
    1. Simulate per-point coordinate transformation (no batch optimization)
    2. No viewport caching (re-project every frame)
    3. Render loop that transforms coordinates on every call
    4. Method: perPointProjection(source: GeoSource, viewport: Viewport): Duration
    
    This simulates the old code path for fair comparison with optimized version.
    Use the same coordinate transformation math as current implementation,
    just applied per-point instead of batch + cache.
    
    The baseline represents: Phase 11 + Phase 12 optimizations = NOT applied
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.performance.BaselineSimulatorTest" -x</automated>
    <manual>Verify baseline produces same visual output as before (correctness)</manual>
  </verify>
  <done>
    - BaselineSimulator class exists with perPointProjection() method
    - Simulates pre-optimization rendering (no batch, no cache)
    - Produces correct geographic results (same as optimized, just slower)
    - Unit test verifies it produces same output as optimized path
  </done>
</task>

<task type="auto">
  <name>Task 3: Create PerformanceBenchmark with timing measurements</name>
  <files>src/test/kotlin/geo/performance/PerformanceBenchmark.kt</files>
  <action>
    Create the main benchmark test class that compares performance:
    
    1. Test scenarios:
       - Static camera with Point datasets (10k, 50k, 100k, 250k)
       - Static camera with LineString datasets
       - Static camera with Polygon datasets
       - Pan operations (simulate camera movement)
    
    2. Measurement approach (simple timing, not JMH):
       - Warmup: 10 iterations to stabilize JVM
       - Measurement: 50 iterations per scenario
       - Use System.nanoTime() for high-resolution timing
       - Report: mean, median, min, max, std dev
    
    3. Compare three modes:
       - v1.2.0 Baseline (per-point, no cache)
       - Phase 11 only (batch projection, no cache)
       - Phase 12 (batch + viewport cache) - CURRENT
    
    4. Output format (console + file):
       - Table format with columns: Dataset, Features, Baseline, Optimized, Speedup
       - Speedup ratio: baseline_time / optimized_time
       - Mark pass/fail based on thresholds:
         * Static camera: PASS if ≥8x (target 10x)
         * Pan operations: PASS if ≥2x improvement
    
    5. File output: build/reports/performance-benchmark.txt
    
    Run via: ./gradlew test --tests "geo.performance.PerformanceBenchmark"
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.performance.PerformanceBenchmark" -x</automated>
    <manual>Verify benchmark report shows speedup ratios for all scenarios</manual>
  </verify>
  <done>
    - PerformanceBenchmark class runs all benchmark scenarios
    - Measures 10k, 50k, 100k, 250k feature datasets
    - Reports speedup ratios for static camera (target: 10x+)
    - Reports speedup ratios for pan operations (target: 2x+)
    - Outputs results to console and build/reports/performance-benchmark.txt
    - All scenarios complete without errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run: `./gradlew test --tests "geo.performance.*"` - all tests pass
2. Check: `cat build/reports/performance-benchmark.txt` exists with results
3. Verify: Benchmark shows measurable speedup (baseline vs. optimized)
4. Confirm: Results include all 4 dataset sizes (10k, 50k, 100k, 250k)
</verification>

<success_criteria>
- SyntheticDataGenerator creates reproducible test datasets at all specified sizes
- BaselineSimulator accurately simulates v1.2.0 behavior for fair comparison
- PerformanceBenchmark measures and reports speedup ratios
- Static camera scenarios show 8x-15x improvement (target: 10x+)
- Pan operations show measurable improvement from batch projection
- Results output to both console and file for documentation
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration-validation/13-01-SUMMARY.md`
</output>
