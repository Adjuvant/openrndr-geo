---
phase: 20-layer-features
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/layer/Graticule.kt
  - src/main/kotlin/geo/core/Feature.kt
autonomous: true
requirements:
  - LAYER-01
must_haves:
  truths:
    - "Graticule generates LineString features, not Points"
    - "Lines are organized as MultiLineString with latLines and lngLines"
    - "Spacing adapts to viewport: 1°, 10°, 30°, 90° based on visible bounds"
    - "1° minimum floor prevents clutter when very zoomed"
    - "Lines crossing antimeridian are split at ±180°"
    - "Lines are clipped to viewport bounds"
  artifacts:
    - path: src/main/kotlin/geo/layer/Graticule.kt
      provides: LineString-based graticule generation
      min_lines: 150
    - path: src/test/kotlin/geo/layer/GraticuleSpacingTest.kt
      provides: Power-of-10 spacing unit tests
    - path: src/test/kotlin/geo/layer/GraticuleLineGenerationTest.kt
      provides: Line coordinate generation tests
    - path: src/test/kotlin/geo/layer/GraticuleAntimeridianTest.kt
      provides: Antimeridian splitting tests
  key_links:
    - from: src/main/kotlin/geo/layer/Graticule.kt
      to: src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt
      via: splitAtAntimeridian() import and call
      pattern: import.*splitAtAntimeridian
---

<objective>
Implement core graticule line generation with LineString features and adaptive spacing.

Purpose: Replace Point-based graticule with proper LineString-based grid lines that adapt to zoom level and handle antimeridian correctly.
Output: Modified Graticule.kt with generateGraticuleLayer() function and supporting tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-layer-features/20-CONTEXT.md
@.planning/phases/20-layer-features/20-RESEARCH.md
@src/main/kotlin/geo/layer/Graticule.kt (current Point-based implementation)
@src/main/kotlin/geo/layer/GeoLayer.kt (API pattern to follow)
@src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt (splitAtAntimeridian for reuse)
@src/main/kotlin/geo/core/Feature.kt (Feature, Point, LineString types)
</context>

<feature>
  <name>LineString-based Graticule Generation</name>
  <files>
    <source>src/main/kotlin/geo/layer/Graticule.kt</source>
    <test>src/test/kotlin/geo/layer/GraticuleSpacingTest.kt</test>
    <test>src/test/kotlin/geo/layer/GraticuleLineGenerationTest.kt</test>
    <test>src/test/kotlin/geo/layer/GraticuleAntimeridianTest.kt</test>
  </files>
  <behavior>
    - Test: generateGraticuleLayer(bounds, spacing=auto) returns GeoLayer with latLines/lngLines
    - Test: Adaptive spacing selects 1° for bounds < 2°, 10° for < 20°, 30° for < 60°, 90° otherwise
    - Test: Minimum 1° floor even when very zoomed in
    - Test: Latitude lines are horizontal LineStrings across bounds.width
    - Test: Longitude lines are vertical LineStrings across bounds.height
    - Test: Lines crossing antimeridian (bounds spanning ±180°) are split
    - Test: Lines are generated only within viewport bounds (clipped)
    - Test: Returns MultiLineString for latLines and lngLines via GeoSource
  </behavior>
  <implementation>
    1. Add data class GraticuleLines with latLines/lngLines GeoSource properties to Graticule.kt
    2. Implement adaptive spacing: calculate visibleDegrees from bounds, select power-of-10
    3. Generate latitude lines: for each lat in range(minLat, maxLat, spacing) create LineString
    4. Generate longitude lines: for each lon in range(minLon, maxLon, spacing) create LineString
    5. Call splitAtAntimeridian() on longitude lines that cross ±180°
    6. Wrap in GeoSource with MultiLineString geometry
    7. Create generateGraticuleLayer() function returning GeoLayer with latLines/lngLines
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Create Wave 0 test scaffolds</name>
  <files>
    - src/test/kotlin/geo/layer/GraticuleSpacingTest.kt
    - src/test/kotlin/geo/layer/GraticuleLineGenerationTest.kt
    - src/test/kotlin/geo/layer/GraticuleAntimeridianTest.kt
  </files>
  <action>
    Create three JUnit 5 test files with empty test functions for:
    1. GraticuleSpacingTest.kt - test adaptive spacing logic (power-of-10 selection)
    2. GraticuleLineGenerationTest.kt - test line coordinate generation
    3. GraticuleAntimeridianTest.kt - test antimeridian splitting

    Each test file should have basic structure:
    - Package: geo.layer
    - Import: org.junit.jupiter.api.Test, org.junit.jupiter.api.Assertions.*
    - Import: geo.core.Bounds, geo.layer (the functions under test)
    - Empty test functions that will fail compilation until implemented
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.GraticuleSpacingTest" --tests "geo.layer.GraticuleLineGenerationTest" --tests "geo.layer.GraticuleAntimeridianTest" 2>&1 | head -50</automated>
  </verify>
  <done>Three test files exist with empty test methods that compile (fail at runtime until implemented)</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Implement adaptive spacing logic</name>
  <files>
    - src/main/kotlin/geo/layer/Graticule.kt
  </files>
  <action>
    Add spacing selection logic to Graticule.kt:
    
    1. Add function calculateAdaptiveSpacing(bounds: Bounds): Double
       - Calculate visibleDegrees = max(bounds.maxX - bounds.minX, bounds.maxY - bounds.minY)
       - Return 1.0 if visibleDegrees < 2.0
       - Return 10.0 if visibleDegrees < 20.0
       - Return 30.0 if visibleDegrees < 60.0
       - Return 90.0 otherwise
       - Minimum floor: always return at least 1.0
    
    2. Write tests in GraticuleSpacingTest.kt for this logic before implementing
    
    3. Implement the function, verify tests pass
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.GraticuleSpacingTest" -q</automated>
  </verify>
  <done>calculateAdaptiveSpacing() returns correct power-of-10 values for different viewport sizes</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Implement LineString generation</name>
  <files>
    - src/main/kotlin/geo/layer/Graticule.kt
  </files>
  <action>
    Add LineString-based generation to Graticule.kt:
    
    1. Add data class GraticuleLayerData with latLines and lngLines GeoSource properties
    2. Add generateGraticuleLines(bounds: Bounds, spacing: Double): GraticuleLayerData
       - Generate latitude LineStrings: for lat in floor(minLat) to ceil(maxLat) by spacing
         - Each line: LineString(Point(bounds.minX, lat), Point(bounds.maxX, lat))
       - Generate longitude LineStrings: for lon in floor(minLon) to ceil(maxLon) by spacing
         - Each line: LineString(Point(lon, bounds.minY), Point(lon, bounds.maxY))
    3. Wrap in GeoSource returning MultiLineString for each set
    4. Write tests in GraticuleLineGenerationTest.kt before implementing
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.GraticuleLineGenerationTest" -q</automated>
  </verify>
  <done>generateGraticuleLines() produces correct lat/lng LineStrings within bounds</done>
</task>

<task type="auto" tdd="true">
  <name>task 4: Add antimeridian handling</name>
  <files>
    - src/main/kotlin/geo/layer/Graticule.kt
  </files>
  <action>
    Add antimeridian splitting to longitude line generation:
    
    1. Import splitAtAntimeridian from geo.render.geometry.AntimeridianSplitter
    2. For each longitude line that crosses ±180°:
       - Call splitAtAntimeridian() to get list of non-crossing segments
       - Convert each segment back to LineString
    3. Update generateGraticuleLines() to apply splitting
    4. Write tests in GraticuleAntimeridianTest.kt for:
       - Bounds spanning ±180° produce split lines
       - Lines completely on one side are not split
       - Split lines are closed (start == end after interpolation)
    5. Use existing AntimeridianSplitterTest as reference for expected behavior
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.GraticuleAntimeridianTest" -q</automated>
  </verify>
  <done>Longitude lines crossing antimeridian are correctly split at ±180°</done>
</task>

<task type="auto" tdd="true">
  <name>task 5: Create GeoLayer API</name>
  <files>
    - src/main/kotlin/geo/layer/Graticule.kt
    - src/main/kotlin/geo/layer/GeoLayer.kt
  </files>
  <action>
    Finalize the GeoLayer API:
    
    1. Add latLines and lngLines properties to GeoLayer data class
    2. Create generateGraticuleLayer(bounds: Bounds, projection: Projection, includeLabels: Boolean = false): GeoLayer
       - Calls generateGraticuleLines() internally
       - Wraps results in GeoLayer with latLines/lngLines
    3. Keep generateGraticuleSource() for backward compatibility (deprecated, returns combined source)
    4. Update KDoc with new API usage examples
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.Graticule*" -q && ./gradlew compileKotlin -q</automated>
  </verify>
  <done>generateGraticuleLayer() returns GeoLayer with latLines/lngLines, backward-compatible API maintained</done>
</task>

</tasks>

<verification>
- All new tests pass: `./gradlew test --tests "geo.layer.Graticule*" -q`
- Code compiles: `./gradlew compileKotlin -q`
- Existing graticule tests still pass (backward compatibility)
</verification>

<success_criteria>
- GeoLayer has latLines and lngLines GeoSource properties
- generateGraticuleLayer() returns correct LineString-based grid
- Adaptive spacing selects appropriate power-of-10 grid
- Antimeridian crossing lines are split correctly
- Backward compatibility maintained with generateGraticuleSource()
</success_criteria>

<output>
After completion, create `.planning/phases/20-layer-features/{phase}-01-SUMMARY.md`
</output>
