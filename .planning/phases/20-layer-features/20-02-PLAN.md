---
phase: 20-layer-features
plan: 02
type: tdd
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/main/kotlin/geo/layer/Graticule.kt
  - src/main/kotlin/geo/layer/GeoLayer.kt
autonomous: true
requirements:
  - LAYER-01
must_haves:
  truths:
    - "GraticuleLabels data class contains latitude and longitude label lists"
    - "Labels include text (e.g., '45°N', '120°W'), coordinates, and projected screen position"
    - "Latitude labels positioned at left edge of viewport"
    - "Longitude labels positioned at bottom edge of viewport"
    - "Degree formatting uses standard cartographic notation (N/S/E/W)"
    - "Labels off by default (includeLabels parameter)"
    - "Auto-thinning reduces label density when lines would be too dense"
  artifacts:
    - path: src/main/kotlin/geo/layer/Graticule.kt
      provides: Label generation with GraticuleLabels data class
      min_lines: 100
    - path: src/test/kotlin/geo/layer/GraticuleLabelPositionTest.kt
      provides: Label placement tests
    - path: src/test/kotlin/geo/layer/GraticuleLabelFormatTest.kt
      provides: Label formatting tests
    - path: src/test/kotlin/geo/layer/GraticuleDensityTest.kt
      provides: Auto-thinning density tests
  key_links:
    - from: src/main/kotlin/geo/layer/Graticule.kt
      to: geo.projection.Projection
      via: project() call for screen coordinate calculation
      pattern: projection\.project
---

<objective>
Implement graticule label generation including positioning, formatting, and density management.

Purpose: Add optional labels to graticule showing degree values (45°N, 120°W) with proper placement and formatting.
Output: Label generation in Graticule.kt with supporting tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-layer-features/20-CONTEXT.md
@.planning/phases/20-layer-features/20-RESEARCH.md
@.planning/phases/20-layer-features/20-01-PLAN.md
@src/main/kotlin/geo/layer/Graticule.kt (Plan 01 implementation)
@src/main/kotlin/geo/layer/GeoLayer.kt (GeoLayer with latLines/lngLines from Plan 01)
@src/main/kotlin/geo/projection/Projection.kt (projection interface for screen coordinates)
</context>

<feature>
  <name>Graticule Label Generation</name>
  <files>
    <source>src/main/kotlin/geo/layer/Graticule.kt</source>
    <test>src/test/kotlin/geo/layer/GraticuleLabelPositionTest.kt</test>
    <test>src/test/kotlin/geo/layer/GraticuleLabelFormatTest.kt</test>
    <test>src/test/kotlin/geo/layer/GraticuleDensityTest.kt</test>
  </files>
  <behavior>
    - Test: GraticuleLabels contains latitudeLabels and longitudeLabels lists
    - Test: LabelPosition has text, coordinate, and projected screen position
    - Test: Latitude labels at left edge (x = bounds.minX projected)
    - Test: Longitude labels at bottom edge (y = bounds.minY projected)
    - Test: "45°N" format for positive latitude, "30°S" for negative
    - Test: "120°W" format for negative longitude, "90°E" for positive, "0°" for near-zero
    - Test: includeLabels=false (default) produces null labels
    - Test: Auto-thinning increases spacing when projected label spacing < 20px
  </behavior>
  <implementation>
    1. Add GraticuleLabels and LabelPosition data classes to Graticule.kt
    2. Add generateGraticuleLabels() function with projection parameter
    3. Calculate projected screen positions using projection.project()
    4. Implement degree formatting with N/S/E/W suffixes
    5. Add auto-thinning: if projected spacing < 20px, skip every other label
    6. Update generateGraticuleLayer() to include labels when includeLabels=true
    7. Set labels = null by default
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Create Wave 0 label test scaffolds</name>
  <files>
    - src/test/kotlin/geo/layer/GraticuleLabelPositionTest.kt
    - src/test/kotlin/geo/layer/GraticuleLabelFormatTest.kt
    - src/test/kotlin/geo/layer/GraticuleDensityTest.kt
  </files>
  <action>
    Create three JUnit 5 test files with empty test functions for:
    1. GraticuleLabelPositionTest.kt - test label placement at viewport edges
    2. GraticuleLabelFormatTest.kt - test degree notation formatting (N/S/E/W)
    3. GraticuleDensityTest.kt - test auto-thinning when zoomed out
    
    Each test file should have:
    - Package: geo.layer
    - Import: org.junit.jupiter.api.Test, org.junit.jupiter.api.Assertions.*
    - Import: geo.core.Bounds, geo.projection.Projection, geo.layer.GraticuleLabels
    - Empty test functions with descriptive names
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.GraticuleLabelPositionTest" --tests "geo.layer.GraticuleLabelFormatTest" --tests "geo.layer.GraticuleDensityTest" 2>&1 | head -50</automated>
  </verify>
  <done>Three test files exist with empty test methods that compile</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Implement LabelPosition and GraticuleLabels data classes</name>
  <files>
    - src/main/kotlin/geo/layer/Graticule.kt
  </files>
  <action>
    Add label data structures:
    
    1. data class LabelPosition(
         text: String,           // "45°N"
         longitude: Double,      // geographic coordinate
         latitude: Double,       // geographic coordinate  
         projectedX: Double,     // screen position
         projectedY: Double     // screen position
       )
    
    2. data class GraticuleLabels(
         latitudeLabels: List<LabelPosition>,
         longitudeLabels: List<LabelPosition>
       )
    
    Write tests in GraticuleLabelPositionTest.kt verifying:
    - LabelPosition has all required properties
    - GraticuleLabels groups lat/lng labels separately
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.GraticuleLabelPositionTest" -q</automated>
  </verify>
  <done>LabelPosition and GraticuleLabels data classes exist with correct properties</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Implement degree formatting</name>
  <files>
    - src/main/kotlin/geo/layer/Graticule.kt
  </files>
  <action>
    Add degree formatting functions:
    
    1. formatLatitude(lat: Double): String
       - |lat| + "°N" if lat >= 0
       - |lat| + "°S" if lat < 0
       - Special case: |lat| < 0.001 -> "0°"
    
    2. formatLongitude(lon: Double): String
       - |lon| + "°E" if lon >= 0
       - |lon| + "°W" if lon < 0
       - Special case: |lon| < 0.001 or near 180 -> "0°" or "180°"
    
    Write tests in GraticuleLabelFormatTest.kt:
    - formatLatitude(45.0) == "45°N"
    - formatLatitude(-30.5) == "30.5°S"
    - formatLatitude(0.0) == "0°"
    - formatLongitude(120.0) == "120°E"
    - formatLongitude(-90.0) == "90°W"
    - formatLongitude(0.0) == "0°"
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.GraticuleLabelFormatTest" -q</automated>
  </verify>
  <done>Degree formatting produces correct N/S/E/W notation for all quadrants</done>
</task>

<task type="auto" tdd="true">
  <name>task 4: Implement label generation with projection</name>
  <files>
    - src/main/kotlin/geo/layer/Graticule.kt
  </files>
  <action>
    Add generateGraticuleLabels() function:
    
    1. generateGraticuleLabels(
         bounds: Bounds,
         projection: Projection,
         spacing: Double
       ): GraticuleLabels
    
    2. For latitude labels:
       - Generate labels at each latitude line
       - Position at left edge: Point(bounds.minX, lat) -> project() -> x, y
       - Store text, geographic coords, projected position
    
    3. For longitude labels:
       - Generate labels at each longitude line
       - Position at bottom edge: Point(lon, bounds.minY) -> project() -> x, y
       - Store text, geographic coords, projected position
    
    4. Use projection.project(Vector2(lon, lat)) to get screen coordinates
    
    Write tests using mock Projection or real MercatorProjection
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.GraticuleLabelPositionTest" -q</automated>
  </verify>
  <done>generateGraticuleLabels() produces correct label positions at viewport edges</done>
</task>

<task type="auto" tdd="true">
  <name>task 5: Implement auto-thinning and integrate with GeoLayer</name>
  <files>
    - src/main/kotlin/geo/layer/Graticule.kt
  </files>
  <action>
    Add auto-thinning and finalize integration:
    
    1. Add autoThinning parameter to generateGraticuleLabels() with default minPixelSpacing=20.0
    2. Calculate projected spacing between adjacent labels
    3. If projected spacing < minPixelSpacing, skip every other label
    4. Update generateGraticuleLayer() to accept includeLabels: Boolean = false
    5. When includeLabels=true, call generateGraticuleLabels() and attach to GeoLayer
    6. When includeLabels=false (default), labels = null
    
    Write tests in GraticuleDensityTest.kt:
    - Very zoomed out (many labels) should thin to ~20px spacing
    - Zoomed in (few labels) should show all labels
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.layer.GraticuleDensityTest" -q && ./gradlew test --tests "geo.layer.Graticule*" -q</automated>
  </verify>
  <done>Labels auto-thin at low zoom levels, default includeLabels=false maintains backward compatibility</done>
</task>

</tasks>

<verification>
- All label tests pass: `./gradlew test --tests "geo.layer.GraticuleLabel*" -q`
- All graticule tests pass: `./gradlew test --tests "geo.layer.Graticule*" -q`
- Code compiles: `./gradlew compileKotlin -q`
</verification>

<success_criteria>
- GraticuleLabels data class groups latitude and longitude labels
- LabelPosition contains text, coordinates, and projected screen position
- Degree formatting correct: N/S for latitude, E/W for longitude
- Labels positioned at left edge (lat) and bottom edge (lng) of viewport
- Auto-thinning maintains ~20px minimum spacing between labels
- includeLabels=false (default) produces null labels for backward compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/20-layer-features/{phase}-02-SUMMARY.md`
</output>
