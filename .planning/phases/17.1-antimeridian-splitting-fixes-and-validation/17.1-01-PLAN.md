---
phase: 17.1-antimeridian-splitting-fixes-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt
  - src/main/kotlin/geo/render/geometry/GeometryNormalizer.kt
  - src/main/kotlin/geo/render/DrawerGeoExtensions.kt
  - src/test/kotlin/geo/render/geometry/OptimizedGeometryNormalizerTest.kt
autonomous: false
requirements:
  - PERF-11

must_haves:
  truths:
    - "OptimizedGeoSource rendering splits antimeridian-crossing polygons correctly"
    - "GeometryNormalizer handles antimeridian splits for optimized geometries"
    - "Unit tests cover antimeridian scenarios in optimized path"
  artifacts:
    - path: "src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt"
      provides: "splitAtAntimeridian utility"
      contains: "splitAtAntimeridian(ring: List<Vector2>)"
    - path: "src/main/kotlin/geo/render/geometry/GeometryNormalizer.kt"
      provides: "normalize polygon handling with antimeridian"
      contains: "normalizePolygon(polygon: Polygon)"
    - path: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      provides: "OptimizedGeoSource render path"
      contains: "is OptimizedGeoSource ->"
    - path: "src/test/kotlin/geo/render/geometry/OptimizedGeometryNormalizerTest.kt"
      provides: "antimeridian splitting tests for optimized path"
  key_links:
    - from: "src/main/kotlin/geo/render/geometry/GeometryNormalizer.kt"
      to: "src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt"
      via: "splitAtAntimeridian call"
      pattern: "splitAtAntimeridian"
    - from: "src/main/kotlin/geo/render/DrawerGeoExtensions.kt"
      to: "src/main/kotlin/geo/render/geometry/GeometryNormalizer.kt"
      via: "normalization in optimized branch"
      pattern: "normalizePolygon"
---

<objective>
Fix antimeridian splitting logic and validate behavior in the optimized geometry rendering path.

Purpose: Ensure polygons crossing the antimeridian are correctly split and rendered when using OptimizedGeoSource with ViewportCache.

Output: Updated splitting and normalization utilities, integration in optimized path, and new unit tests covering antimeridian scenarios.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt
@src/main/kotlin/geo/render/geometry/GeometryNormalizer.kt
@src/main/kotlin/geo/render/DrawerGeoExtensions.kt
</context>

<interfaces>
From AntimeridianSplitter.kt:
```kotlin
internal fun splitAtAntimeridian(ring: List<Vector2>): List<List<Vector2>>
```
From GeometryNormalizer.kt:
```kotlin
fun normalizePolygon(polygon: Polygon): List<Polygon>
```
From DrawerGeoExtensions.kt (optimized branch):
```kotlin
when (source is OptimizedGeoSource) {
  // Bypasses cache — needs normalization hook
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: scaffold antimeridian tests for optimized path</name>
  <files>src/test/kotlin/geo/render/geometry/OptimizedGeometryNormalizerTest.kt</files>
  <behavior>
    - Test 1: `splitAtAntimeridian` splits a ring crossing 180° once into two fragments
    - Test 2: `splitAtAntimeridian` handles rings crossing multiple times for optimized geometries
    - Test 3: Normalized polygon list contains correct winding for each fragment
  </behavior>
  <action>
    Create `OptimizedGeometryNormalizerTest.kt` with JUnit tests importing `splitAtAntimeridian` and `normalizePolygon`.
    Write failing test cases for antimeridian crossing scenarios in optimized rendering path.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.render.geometry.OptimizedGeometryNormalizerTest"</automated>
  </verify>
  <done>Test scaffold file exists and tests run (fail) indicating missing implementation</done>
</task>

<task type="auto">
  <name>task 2: implement antimeridian fixes in normalization and renderer</name>
  <files>
    src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt,
    src/main/kotlin/geo/render/geometry/GeometryNormalizer.kt,
    src/main/kotlin/geo/render/DrawerGeoExtensions.kt
  </files>
  <action>
    - Update `splitAtAntimeridian` interpolation thresholds to handle longitude wrap in optimized path.
    - Ensure `normalizePolygon` applies splitting for any polygon from OptimizedGeoSource.
    - In `DrawerGeoExtensions.kt`, route optimized geometry branch through `normalizePolygon` before cache lookup.
    - Avoid regressions: preserve existing behavior for non-crossing and standard geometries.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.render.geometry.OptimizedGeometryNormalizerTest"</automated>
  </verify>
  <done>New tests pass, no regressions in existing antimeridian tests</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Antimeridian splitting fixes for optimized rendering path</what-built>
  <how-to-verify>
    1. Run the demo with a polygon crossing the 180° meridian using optimized loading (`loadGeo(optimize=true)`).
    2. Observe that the shape is split and rendered without seams or artifacts.
    3. Confirm caching still works (panning the view does not recompute splits unexpectedly).
  </how-to-verify>
  <resume-signal>approved or report issues</resume-signal>
</task>

</tasks>

<verification>
Automated tests and manual rendering check ensure correctness across optimized and standard paths.
</verification>

<success_criteria>
- All `OptimizedGeometryNormalizerTest` tests pass.
- Demo polygon crossing the antimeridian renders seamlessly in optimized mode.
- Caching remains effective with no performance regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-antimeridian-splitting-fixes-and-validation/17.1-01-SUMMARY.md`
</output>
