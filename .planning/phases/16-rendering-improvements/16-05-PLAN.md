---
phase: 16-rendering-improvements
plan: 16-05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/GeoJSON.kt
autonomous: true
requirements: [RENDER-01]
gap_closure: true
must_haves:
  truths:
    - "No ProjectionOverflowException when parsing and rendering polar geometries"
  artifacts:
    - path: "src/main/kotlin/geo/GeoJSON.kt"
      provides: "Geometry.clampToMercator() invocation in parseFeature"
  key_links:
    - from: "src/main/kotlin/geo/GeoJSON.kt"
      to: "geo.render.MultiRenderer pipeline"
      via: "parseFeature → clampToMercator → projection"
---

<objective>
Ensure latitude values are clamped to valid Web Mercator range at parse-time to prevent ProjectionOverflowException when rendering global datasets.

Purpose: Prevent application crashes when input coordinates at ±90° are projected to Mercator.
Output: GeoJSON parsing pipeline applies Geometry.clampToMercator() to all geometries.
</objective>

<context>
@.planning/phases/16-rendering-improvements/16-04-UAT.md
/src/main/kotlin/geo/GeoJSON.kt
/src/main/kotlin/geo/Geometry.kt
</context>

<tasks>

<task type="auto">
  <name>task: apply latitude clamping in GeoJSON parser</name>
  <files>src/main/kotlin/geo/GeoJSON.kt</files>
  <action>
    In GeoJSON.parseFeature, modify the geometry assignment:
    - Change `val geometry = parseGeometry(...)` to `var geometry = parseGeometry(...)`
    - Immediately after, add `geometry = geometry.clampToMercator()` to clamp latitudes.
    - Alternatively, inline: `val geometry = parseGeometry(...).clampToMercator()`.
  </action>
  <verify>
    <automated>./gradlew test --tests geo.render.MultiRendererTest</automated>
  </verify>
  <done>ProjectionOverflowException no longer occurs; MultiRendererTest passes all clamping-related tests.</done>
</task>

</tasks>

<verification>
Rerun full render tests: `./gradlew test --tests geo.render.MultiRendererTest`.
</verification>

<success_criteria>
- No ProjectionOverflowException when rendering data at ±90°
- geo.render.MultiRendererTest passes clamping scenarios
</success_criteria>

<output>
After gap closure, create `.planning/phases/16-rendering-improvements/16-05-SUMMARY.md`
</output>