---
phase: 16-rendering-improvements
plan: 16-07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt
autonomous: true
requirements: [RENDER-01]
gap_closure: true
must_haves:
  truths:
    - "Polygons crossing the ±180° meridian are split into correct fragments"
  artifacts:
    - path: "src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt"
      provides: "splitRings algorithm with interpolation at ±180°"
  key_links:
    - from: "src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt"
      to: "GeoJSON.parsePolygon normalization"
      via: "normalizePolygon → splitRings"
---

<objective>
Implement robust antimeridian splitting algorithm for vector geometries to prevent world-spanning artifacts when loading GeoJSON data.

Purpose: Ensure polygons crossing the ±180° meridian are split into correct ring fragments at load time, matching QGIS behavior.
Output: AntimeridianSplitter detects edges with |Δlon|>180, interpolates boundary points at ±180, and produces separate polygon fragments assigned correctly to exteriors and holes.
</objective>

<context>
@.planning/phases/16-rendering-improvements/16-04-UAT.md
/src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt
</context>

<tasks>

<task type="auto">
  <name>task: enhance antimeridian splitting in geometry normalizer</name>
  <files>src/main/kotlin/geo/render/geometry/AntimeridianSplitter.kt</files>
  <action>
    Update AntimeridianSplitter.splitRings to:
    - Detect edges where absolute difference in longitude > 180°.
    - For each crossing edge, interpolate two points at ±180° meridian intersection.
    - Divide the ring sequence into segments on either side of the antimeridian.
    - Assign segments to new exterior polygons or holes, preserving original winding.
    - Ensure geometries exactly at ±180° are left unchanged.
  </action>
  <verify>
    <automated>./gradlew test --tests geo.render.geometry.AntimeridianSplitterTest</automated>
  </verify>
  <done>AntimeridianSplitterTest covers test cases for ocean.geojson and passes without world-spanning artifacts.</done>
</task>

</tasks>

<verification>
Re-run geometry normalization tests: `./gradlew test --tests geo.render.geometry.AntimeridianSplitterTest`.
</verification>

<success_criteria>
- No world-spanning line artifacts when loading GeoJSON crossing the antimeridian
- geo.render.geometry.AntimeridianSplitterTest passes all normalization scenarios
</success_criteria>

<output>
After gap closure, create `.planning/phases/16-rendering-improvements/16-07-SUMMARY.md`
</output>