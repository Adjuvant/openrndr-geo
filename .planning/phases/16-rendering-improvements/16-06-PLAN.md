---
phase: 16-rendering-improvements
plan: 16-06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/geo/render/PolygonRenderer.kt
autonomous: true
requirements: [RENDER-02]
gap_closure: true
must_haves:
  truths:
    - "Polygons with holes render as transparent cutouts after winding normalization"
  artifacts:
    - path: "src/main/kotlin/geo/render/PolygonRenderer.kt"
      provides: "writePolygonWithHoles() with exterior.clockwise() and interiors.counterClockwise()"
  key_links:
    - from: "src/main/kotlin/geo/render/PolygonRenderer.kt"
      to: "compound { difference {} }"
      via: "normalized contours"
---

<objective>
Enforce correct winding order for polygon exterior and interior contours in writePolygonWithHoles() to ensure transparent cutouts render correctly.

Purpose: Normalize contour winding before performing compound difference so holes appear as cutouts.
Output: writePolygonWithHoles() applies `.clockwise()` to exterior and `.counterClockwise()` to each interior ring before computing the shape.
</objective>

<context>
@.planning/phases/16-rendering-improvements/16-04-UAT.md
/src/main/kotlin/geo/render/PolygonRenderer.kt
</context>

<tasks>

<task type="auto">
  <name>task: add winding normalization in writePolygonWithHoles</name>
  <files>src/main/kotlin/geo/render/PolygonRenderer.kt</files>
  <action>
    In the writePolygonWithHoles function:
    - Before invoking `compound { difference {} }`, convert the exterior contour to `.clockwise()`.
    - For each interior contour, convert to `.counterClockwise()`.
    - Ensure the contours passed to the compound block use these normalized shapes.
  </action>
  <verify>
    <automated>./gradlew test --tests geo.render.PolygonRendererTest</automated>
  </verify>
  <done>PolygonRendererTest passes all hole-rendering tests for single, multi, and overlapping holes.</done>
</task>

</tasks>

<verification>
Re-run polygon rendering tests: `./gradlew test --tests geo.render.PolygonRendererTest`.
</verification>

<success_criteria>
- Holes render as transparent cutouts in polygons with holes
- geo.render.PolygonRendererTest covers tests 2,3,4,7 and passes
</success_criteria>

<output>
After gap closure, create `.planning/phases/16-rendering-improvements/16-06-SUMMARY.md`
</output>