---
phase: 16-rendering-improvements
plan: 16-08
type: execute
wave: 2
depends_on: [16-06]
files_modified:
  - src/main/kotlin/geo/internal/cache/ViewportCache.kt
autonomous: true
requirements: [RENDER-02]
gap_closure: true
must_haves:
  truths:
    - "Viewport cache retains interior rings and renders holes correctly"
  artifacts:
    - path: "src/main/kotlin/geo/internal/cache/ViewportCache.kt"
      provides: "projectGeometryToArray includes interior arrays; renderProjectedCoordinates uses writePolygonWithHoles()"
  key_links:
    - from: "src/main/kotlin/geo/internal/cache/ViewportCache.kt"
      to: "PolygonRenderer.writePolygonWithHoles"
      via: "cached render path"
---

<objective>
Preserve and render interior rings (holes) when caching viewport-projected geometries to maintain transparent cutouts in cached render paths.

Purpose: Ensure ViewportCache retains hole definitions during caching and invokes writePolygonWithHoles() for correct hole rendering in cached scenarios.
Output: ViewportCache.projectGeometryToArray includes interiors, and renderProjectedCoordinates uses writePolygonWithHoles() for polygons with holes.
</objective>

<context>
@.planning/phases/16-rendering-improvements/16-04-UAT.md
/src/main/kotlin/geo/internal/cache/ViewportCache.kt
</context>

<tasks>

<task type="auto">
  <name>task: include hole coordinates in viewport cache projection</name>
  <files>src/main/kotlin/geo/internal/cache/ViewportCache.kt</files>
  <action>
    In ViewportCache.projectGeometryToArray:
    - For Polygon and MultiPolygon geometries, capture both exterior and interior coordinate arrays.
    - Preserve interior rings in the cached data structure.
    In renderProjectedCoordinates:
    - When a cached geometry has interiors, call writePolygonWithHoles() instead of writePolygon.
  </action>
  <verify>
    <automated>./gradlew run -Popenrndr.application=uat.ViewportCacheHolesKt</automated>
  </verify>
  <done>Viewport cache renders holes correctly in examples/render/04-multipolygons.kt with caching enabled.</done>
</task>

</tasks>

<verification>
Run the UAT caching script: `./gradlew run -Popenrndr.application=uat.ViewportCacheHolesKt`.
</verification>

<success_criteria>
- Cached render path shows transparent holes for polygons with holes
- No missing hole artifacts when caching is enabled
</success_criteria>

<output>
After gap closure, create `.planning/phases/16-rendering-improvements/16-08-SUMMARY.md`
</output>