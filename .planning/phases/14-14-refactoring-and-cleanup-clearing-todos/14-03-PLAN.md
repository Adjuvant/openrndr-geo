---
phase: 14-refactoring-and-cleanup-clearing-todos
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/main/kotlin/geo/examples/layer_BlendModes.kt
  - src/main/kotlin/geo/examples/render_BasicRendering.kt
autonomous: true
requirements:
  - CLEANUP-03
must_haves:
  truths:
    - drawDataQuadrant helper is promoted to proper API
    - render_BasicRendering.kt example is renamed or fixed
  artifacts:
    - path: src/main/kotlin/geo/examples/layer_BlendModes.kt
      provides: Example using promoted API
    - path: src/main/kotlin/geo/layer/Layer.kt
      provides: New location for drawDataQuadrant (if promoted)
    - path: src/main/kotlin/geo/examples/render_BasicRendering.kt
      provides: Renamed or fixed example file
  key_links:
    - from: "layer_BlendModes.kt"
      to: "New API location"
      via: "import statement"
---

<objective>
Promote reusable helper function to API and fix example naming

Purpose: Extract drawDataQuadrant to public API and clarify example responsibilities
Output: New API function available, example files properly named
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-CONTEXT.md
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-RESEARCH.md

Source files:
- src/main/kotlin/geo/examples/layer_BlendModes.kt (line 91 - promote helper TODO)
- src/main/kotlin/geo/examples/render_BasicRendering.kt (line 84 - fix example TODO)
</context>

<tasks>

<task type="auto">
  <name>Design API for drawDataQuadrant functionality</name>
  <files>
    src/main/kotlin/geo/examples/layer_BlendModes.kt
    src/main/kotlin/geo/layer/
    src/main/kotlin/geo/GeoSource.kt
  </files>
  <action>
    Analyze the drawDataQuadrant helper function to design proper API:
    
    1. Read the helper function at line 91-120 in layer_BlendModes.kt
    2. Understand what it does:
       - Takes drawer, data, projection, offset, dimensions
       - Renders subset of features to a specific quadrant/region
       - Takes first 200 features only
    
    3. Determine appropriate API location:
       - Option A: Add to GeoSource as renderQuadrant() or renderRegion()
       - Option B: Add to Layer class if one exists
       - Option C: Create new utility class in geo/util/
    
    4. Design API signature:
       ```kotlin
       fun GeoSource.renderQuadrant(
           drawer: Drawer,
           projection: GeoProjection,
           region: Rectangle,
           maxFeatures: Int = 200
       )
       ```
       Or similar based on actual function behavior.
    
    5. Check if Layer.kt exists and has appropriate place for this.
  </action>
  <verify>
    <automated>ls src/main/kotlin/geo/layer/</automated>
    <manual>Review Layer classes for API placement</manual>
  </verify>
  <done>API design is determined and documented</done>
</task>

<task type="auto">
  <name>Extract drawDataQuadrant to public API</name>
  <files>
    src/main/kotlin/geo/examples/layer_BlendModes.kt
    src/main/kotlin/geo/layer/Layer.kt
  </files>
  <action>
    Implement the API extraction based on design from task 1:
    
    **If adding to Layer.kt:**
    - Add new method to Layer class
    - Make it public (not private)
    - Add KDoc documentation
    - Keep parameters sensible: drawer, projection, bounds/region, feature limit
    - Ensure it handles the rendering properly
    
    **If adding to GeoSource:**
    - Add as extension function or member function
    - Follow existing GeoSource patterns
    
    **Update layer_BlendModes.kt:**
    - Remove the private helper function
    - Update calls to use new public API
    - Remove TODO comment at line 91
    - Add import for new API if needed
    
    **Clean code:**
    - Proper KDoc with @param documentation
    - Consistent naming with existing codebase
    - Error handling if needed
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -i "error" | head -10 || echo "No errors"</automated>
    <manual>layer_BlendModes.kt uses new API without TODO</manual>
  </verify>
  <done>Helper promoted to API, example updated, TODO removed</done>
</task>

<task type="auto">
  <name>Fix render_BasicRendering.kt example naming</name>
  <files>
    src/main/kotlin/geo/examples/render_BasicRendering.kt
  </files>
  <action>
    Address TODO at line 84: "this isn't basic now, this is feature rendering, intermediate. Replace with dump geojson to screen."
    
    Two options:
    
    **Option A: Rename file (recommended)**
    - Rename to render_FeatureIteration.kt
    - Update internal documentation to match
    - Remove TODO comment
    - File remains as intermediate example
    
    **Option B: Make it truly basic**
    - Simplify the example to just render GeoJSON without feature iteration
    - Remove the forEach with feature processing
    - Keep simple render call
    - Remove TODO
    
    Given the comment says "Replace with dump geojson to screen", Option A is likely intended - rename to reflect actual content.
    
    Implementation:
    ```bash
    git mv src/main/kotlin/geo/examples/render_BasicRendering.kt src/main/kotlin/geo/examples/render_FeatureIteration.kt
    ```
    
    Update any internal references if needed.
    Remove TODO comment at line 84.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -i "error" | head -10 || echo "No errors"</automated>
    <manual>File is renamed, old name no longer exists, new name compiles</manual>
  </verify>
  <done>Example properly named, TODO removed</done>
</task>

</tasks>

<verification>
- Build passes: ./gradlew compileKotlin
- No TODOs remain in layer_BlendModes.kt
- No TODOs remain in render_BasicRendering.kt (or its renamed version)
- drawDataQuadrant functionality is accessible as public API
- All tests pass: ./gradlew test
</verification>

<success_criteria>
- drawDataQuadrant promoted to public API (GeoSource or Layer)
- layer_BlendModes.kt uses public API, TODO removed
- render_BasicRendering.kt renamed to render_FeatureIteration.kt, TODO removed
- All examples compile and run
</success_criteria>

<output>
After completion, create `.planning/phases/14-refactoring-and-cleanup-clearing-todos/14-03-SUMMARY.md`
</output>
