---
phase: 14-refactoring-and-cleanup-clearing-todos
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/App.kt
autonomous: true
gap_closure: true
requirements:
  - CLEANUP-01
must_haves:
  truths:
    - App.kt exists as the canonical entry point
    - App.kt has proper documentation header
    - App.kt compiles and runs successfully
    - TemplateProgram.kt remains as alternative/template
  artifacts:
    - path: src/main/kotlin/App.kt
      provides: Canonical application entry point
      min_lines: 20
  key_links:
    - from: App.kt
      to: openrndr-geo library APIs
      via: import statements
      pattern: "import geo\\."
---

<objective>
Restore App.kt as the canonical entry point for the openrndr-geo project. Phase 14-01 incorrectly deleted App.kt as "redundant," but the user expects App.kt to be the canonical entry point, with TemplateProgram.kt serving as an alternative/template.

Purpose: Provide a clear, canonical entry point that users expect (App.kt) while keeping TemplateProgram.kt as a documented template alternative.
Output: src/main/kotlin/App.kt created with proper documentation
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-UAT.md
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-01-SUMMARY.md
@src/main/kotlin/TemplateProgram.kt

## Gap Details

From UAT.md:
- **Gap**: App.kt was deleted but user expects it as canonical entry point
- **Severity**: Major
- **User report**: "the canonical entrypoint should be App.kt which you deleted"
- **Note**: Phase 14-01 deleted App.kt as redundant duplicate of TemplateProgram.kt

## What Existed Before

App.kt was deleted in commit a8b02b6. It was approximately 140 lines and similar in structure to TemplateProgram.kt, serving as an experimental/demo entry point.

## Current State

- TemplateProgram.kt exists with full documentation and serves as the starter template
- App.kt does NOT exist (was deleted)
- User expects App.kt to be the canonical entry point

## Design Decision

App.kt should be a CLEAN, MINIMAL canonical entry point — not a duplicate of TemplateProgram.kt's complexity. It should:
1. Be simpler than TemplateProgram.kt
2. Demonstrate basic library usage
3. Have clear "canonical entry point" documentation
4. TemplateProgram.kt remains as the comprehensive starter template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create App.kt as canonical entry point</name>
  <files>src/main/kotlin/App.kt</files>
  <action>
Create src/main/kotlin/App.kt as a clean, minimal canonical entry point. It should be SIMPLER than TemplateProgram.kt, not a duplicate.

Structure:
1. Documentation header explaining this is the CANONICAL entry point
2. Minimal imports (only essential geo.* and openrndr classes)
3. Simple main() that:
   - Loads a single GeoJSON file (data/sample.geojson)
   - Creates a basic projection
   - Renders with simple styling
   - No animations, no complex features

Key differences from TemplateProgram.kt:
- Remove: Multiple datasets, geoStack, animator, image loading, font loading
- Keep: Simple, focused demonstration of core functionality
- Tone: "This is where you start" vs TemplateProgram's "This is what you can do"

The goal is ~50-80 lines vs TemplateProgram.kt's ~147 lines.

Reference TemplateProgram.kt for API usage patterns but simplify significantly.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -q "BUILD SUCCESSFUL" && echo "PASS" || echo "FAIL"</automated>
    <manual>Verify App.kt exists and has documentation header marking it as canonical entry point</manual>
  </verify>
  <done>
    - App.kt exists at src/main/kotlin/App.kt
    - File has clear documentation: "Canonical Entry Point for openrndr-geo"
    - File compiles successfully
    - File is simpler than TemplateProgram.kt (~50-80 lines vs ~147)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify both entry points work</name>
  <files>src/main/kotlin/App.kt, src/main/kotlin/TemplateProgram.kt</files>
  <action>
Verify both App.kt and TemplateProgram.kt compile successfully and can serve their respective purposes.

1. Confirm App.kt compiles: ./gradlew compileKotlin
2. Confirm TemplateProgram.kt still compiles (was modified in 14-01)
3. Check that both files have distinct purposes documented:
   - App.kt: "Canonical entry point - simple, minimal"
   - TemplateProgram.kt: "Starter template - comprehensive example"

No code changes needed — just verification that the gap is closed.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -q "BUILD SUCCESSFUL" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Both App.kt and TemplateProgram.kt compile successfully
    - Build passes with no errors
    - Gap "App.kt should be canonical entry point" is closed
  </done>
</task>

</tasks>

<verification>
1. App.kt exists at src/main/kotlin/App.kt
2. App.kt has documentation identifying it as canonical entry point
3. App.kt compiles successfully
4. TemplateProgram.kt still exists and compiles
5. Build passes: ./gradlew compileKotlin shows BUILD SUCCESSFUL
</verification>

<success_criteria>
- App.kt created as canonical entry point with proper documentation
- File compiles without errors
- TemplateProgram.kt preserved as comprehensive template
- Gap from UAT.md Test 4 is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/14-refactoring-and-cleanup-clearing-todos/14-05-SUMMARY.md`
</output>
