---
phase: 14-refactoring-and-cleanup-clearing-todos
plan: 04
type: execute
wave: 4
depends_on:
  - 14-03
files_modified:
  - All Kotlin source files (final TODO sweep)
autonomous: true
requirements:
  - CLEANUP-04
must_haves:
  truths:
    - Zero TODOs remain in entire codebase
    - Build passes
    - All tests pass
  artifacts:
    - path: "src/"
      provides: Codebase with zero TODOs
    - path: "build/test-results/"
      provides: Passing test results
  key_links:
    - from: "Phase 14 completion"
      to: "v1.3.0 milestone"
      via: "clean codebase"
---

<objective>
Final cleanup sweep and verification - ensure zero TODOs remain

Purpose: Comprehensive final check to ensure all technical debt is addressed
Output: Clean codebase with no TODOs, passing build and tests
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-CONTEXT.md
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-RESEARCH.md

All previous plans (14-01, 14-02, 14-03) should be complete before this plan runs.
</context>

<tasks>

<task type="auto">
  <name>Comprehensive TODO sweep</name>
  <files>
    src/
  </files>
  <action>
    Search the ENTIRE codebase for any remaining TODO, FIXME, XXX, or HACK markers:
    
    ```bash
    grep -rn "TODO\|FIXME\|XXX\|HACK" --include="*.kt" --include="*.kts" src/ 2>/dev/null
    ```
    
    Also check for:
    - "todo" (lowercase variations)
    - "Todo" (mixed case)
    - Any other technical debt markers
    
    For each found TODO:
    1. Determine if it was missed in previous plans
    2. Either:
       a) Fix it now (if small)
       b) Document why it's being kept (if valid reason)
       c) Remove it (if outdated)
    
    Expected result: Zero TODOs found, or all remaining TODOs have documented justification.
  </action>
  <verify>
    <automated>grep -rn "TODO\|FIXME\|XXX\|HACK" --include="*.kt" --include="*.kts" src/ 2>&1 | wc -l</automated>
    <manual>Confirm zero TODOs or document remaining ones</manual>
  </verify>
  <done>TODO count is zero (or all remaining are documented with justification)</done>
</task>

<task type="auto">
  <name>Full build verification</name>
  <files>
    build.gradle.kts
  </files>
  <action>
    Run complete build to verify all changes compile:
    
    ```bash
    ./gradlew clean compileKotlin compileTestKotlin
    ```
    
    Check for:
    - Compilation errors
    - Warnings (note them, may not need to fix all)
    - Any issues introduced by file moves/renames
    
    If build fails:
    - Identify which changes caused failure
    - Fix incrementally
    - User specified "interactive fixing is acceptable" for build issues
    
    Build must pass before proceeding to tests.
  </action>
  <verify>
    <automated>./gradlew clean compileKotlin 2>&1 | tail -10</automated>
    <manual>Build completes without errors</manual>
  </verify>
  <done>Build passes cleanly</done>
</task>

<task type="auto">
  <name>Run all tests</name>
  <files>
    src/test/
  </files>
  <action>
    Run full test suite to ensure refactoring didn't break anything:
    
    ```bash
    ./gradlew test
    ```
    
    Check:
    - All tests pass
    - No new failures compared to baseline
    - If tests fail, determine if related to refactoring or pre-existing
    
    Note: Some test failures may be acceptable if:
    - Tests were testing deprecated/experimental code that was removed
    - File moves require test updates (imports)
    
    Fix any test issues caused by:
    - Import statement changes from file moves
    - API signature changes
    - Missing test dependencies
    
    User specified "testing and building can allow interactive fixing".
  </action>
  <verify>
    <automated>./gradlew test 2>&1 | tail -20</automated>
    <manual>Review test results, ensure no regressions from refactoring</manual>
  </verify>
  <done>All tests pass or failures are explained and acceptable</done>
</task>

<task type="auto">
  <name>Final documentation updates</name>
  <files>
    README.md
    CHANGELOG.md
  </files>
  <action>
    Update documentation to reflect Phase 14 completion:
    
    **CHANGELOG.md:**
    - Add entry for Phase 14: "Code cleanup and TODO resolution"
    - List major changes (file renames, API additions)
    - Note any breaking changes (if API was modified)
    
    **README.md:**
    - Check if any examples referenced changed filenames
    - Update any links or references to renamed files
    - Ensure code examples still compile
    
    **API Documentation:**
    - If drawDataQuadrant was promoted to API, ensure it's in API docs
    - Check that new public methods have KDoc
    
    **No changes needed if:**
    - Documentation already reflects current state
    - All examples are up to date
  </action>
  <verify>
    <automated>cat CHANGELOG.md | grep -i "phase 14\|cleanup" | head -5</automated>
    <manual>CHANGELOG updated with Phase 14 notes</manual>
  </verify>
  <done>Documentation reflects all changes made in Phase 14</done>
</task>

</tasks>

<verification>
- TODO count: grep shows 0 results
- Build: ./gradlew clean compileKotlin passes
- Tests: ./gradlew test passes
- Documentation: CHANGELOG.md updated
- Phase 14-01, 14-02, 14-03 SUMMARYs exist
</verification>

<success_criteria>
- Zero TODOs remain in entire Kotlin codebase
- Build compiles without errors
- All tests pass (or documented exceptions)
- CHANGELOG.md reflects Phase 14 completion
- Code review standards met
</success_criteria>

<output>
After completion, create `.planning/phases/14-refactoring-and-cleanup-clearing-todos/14-04-SUMMARY.md`
</output>
