---
phase: 14-refactoring-and-cleanup-clearing-todos
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/render/08-feature-iteration.kt
  - src/main/kotlin/geo/examples/render_FeatureIteration.kt
autonomous: true
gap_closure: true
requirements:
  - CLEANUP-03
must_haves:
  truths:
    - render_FeatureIteration.kt moved to examples/render/
    - File renamed to follow numbered convention (08-feature-iteration.kt)
    - Examples compile and run successfully
    - Old location file removed
  artifacts:
    - path: examples/render/08-feature-iteration.kt
      provides: Numbered example demonstrating feature iteration
      min_lines: 100
  key_links:
    - from: 08-feature-iteration.kt
      to: geo.GeoSource
      via: geoSource() function
      pattern: "geoSource\\("
---

<objective>
Move render_FeatureIteration.kt from src/main/kotlin/geo/examples/ to examples/render/ and rename it to follow the numbered naming convention (08-feature-iteration.kt). The user expects examples to be in the examples/ directory with consistent numbered naming like 01-points.kt, 02-linestrings.kt, etc.

Purpose: Follow the established project convention for example organization with numbered naming in examples/render/ directory.
Output: examples/render/08-feature-iteration.kt (moved and renamed from src/main/kotlin/geo/examples/render_FeatureIteration.kt)
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-refactoring-and-cleanup-clearing-todos/14-UAT.md
@.planning/phases/14-refactoring-and-cleanup-clearing-todos/14-03-SUMMARY.md
@examples/render/README.md

## Gap Details

From UAT.md Test 6:
- **Expected**: Example file renamed correctly (render_FeatureIteration.kt exists, render_BasicRendering.kt does not)
- **Result**: Issue
- **User report**: "pass, but this should be placed in @examples/render/ and numbered/renamed in sensible way"
- **Severity**: Minor
- **Note**: File was renamed from render_BasicRendering.kt to render_FeatureIteration.kt in src/main/kotlin/geo/examples/, but user expects it in examples/render/ directory with numbered naming convention

## Current State

- File exists at: src/main/kotlin/geo/examples/render_FeatureIteration.kt
- File should be at: examples/render/08-feature-iteration.kt (following convention)

## Existing Examples Convention

examples/render/ directory contains:
- 01-points.kt
- 02-linestrings.kt
- 03-polygons.kt
- 04-multipolygons.kt
- 05-style-dsl.kt
- 06-quick-geo.kt
- 07-geostack-render.kt

Next available number: 08

## File Content Context

From 14-03-SUMMARY.md, the file demonstrates:
- Feature iteration and handling multiple geometry types
- Using renderQuadrant() API
- Intermediate complexity level (not "basic")

Appropriate name: 08-feature-iteration.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move and rename example file</name>
  <files>examples/render/08-feature-iteration.kt, src/main/kotlin/geo/examples/render_FeatureIteration.kt</files>
  <action>
Move render_FeatureIteration.kt from src/main/kotlin/geo/examples/ to examples/render/ and rename to 08-feature-iteration.kt.

Steps:
1. Read the current file at src/main/kotlin/geo/examples/render_FeatureIteration.kt
2. Update package declaration: change from `package geo.examples` to `package examples.render`
3. Update imports if needed (should still import geo.* classes)
4. Write to new location: examples/render/08-feature-iteration.kt
5. Delete the old file: src/main/kotlin/geo/examples/render_FeatureIteration.kt
6. Verify the file uses sample data paths that work from the new location (data/sample.geojson should be accessible)

The file demonstrates feature iteration using the renderQuadrant() API. Ensure all imports and data paths remain valid after the move.
  </action>
  <verify>
    <automated>test -f examples/render/08-feature-iteration.kt && test ! -f src/main/kotlin/geo/examples/render_FeatureIteration.kt && echo "PASS" || echo "FAIL"</automated>
    <manual>Verify file content preserved with updated package declaration</manual>
  </verify>
  <done>
    - File moved to examples/render/08-feature-iteration.kt
    - Old file deleted from src/main/kotlin/geo/examples/
    - Package declaration updated to examples.render
    - All imports preserved and valid
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify example compiles</name>
  <files>examples/render/08-feature-iteration.kt</files>
  <action>
Verify the moved example compiles successfully.

Run: ./gradlew compileKotlin

This will confirm:
1. Package declaration is correct
2. All imports resolve
3. No syntax errors introduced during move
4. File integrates properly with the project structure

If compilation fails, check:
- Package name matches directory structure (examples.render for examples/render/)
- All geo.* imports are still valid
- Data file paths are correct relative to working directory
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -q "BUILD SUCCESSFUL" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Example compiles without errors
    - Build passes successfully
    - Gap from UAT.md Test 6 is resolved
  </done>
</task>

<task type="auto">
  <name>Task 3: Update any references</name>
  <files>examples/render/08-feature-iteration.kt, src/main/kotlin/geo/examples/layer_BlendModes.kt</files>
  <action>
Check if any other files reference render_FeatureIteration.kt by name and update them.

Search for references:
- grep -r "render_FeatureIteration" src/ examples/ --include="*.kt" --include="*.md"

Files that might reference it:
1. layer_BlendModes.kt (from 14-03-SUMMARY.md, it was updated to use renderQuadrant() API - might mention the example)
2. Any documentation files
3. Any other examples that import or reference it

If found, update references to point to the new location/name or remove outdated references.

Also check if render_FeatureIteration.kt is mentioned in any PLAN or SUMMARY files — update those references for consistency.
  </action>
  <verify>
    <automated>grep -r "render_FeatureIteration" src/ examples/ --include="*.kt" 2>/dev/null | wc -l | xargs test 0 -eq && echo "PASS" || echo "REFERENCES_FOUND"</automated>
  </verify>
  <done>
    - No lingering references to old file name in code
    - All references updated or removed
    - Documentation consistent with new location
  </done>
</task>

</tasks>

<verification>
1. examples/render/08-feature-iteration.kt exists
2. src/main/kotlin/geo/examples/render_FeatureIteration.kt does NOT exist
3. File compiles successfully: ./gradlew compileKotlin passes
4. Package declaration is `package examples.render`
5. No lingering references to old file location/name
6. Gap from UAT.md Test 6 is resolved
</verification>

<success_criteria>
- Example file moved to examples/render/08-feature-iteration.kt
- File follows numbered naming convention (08-*)
- Old file removed from src/main/kotlin/geo/examples/
- Example compiles without errors
- All references updated
</success_criteria>

<output>
After completion, create `.planning/phases/14-refactoring-and-cleanup-clearing-todos/14-06-SUMMARY.md`
</output>
