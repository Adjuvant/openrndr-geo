---
phase: 14-refactoring-and-cleanup-clearing-todos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/App.kt
  - src/main/kotlin/TemplateProgram.kt
autonomous: true
requirements:
  - CLEANUP-01
must_haves:
  truths:
    - App.kt and TemplateProgram.kt redundancy is resolved
    - File organization follows clear purpose
  artifacts:
    - path: src/main/kotlin/App.kt
      provides: Main application or removed if duplicate
    - path: src/main/kotlin/TemplateProgram.kt
      provides: Template starter or removed if duplicate
  key_links:
    - from: "TemplateProgram.kt"
      to: "examples/"
      via: "file move or deletion"
---

<objective>
Resolve App.kt and TemplateProgram.kt redundancy and organize entry points

Purpose: Consolidate or clarify the purpose of main entry point files
Output: Clear file organization with single-responsibility entry points
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-CONTEXT.md
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-RESEARCH.md

Source files:
- src/main/kotlin/App.kt (experimental/demo file with TODOs)
- src/main/kotlin/TemplateProgram.kt (appears to be template starter)
</context>

<tasks>

<task type="auto">
  <name>Analyze App.kt and TemplateProgram.kt purpose</name>
  <files>
    src/main/kotlin/App.kt
    src/main/kotlin/TemplateProgram.kt
  </files>
  <action>
    Read both files completely to understand their purpose and relationship.
    
    Determine:
    1. Are they duplicates? (Same content)
    2. Is one a template for the other?
    3. Does each serve a distinct purpose?
    4. Which one (if any) is the "main" application entry point?
    
    Check if App.kt is used by any build configuration (build.gradle.kts) as the main class.
    Check if TemplateProgram.kt is referenced anywhere.
    
    Decision matrix:
    - If duplicates: Keep TemplateProgram.kt as the starter template, remove or rename App.kt
    - If different purposes: Clarify naming (App.kt -> Demo.kt or Example.kt)
    - If App.kt is main: Keep it, clean up TODOs, document it properly
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -20</automated>
    <manual>Verify both files still compile</manual>
  </verify>
  <done>Purpose of each file is understood and documented</done>
</task>

<task type="auto">
  <name>Consolidate or rename entry point files</name>
  <files>
    src/main/kotlin/App.kt
    src/main/kotlin/TemplateProgram.kt
  </files>
  <action>
    Based on analysis from task 1, take one of these actions:
    
    **Option A: If duplicates or App.kt is experimental:**
    - Delete src/main/kotlin/App.kt
    - Keep TemplateProgram.kt as the canonical starter template
    - Remove the TODO comments from TemplateProgram.kt lines 77,84
    - Add proper documentation header explaining it's a template
    
    **Option B: If both serve different purposes:**
    - Rename App.kt to ChainExample.kt (since it demos chain API)
    - Clean up TODOs by either:
      a) Converting to documentation comments explaining chain ordering
      b) Removing if no longer relevant
    - Keep TemplateProgram.kt as simple starter template
    - Remove experimental code from TemplateProgram.kt
    
    **Option C: If App.kt is the main:**
    - Remove TemplateProgram.kt
    - Clean up App.kt TODOs
    - Document it as main entry point
    
    Preferred approach: Option A (delete App.kt, clean TemplateProgram.kt) unless there's evidence App.kt is used as main.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | tail -5</automated>
    <manual>Verify project compiles with changes</manual>
  </verify>
  <done>File organization is clear, redundant files removed or renamed, remaining files cleaned</done>
</task>

</tasks>

<verification>
- Build passes: ./gradlew compileKotlin
- No duplicate entry points remain
- TemplateProgram.kt is clean and documented
- If App.kt remains, it's renamed and has clear purpose
</verification>

<success_criteria>
- Single clear entry point/template file remains
- TODOs at lines 77,84 of remaining file(s) are resolved
- Files have clear single responsibilities
</success_criteria>

<output>
After completion, create `.planning/phases/14-refactoring-and-cleanup-clearing-todos/14-01-SUMMARY.md`
</output>
