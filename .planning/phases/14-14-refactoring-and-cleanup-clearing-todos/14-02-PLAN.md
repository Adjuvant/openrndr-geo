---
phase: 14-refactoring-and-cleanup-clearing-todos
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/main/kotlin/geo/animation/GeoAnimator.kt
  - src/main/kotlin/geo/GeoSource.kt
autonomous: true
requirements:
  - CLEANUP-02
must_haves:
  truths:
    - GeoAnimator singleton decision is resolved (kept or refactored)
    - GeoSource padding semantics are clarified
  artifacts:
    - path: src/main/kotlin/geo/animation/GeoAnimator.kt
      provides: Animation system with resolved TODO
    - path: src/main/kotlin/geo/GeoSource.kt
      provides: GeoSource with correct padding usage
  key_links:
    - from: "GeoAnimator.kt"
      to: "Animation usage patterns"
      via: "codebase search"
    - from: "GeoSource.kt"
      to: "ProjectionFactory.fitBounds"
      via: "padding parameter documentation"
---

<objective>
Resolve GeoAnimator singleton TODO and GeoSource padding clarification

Purpose: Address architecture decision in animator and fix/Document padding semantics
Output: Clean animator code with documented design, correct padding usage
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-CONTEXT.md
@.planning/phases/14-14-refactoring-and-cleanup-clearing-todos/14-RESEARCH.md

Source files:
- src/main/kotlin/geo/animation/GeoAnimator.kt (line 114 - singleton TODO)
- src/main/kotlin/geo/GeoSource.kt (line 192 - padding TODO)
</context>

<tasks>

<task type="auto">
  <name>Analyze GeoAnimator singleton usage patterns</name>
  <files>
    src/main/kotlin/geo/animation/GeoAnimator.kt
  </files>
  <action>
    Search the entire codebase to understand how GeoAnimator is used:
    
    1. Find all usages of GeoAnimator.instance
    2. Determine if multiple animations ever run concurrently
    3. Check if the singleton pattern causes any actual limitations
    4. Look at test files to see expected behavior
    
    Command to run:
    grep -rn "GeoAnimator" --include="*.kt" src/
    
    Decision criteria:
    - If all usages are singleton and concurrent animations aren't needed: Document the design decision
    - If tests or usage suggest need for multiple instances: Refactor to allow instance creation
    
    Given creative coding use case, singleton is likely intentional (one animation at a time).
  </action>
  <verify>
    <automated>grep -rn "GeoAnimator" --include="*.kt" src/ | wc -l</automated>
    <manual>Review all GeoAnimator usages</manual>
  </verify>
  <done>Usage patterns are understood and documented</done>
</task>

<task type="auto">
  <name>Resolve GeoAnimator singleton TODO</name>
  <files>
    src/main/kotlin/geo/animation/GeoAnimator.kt
  </files>
  <action>
    Based on usage analysis from task 1:
    
    **If singleton is appropriate (most likely):**
    - Replace TODO comment at line 114 with documentation explaining the design decision:
      ```kotlin
      /**
       * Singleton instance ensures single animation focus for creative coding workflows.
       * Multiple concurrent animations are not supported by design - compose animations
       * using AnimationGroup for complex sequences instead.
       */
      ```
    - Remove the "TODO Not sure..." comment
    
    **If singleton should be removed:**
    - Remove companion object singleton
    - Make constructor public
    - Update all usages from GeoAnimator.instance to GeoAnimator()
    - Add documentation about instance lifecycle management
    
    Recommendation: Keep singleton with proper documentation (creative coding = one animation at a time).
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -i "error" | head -10 || echo "No errors"</automated>
    <manual>Verify no TODO remains at line 114</manual>
  </verify>
  <done>TODO removed, design decision documented</done>
</task>

<task type="auto">
  <name>Clarify GeoSource padding semantics</name>
  <files>
    src/main/kotlin/geo/GeoSource.kt
    src/main/kotlin/geo/projection/ProjectionFactory.kt
  </files>
  <action>
    Resolve the padding TODO at line 192 in GeoSource.kt:
    
    1. Read ProjectionFactory.fitBounds() to understand padding parameter:
       - Check parameter type (Double)
       - Check documentation on what values mean
       - Check default values elsewhere in codebase
    
    2. Current code: `padding = 0.9`
       - If padding is a ratio (0.0-1.0): 0.9 means 90% of available space (10% padding)
       - If padding is pixels: 0.9 pixels is effectively zero
    
    3. Compare with other usages in codebase:
       grep -rn "fitBounds" --include="*.kt" src/ | grep padding
    
    4. Resolution:
       - If 0.9 is correct as ratio: Add comment explaining "padding as ratio of available space"
       - If should be pixels: Change to appropriate pixel value (e.g., 20.0)
       - If uncertain: Check ProjectionFactory documentation or similar usages
    
    Most likely: 0.9 is a ratio (90% fill, 10% padding), so add clarifying comment.
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | grep -i "error" | head -10 || echo "No errors"</automated>
    <manual>Padding value is correct and documented</manual>
  </verify>
  <done>TODO replaced with clear documentation, padding semantics clarified</done>
</task>

</tasks>

<verification>
- Build passes: ./gradlew compileKotlin
- No TODOs remain in GeoAnimator.kt
- No TODOs remain in GeoSource.kt
- All changes are documented with comments
</verification>

<success_criteria>
- GeoAnimator.kt line 114 has design documentation instead of TODO
- GeoSource.kt line 192 has clarified padding with proper comment
- Both files compile and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-refactoring-and-cleanup-clearing-todos/14-02-SUMMARY.md`
</output>
