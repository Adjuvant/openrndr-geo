---
phase: 09-api-design
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: [API-01, API-02, API-03]
user_setup: []

must_haves:
  truths:
    - "Test files exist for all new API features"
    - "Each test file has placeholder tests for planned functionality"
    - "Tests compile without errors"
  artifacts:
    - path: "src/test/kotlin/geo/GeoSourceChainingTest.kt"
      provides: "Test scaffold for forEach/filter/map chainable operations"
      contains: "class GeoSourceChainingTest"
    - path: "src/test/kotlin/geo/render/DrawerGeoExtensionsTest.kt"
      provides: "Test scaffold for config block API"
      contains: "class DrawerGeoExtensionsTest"
    - path: "src/test/kotlin/geo/render/EscapeHatchTest.kt"
      provides: "Test scaffold for RawProjection and style resolution"
      contains: "class EscapeHatchTest"
  key_links: []
---

<objective>
Create test scaffolds for Phase 9 API features before implementation.

Purpose: Enable TDD-style development with tests ready for verification
Output: Three test files with placeholder tests for API-01, API-02, API-03
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-api-design/09-CONTEXT.md
@.planning/phases/09-api-design/09-RESEARCH.md

# Existing patterns to follow
@src/test/kotlin/geo/render/StyleTest.kt
</context>

<tasks>

<task type="auto">
  <name>task 1: Create GeoSourceChainingTest scaffold</name>
  <files>src/test/kotlin/geo/GeoSourceChainingTest.kt</files>
  <action>
Create test file for API-01 (feature iteration with projected coordinates).

Include placeholder test methods for:
1. `testForEachWithProjection` - verify forEach provides projected geometry
2. `testFilterReturnsProjectedSequence` - verify filter works with projection context
3. `testMapTransformsFeatures` - verify map transforms features while preserving projection
4. `testChainedOperations` - verify filter().map().forEach() chain works
5. `testWithProjectionLazy` - verify withProjection() stays lazy (Sequence-based)

Follow existing test patterns from StyleTest.kt:
- Use JUnit 4 with @Test annotation
- Import org.junit.Assert.*
- Use descriptive test names with backticks

Mark tests with `@Ignore("Implementation pending in 09-01")` so they compile but don't run yet.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.GeoSourceChainingTest" 2>&1 | grep -E "(BUILD|Test|IGNORED)" | head -20</automated>
    <manual>All test methods present and marked @Ignore</manual>
  </verify>
  <done>GeoSourceChainingTest.kt exists with 5 placeholder tests, all compile, all marked @Ignore</done>
</task>

<task type="auto">
  <name>task 2: Create DrawerGeoExtensionsTest scaffold</name>
  <files>src/test/kotlin/geo/render/DrawerGeoExtensionsTest.kt</files>
  <action>
Create test file for API-02 (two-tier API with config block).

Include placeholder test methods for:
1. `testSimpleGeoCall` - verify drawer.geo(source) works without config block
2. `testGeoWithConfigBlock` - verify drawer.geo(source) { projection = ... } works
3. `testConfigBlockProjection` - verify projection is applied from config block
4. `testConfigBlockStyle` - verify style is applied from config block
5. `testAutoFitWhenNoProjection` - verify auto-fit when projection not specified

Follow existing test patterns from PolygonRendererTest.kt or StyleTest.kt.

Mark tests with `@Ignore("Implementation pending in 09-02")`.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.render.DrawerGeoExtensionsTest" 2>&1 | grep -E "(BUILD|Test|IGNORED)" | head -20</automated>
    <manual>All test methods present and marked @Ignore</manual>
  </verify>
  <done>DrawerGeoExtensionsTest.kt exists with 5 placeholder tests, all compile, all marked @Ignore</done>
</task>

<task type="auto">
  <name>task 3: Create EscapeHatchTest scaffold</name>
  <files>src/test/kotlin/geo/render/EscapeHatchTest.kt</files>
  <action>
Create test file for API-03 (escape hatches - RawProjection and style resolution).

Include placeholder test methods for:
1. `testRawProjectionIdentity` - verify RawProjection returns input unchanged
2. `testRawProjectionBypass` - verify RawProjection bypasses projection math
3. `testStyleByFeatureFunction` - verify styleByFeature function is called per feature
4. `testStyleByTypeMap` - verify styleByType map applies style by geometry type
5. `testStyleResolutionPrecedence` - verify per-feature > by-type > global > default precedence
6. `testFeatureGeometryDirectAccess` - verify feature.geometry is accessible for custom rendering

Follow existing test patterns.

Mark tests with `@Ignore("Implementation pending in 09-03")`.
  </action>
  <verify>
    <automated>./gradlew test --tests "geo.render.EscapeHatchTest" 2>&1 | grep -E "(BUILD|Test|IGNORED)" | head -20</automated>
    <manual>All test methods present and marked @Ignore</manual>
  </verify>
  <done>EscapeHatchTest.kt exists with 6 placeholder tests, all compile, all marked @Ignore</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All three test files exist in src/test/kotlin/
2. All tests compile: `./gradlew testClasses`
3. All tests are marked @Ignore (won't fail the build)
</verification>

<success_criteria>
- [ ] GeoSourceChainingTest.kt created with 5 placeholder tests
- [ ] DrawerGeoExtensionsTest.kt created with 5 placeholder tests
- [ ] EscapeHatchTest.kt created with 6 placeholder tests
- [ ] All tests compile without errors
- [ ] All tests marked @Ignore for later implementation
</success_criteria>

<output>
After completion, create `.planning/phases/09-api-design/09-00-SUMMARY.md`
</output>
